\documentclass{article}
\usepackage{amsmath}
\begin{document}

% Works: 2-clause with LIMITOP
\[ \text{a)}\max_{p}\quad\text{b)}\lim_{p} \]

% Works: 3-clause with OPFUNCTION only
\[ \text{a)}\max_{p}\quad\text{b)}\max_{p}\quad\text{c)}\max_{p} \]

% FAILS: 3-clause with LIMITOP in 3rd position
\[ \text{a)}\max_{p}\quad\text{b)}\max_{p}\quad\text{c)}\lim_{p} \]

% FAILS: 3-clause with LIMITOP in 3rd position (sup)
\[ \text{a)}\max_{p}\quad\text{b)}\max_{p}\quad\text{c)}\sup_{p} \]

% Works: 3-clause with LIMITOP in 2nd position
%\[ \text{a)}\max_{p}\quad\text{b)}\lim_{p}\quad\text{c)}\max_{p} \]

\end{document}
