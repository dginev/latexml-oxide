\documentclass{article}
\newenvironment{myquote}{\begin{quote} MY\\}{\end{quote}}
\newenvironment{quote1}{\begin{quote} 1\\}{\end{quote}}
\begin{document}
\begin{quote}
A Quote!
\end{quote}

\begin{myquote}
A Quote!
\end{myquote}

\begin{quote1}
A Quote!
\end{quote1}

\renewenvironment{quote1}{\begin{quote} ONE\\}{\end{quote}}

\begin{quote1}
A Quote!
\end{quote1}

\def\foo{quote}
\begin\foo
a + b
\end\foo

\begin{\foo}
a + b
\end{\foo}

\def\foo{equation}
\begin\foo
a + b
\end\foo

\begin{\foo}
a + b
\end{\foo}

% env-markup-class raw-side regressions (token-free invoke markers):
% (a) font-only \newenvironment — no wrapper element; the single deposited node
% is the auto-para, which carries ltx_env_myem (documented inline-in-block caveat)
\newenvironment{myem}{\em}{}
\begin{myem}emphasized\end{myem}

% (b) nested \newenvironment envs — LIFO begin/end pairing stays correct (no
% cross-contamination). The OUTER (myquote) tags its para; the inner quote1's
% block is re-parented away from its snapshot node, so it is left untagged — the
% documented best-effort caveat for nested block envs, not a regression.
\begin{myquote}\begin{quote1}deep\end{quote1}\end{myquote}

% (c) \expandafter begin-code — token-free robustness: a marker token in the
% stream would derail this; \romannumeral5 -> "v" inside \textbf, then body "x"
\newenvironment{peek}{\expandafter\textbf\expandafter{\romannumeral5}}{}
\begin{peek}x\end{peek}

% (d) math-mode \newenvironment — IN_MATH gate: the env is NOT tagged and no
% \lx@env@begin@mark leaks into the Math tex= reversion (\langle/\rangle avoid the
% unrelated \newenvironment-wraps-alignment stray-& limitation)
\newenvironment{mw}{\langle}{\rangle}
$\begin{mw}x\end{mw}$

% (e) \NewDocumentEnvironment (l3/xparse) is NOT tagged — documented limitation.
% l3 declares the env via \cs_new (not our \newenvironment), so the lx@envmarkup
% flag is never set; the TeX-wrapper approach to set it caused a NODUMP base-load
% infinite-expansion loop and was removed for robustness. So ndfancy's quote gets
% no ltx_env_ndfancy (the inner quote is name-match-skipped too).
\NewDocumentEnvironment{ndfancy}{}{\begin{quote}}{\end{quote}}
\begin{ndfancy}nd-body\end{ndfancy}

% (f) balance under IN_MATH skew: a \newenvironment that ENTERS math in its begin-
% code (\[, IN_MATH false at \begin) and EXITS in its end-code (\], IN_MATH true at
% \end → end marker suppressed to keep markers out of math). So myeq itself is NOT
% tagged (its leaked begin is discarded by the absorb-side by-name match), but the
% FOLLOWING env must stay correctly tagged — never popping myeq's leftover snapshot.
\newenvironment{myeq}{\[}{\]}
\begin{myeq}a=b\end{myeq}
\newenvironment{aftermath}{\begin{quote} AFTER\\}{\end{quote}}
\begin{aftermath}ok\end{aftermath}

\end{document}
