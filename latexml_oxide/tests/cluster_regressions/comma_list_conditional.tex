% Regression: a comma-list LEFT of a conditional bar must parse with `|` binding
% to the LAST item (Perl): `a,b|c` -> list@(a, conditional@(b, c)). Previously
% UNPARSED (the bar-after-comma shape had no grammar rule), which was the root of
% the Class-B dangling-XMRef witness (an aligned \Pr(s_A,s_B|\Omega) whose arg
% failed to parse). See docs/parity/diagnostics/EXPECTED_ID_XMREF_DESIGN_2026-06-08.md (2026-06-26q).
\documentclass{article}
\begin{document}
\( a,b|c \)
\( a,b,c|d \)
\( x|y,z \)
\end{document}
