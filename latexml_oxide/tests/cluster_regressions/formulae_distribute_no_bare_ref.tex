% Regression: a `\quad`-separated formulae sequence whose first item is a
% comma-list-LEFT-of-relation (distribute_list_relation builds a dual with a
% relation-Apply presentation, not an XMWrap) must NOT strand a keyless bare
% <XMRef/> when a further `\quad` formula extends it. Previously
% `a,\quad b=c,\quad d=e` produced formulae@(a=c, b=c, []) — the trailing `d=e`
% became a bare ref ("Missing idref" cluster, ~370 papers; witness 0704.2334).
% The Wrap-presentation guard on the formulae/list extend paths makes the
% distribute dual fall through to a fresh dual whose refs all resolve.
% See docs/EXPECTED_ID_XMREF_DESIGN_2026-06-08.md (2026-06-26v).
\documentclass{article}
\begin{document}
\( a,\quad b=c,\quad d=e \)
\( 0,\quad p(a)=1,\quad p(b)=0 \)
\end{document}
