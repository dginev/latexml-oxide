% Two packages defining the SAME macro name differently — the shape that
% caught an over-broad hoist: promoting pkga to global made pkgb a no-op.
\ProvidesPackage{lx311pkga}
\newcommand{\lxsharedmac}{FROM-A}
