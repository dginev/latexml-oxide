window.SIDEBAR_ITEMS = {"macro":[["fontmap","Instantiates a `Font`, using the `Font` fields as keys, and calling `.into()` for each value. The specification can be partial - missing fields are taken via the `Default` trait."],["map","A flexary macro for constructing `HashMap<String, T>` maps, where `T` is generic."],["mixvec","A variant on `vec!` where each argument receives an additional `.into()` call best used with an outer context that explicitly provides the expected type, such as"],["raw_char_map","A flexary macro for constructing `HashMap<char, T>` maps, where `T` is generic"],["raw_map","A flexary macro for constructing `HashMap<K, V>` maps, where `K` and `V` are both generic (inferred at time of use)"],["s","The `s!` macro is a briefer alias for `format!`, otherwise completely identical"],["set","Simple generic helper for `HashSet<K,V>` creation Source: https://riptutorial.com/rust/example/4149/create-a-hashset-macro"],["static_map","! Helper macros for quicker and more idiomatic construction and access to data structures. A flexary macro for constructing `HashMap<&'static str, &'static str>` maps"],["stored_map","A flexary macro for constructing `HashMap<String, Stored>` maps"],["string_map","A flexary macro for constructing `HashMap<String, String>` maps"]]};