# Keep the Docker build context small + clean. The unified `Dockerfile` does
# `COPY . .` (both the `cli` and `worker` targets); without this, the multi-GB
# `target/` and the `.git/` history would be uploaded as build context and baked
# into the build (huge, slow, stale).
target/
.git
*.latexml.log
**/*.bak
