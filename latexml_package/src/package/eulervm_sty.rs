use crate::prelude::*;

#[rustfmt::skip]
LoadDefinitions!({
  // Perl: eulervm.sty.ltxml
  // There's really nothing to do for LaTeXML.
  Let!("\\upDelta", "\\Delta");
  Let!("\\upOmega", "\\Omega");
});
