use crate::prelude::*;

#[rustfmt::skip]
LoadDefinitions!({
  // Perl: chngcntr.sty.ltxml
  // This package formerly defined \counterwithin & \counterwithout
  // But is now obsolete, as those 2 macros were moved to LaTeX core (LaTeX.pool)
});
