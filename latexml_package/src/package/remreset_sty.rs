use crate::prelude::*;

#[rustfmt::skip]
LoadDefinitions!({
  // Perl: remreset.sty.ltxml
  // This package formerly defined \@removefromreset
  // But is now obsolete, as that macro has been moved to LaTeX core (LaTeX.pool)
});
