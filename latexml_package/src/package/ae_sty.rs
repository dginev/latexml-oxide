use crate::prelude::*;

#[rustfmt::skip]
LoadDefinitions!({
  // Perl: ae.sty.ltxml
  // This style file redefines fonts to use 'Almost European Computer Modern'.
  // There's really nothing to do for LaTeXML.
});
