% witness: texnegar/texnegar-xetex-bidi-leaders-hrule (+22 more bidi/xepersian/polyglossia docs)
% oracle: xetex loads bidi fine; lualatex fatals cleanly (bidi:cannot-use-engine)
% engines: preload [rawstyles,rawclasses]latexml.sty (presents pdfTeX; the sweep adds `luatex` for lualatex-oracle docs)
% expect:  0 diagnostics, `x` kept, under both profiles (56hr); never the 3.3 GB alloc_failed runaway
% status:  GREEN (batch 56ak, 2026-09-06: tex.web §484 — `\batchmode\read -1` halts the job with one Fatal in seconds;
%          guard batchmode_terminal_read_halts_the_job, now on `\RequirepTeX`)
%          -> GREEN, clean (batch 56hr, 2026-09-24: bidi is a binding; `\RequireXeTeX` passes under the default
%          persona and installs XeTeX's inter-character primitives, OXIDIZED_DESIGN #220 update;
%          guard xetex_only_packages_load_under_the_default_persona)
% mechanism (56ak): under the luatex profile bidi.sty:60 \sys_if_engine_xetex:F fires
%   \msg_fatal:nn{bidi}{cannot-use-engine}; expl3 \__msg_fatal_exit: (\batchmode
%   \read-1) did NOT abort input in oxide, so bidi.sty ran on to line 354's
%   \loop, where the UNDEFINED \XeTeXcharclass failed to consume its two count
%   args, degrading "\bidi@digits \bidi@digits@charclass" into a spurious
%   "\bidi@digits=0" assignment -> \loop never terminated -> the in-memory
%   log String (align 1) grew without bound until the 3.28 GB alloc failed.
\documentclass{article}
\usepackage{bidi}
\begin{document}
x
\end{document}
