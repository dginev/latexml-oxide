% witness: guitar (guitar.dtx:397-399 \changes{1.3}{...}{... \cmdcite{\guitarFirstLeft} ...})
% oracle:  pdflatex 0 errors (real \glossary=\@bsphack\begingroup\@sanitize\@wrglossary,
%          and \@wrglossary=\@gobble without \makeglossary -> the arg is DISCARDED unrun)
% engines: rust=1 perl=2 (2026-09-05)   full doc guitar: rust=11 perl(=SHARED)
% expect:  0 errors; document has a <ltx:p> containing "Text after."
% status:  RED
% mechanism: doc.sty \changes@ (doc.sty:629-647) does
%   \protected@edef\@tempa{\noexpand\glossary{...arg3...}} which FULLY expands arg3.
%   \cmdcite{\guitarFirstLeft} = \texttt{\textbackslash\guitarFirstLeft};
%   \guitarFirstLeft (guitar.sty:92) expands to \def\gtr@CrOnHook{...}, and
%   \gtr@CrOnHook (=\let->\toolboxEmpty, toolbox.sty:63, an empty EXPANDABLE macro)
%   expands to nothing inside the \edef -> a MALFORMED \def{...}. \@tempa then runs
%   \glossary{...\def{...}...}. Our \glossary (sect11.rs:919, mirroring Perl
%   latex_constructs.pool.ltxml:4481, a digested {} constructor) EXECUTES the arg
%   [same underlying retokenize-executes-CS as the \index witness], so \def{ opens a
%   boxing { group that the trailing \endgroup (matching \changes's \begingroup,
%   doc.sty:626) closes -> "close non-boxing group", cascading into all downstream
%   mode-frame errors. Real \glossary GOBBLES the group unrun -> pdflatex clean.
\documentclass{article}
\usepackage{doc}
\makeatletter
\let\toolboxEmpty\@empty
\def\guitarFirstLeft{\def\gtr@CrOnHook{\rightskip 0pt plus 1fil\relax\parfillskip 0pt\relax}}
\let\gtr@CrOnHook\toolboxEmpty
\let\orichanges\changes
\def\changes#1#2#3{\orichanges{#1}{#2}{#3}}
\newcommand{\cmdcite}[1]{\texttt{\textbackslash#1}}
\begin{document}
\changes{1.3}{2001/08/24}{Introduced \cmdcite{\guitarFirstLeft} and made \cmdcite{guitarFirstFlush} default}
Text after.
\end{document}
