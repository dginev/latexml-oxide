% CONTROL (raw): $ {x^2$} — math open, brace group open, closing $ inside the group.
% oracle: pdflatex — verify whether real TeX recovers (Missing } inserted) or is clean.
\documentclass{article}
\begin{document}
a $ {x^2$} b
\end{document}
