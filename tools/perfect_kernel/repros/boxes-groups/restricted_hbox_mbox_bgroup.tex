% witness: newcommand/newcommand (syntax.sty:158 \mbox\bgroup); minimal kernel form
% oracle:  pdflatex 0 errors
% engines: rust=1 perl=1 (2026-09-03)
% expect:  0 errors; <ltx:text> box wraps the A B content, no \mbox end-mode error
% status:  GREEN (batch 54m, 2026-09-03)
% guard:   perfect_kernel_batch54::implicit_bgroup_argument_reads_its_group
% mechanism: a box CONSTRUCTOR (\mbox = DefConstructor "{}" mode=>text bounded)
%   whose {} argument is opened by an implicit begin-group token \bgroup (not a
%   literal `{`) and closed by \egroup from a different macro. Rust's arg reader
%   does not scan the balanced \bgroup..\egroup group, so \bgroup executes inside
%   the box as its own boxing frame; \mbox's end_mode then meets that \bgroup frame.
%   tex.web: an undelimited/{}-argument scan accepts an implicit begin-group char
%   (\let\bgroup={) as a group opener (TeXbook ch.24 "grouping").
\documentclass{article}
\def\OPEN{\mbox\bgroup A}
\def\CLOSE{ B\egroup}
\begin{document}
X\OPEN\CLOSE Y
\end{document}
