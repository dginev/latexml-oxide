% witness: biblatex-sbl/sbl-paper (sbl-paper.tex:229 \printbibliography[heading=bibintoc];
%          also biblatex-sbl.tex, biblatex-sbl-ibid.tex — the biblatex-sbl `\blx@*` family)
% oracle:  pdflatex 0 errors (verified 2026-09-03; \printbibliography w/o biber only warns)
% engines: rust=2 perl=? (2026-09-03) -- RUST-ONLY: Perl has NO biblatex.sty.ltxml and RAW-loads
%          biblatex.sty, so \blx@key@bibcheck (biblatex.sty:9643) and \blx@printbibliography
%          (biblatex.sty:9820) ARE defined there; the Rust binding stands in for biblatex.sty and
%          omits them.
% expect:  0 errors; \printbibliography yields an <ltx:bibliography> (empty w/o biber is fine)
% status:  RED
% mechanism: the Rust biblatex binding (latexml_contrib/src/biblatex_sty.rs:727) does NOT raw-load
%   biblatex.sty -- it stands in for it, emulating a subset and defining its own \printbibliography[]
%   (biblatex_sty.rs:1995, -> \biblatex@printbibliography native worker). But the biblatex-sbl STYLE
%   raw-loads biblatex-sbl.def, whose line 663 `\renewrobustcmd*{\printbibliography}` OVERRIDES the
%   binding's \printbibliography with a copy of biblatex's REAL one:
%     \begingroup \blx@key@bibcheck{bibliography} ... \@ifnextchar[{\blx@printbibliography}{\blx@printbibliography[]}
%   That real body reaches \blx@key@bibcheck (biblatex.sty:9643, the `check=' option handler) and
%   \blx@printbibliography (biblatex.sty:9820, the render worker) -- neither defined by the binding.
%   Isolated: style=sbl triggers it (any/no \printbibliography option), style=authoryear is CLEAN
%   (its \printbibliography stays the binding's native macro; \meaning confirms the swap).
% fix: latexml_contrib/src/biblatex_sty.rs -- define the two internals the raw .def reaches:
%     \blx@key@bibcheck#1  -> faithful biblatex.sty:9643 (\ifcsdef{blx@bibcheck@#1}{\letcs\blx@bibcheck
%                             {blx@bibcheck@#1}}{}) or a no-op gobble (the check filter is cosmetic here)
%     \blx@printbibliography[#1] -> the binding's native worker \biblatex@printbibliography[#1]
%   so the sbl-overridden \printbibliography routes to native rendering. VALIDATED: providing these
%   two (\blx@key@bibcheck as no-op, \blx@printbibliography -> \biblatex@printbibliography) -> 0 errors.
\documentclass{article}
\usepackage[style=sbl,backend=biber]{biblatex}
\begin{document}
Text.
\printbibliography
\end{document}
