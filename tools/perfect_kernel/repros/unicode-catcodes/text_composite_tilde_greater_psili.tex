% A text composite keyed on an OTHER character: greek-fontenc.def:226
% \DeclareTextCompositeCommand{\~}{\LastDeclaredEncoding}{>}{\accpsiliperispomeni}
% (and :221 {<} -> \accdasiaperispomeni) makes `\~>{\alpha}` the psili +
% perispomeni accent on alpha. Rust applies the plain tilde accent to `>` and
% prints the alpha bare. Not a case-change issue: plain text shows it (found
% while fixing W10's \MakeLowercase{\~>{\alpha}} in char-list-alphabeta).
% The native `\~` (setup_binding_language.rs DefAccent!) now looks up the
% encoding's declared composite `\<enc>\~-<key>` when the key has no
% precomposed form (latex.ltx:9938-9947 `\@text@composite`).
% preload: [luatex,rawstyles,rawclasses]latexml.sty
% witness: greek-fontenc char-list-alphabeta, alphabeta-doc (lualatex)
% oracle:  lualatex 0 errors: [ἆ] [ἇ] (psili/dasia + perispomeni + alpha)
% engines: rust (56ja + W10) = [>̃α] [<̃α]
% expect:  0 errors, 0 warnings; [ἆ] [ἇ]
% guard:   greek_text::declared_composite_on_a_punctuation_key
% status:  GREEN modulo NFC (W11, 2026-09-25; was RED): the text is α U+0313
%          U+0342 (tuenc-greek.def `\accpsiliperispomeni` appends two `\char`s),
%          which NFC-normalizes to the oracle's ἆ
\documentclass{article}
\usepackage{fontspec}
\usepackage[normalize-symbols]{alphabeta}
\begin{document}
[\~>{\alpha}] [\~<{\alpha}]
\end{document}
