% mechanism: latex.ltx:9903-9904 `\DeclareTextAccent#1#2#3{\DeclareTextCommand#1{#2}{\add@accent{#3}}}`
%   keeps the slot argument in the command it defines, for `\accent` to scan at each use. The binding
%   reads the slot at declaration to find the combining mark ({Number}); since 56jr what followed the
%   number came back as input there, run in the preamble.
% witness: TL manual amsldoc-vi (4 -> 6 warnings in 56jr; vntex pd1supp.def:5-6
%   `\DeclareTextAccent{\h}{PD1}{\texthookabove}`)
% oracle:  pdflatex 0 errors: "X"
% engines: rust 56jr = 2 warnings (Missing number; "\relaxx" read after \DeclareTextAccent);
%          perl = 1 warning (Missing number)
% expect:  0 errors, 1 warning (the slot is not a number)
% status:  RED (56ju) -> GREEN (56ju)
\documentclass{article}
\def\myacc{\relax x}
\DeclareTextAccent{\hh}{OT1}{\myacc}
\begin{document}
X
\end{document}
