% witness: objectz/ozguide (28 err), zed-csp (zed), askmaps (sans), iwonamath (iwonacondensed)
% oracle: pdflatex CLEAN. engines: pdflatex.
% class: SHARED (Perl \DeclareMathVersion is undef/no-op too, latex_constructs.pool:2658;
%   \mathversion errors on unknown, :5290) — surpass: register declared versions.
% expect: 0 Error; \mathversion{oz} accepted because \DeclareMathVersion{oz} registered it.
% status: RED with b54x (Error:unexpected:oz Unknown math version 'oz').
% mechanism: \DeclareMathVersion{} = def_primitive_noop (sect08.rs:715) — does NOT register the
%   name; \mathversion{} (sect13.rs:1058-1067) accepts only bold/normal, else errors. oz.sty:34
%   \DeclareMathVersion{oz} then :70 \mathversion{oz}. CONTROL: an UNDECLARED version must still error
%   (guard mathversion_unknown_version_errors.tex: \mathversion{wobble}).
\documentclass{article}
\makeatletter
\DeclareMathVersion{oz}
\makeatother
\begin{document}
\mathversion{oz}$x=1$
\end{document}
