% CONTROL: \mathversion of an UNDECLARED version MUST still error (pdflatex errors too).
% status: RED expected to STAY red (this is a genuine error) — the fix must not silence it.
\documentclass{article}
\begin{document}
\mathversion{wobble}$x$
\end{document}
