% witness: ltx-talk/verbatim-content (ltx-talk.cls:158 \cs_new:Npn \thepage)
% oracle:  pdflatex 0 errors (standalone class; \thepage free, article not loaded)
% engines: rust=1 perl=0 (2026-09-05); RUST-ONLY (Perl aborts cls at \NeedsDocumentMetadata:32)
% expect:  0 errors; body contains the page number text (\thepage replaced, no <ltx:ERROR>)
% status:  RED
\begin{filecontents*}[overwrite]{poolc.cls}
\NeedsTeXFormat{LaTeX2e}\ProvidesClass{poolc}[2026/01/01 pk-expl3]
\renewcommand\normalsize{\fontsize{10pt}{12pt}\selectfont}
\ExplSyntaxOn
\cs_new:Npn \thepage { \int_to_arabic:n { \value{page} } }
\ExplSyntaxOff
\normalsize
\end{filecontents*}
\documentclass{poolc}
\begin{document}
Page \thepage.
\end{document}
