% witness: contract/contract-example-en (contract.sty:336 \edef\cl@Clause{\cl@Clause\cl@contractClause}; counter made at :353 \newcounter{contractClause})
% oracle:  pdflatex 0 errors
% engines: rust=1 perl=1 (2026-09-03) — SHARED
% expect:  0 errors; \x expands empty (no undefined-cs), body has a <ltx:para>
% status:  GREEN (batch 54m, 2026-09-03)
% guard:   perfect_kernel_batch54::reset_list_is_an_expandable_cl_macro
% mechanism: NewCounter (counter/dialect.rs:106-108 = Package.pm:674) stores the reset
%   list \cl@<ctr> as a STATE VALUE, never as an expandable macro. latex.ltx \@definecounter
%   does \global\expandafter\let\csname cl@#1\endcsname\@empty, so raw package code that
%   expands \cl@<ctr> as a macro (contract.sty, afthesis \@removefromreset) breaks.
\documentclass{article}
\makeatletter
\newcounter{foo}
\edef\x{[\cl@foo]}
\makeatother
\begin{document}
\x
\end{document}
