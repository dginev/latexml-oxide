% witness: kotex-utf/kotex-doc, kotex-oblivoir/obsideparas, istgame/istgame-doc
% oracle: pdflatex clean (ko.TeX utf8 active-char path)
% engines: pdflatex (bytes) vs LaTeXML (unicode-native)
% expect: \dhucs@hu defined after \RequirePackage{dhucs}
% status: RED b56a
% mechanism: dhucs.sty:42 `\ifx 가가\else\RequirePackage{kotexutf}\endinput\fi`
%   is a byte-level engine probe; unicode-native LaTeXML sees `가` as ONE token
%   so \ifx is TRUE, skips kotexutf, never defines \dhucs@hu (kotexutf-core.tex:177)
\documentclass{article}
\makeatletter
\RequirePackage{dhucs}
\newdimen\x@hu \x@hu=\dhucs@hu
\makeatother
\begin{document}
ok
\end{document}
