# Perfect-kernel handoff — 2026-09-01 (session restart for Opus 4.8 subagents)

Branch `perfect_kernel`, never pushed. Goal: docs/PERFECT_KERNEL.md. Standing
directives in force: ≤8 read-only root-causing subagents, plans ledgered in
`PLANS.md`, fixes executed from the main session; first-principles root causes
(latex.ltx / real .sty / tex.web / Perl); every root fixed with a red/green
minimal-repro guard that names the witness PLUS a separate vetting of the
witness's full XML; batch 3-5 fixes per suite run; R9 mode-frame family and the
Japanese/pTeX family are USER-PARKED; surpass-Perl shapes need user approval
(P16 list pending). **Subagents: use `subagent_type: "root-causer"`**
(`.claude/agents/root-causer.md`, pinned to claude-opus-4-8 / xhigh) — the user
rejected Opus 5; the `opus` alias resolves to Opus 5.

## State at handoff

- HEAD `63af737813` = batch 44 (`\newtcbinputlisting`, counter sharing) —
  guard `tcbinputlisting_inputs_file` green, full suite 2225/2225 green, clippy
  clean. incgraph manual vetted: tcb@cnt errors 15→0; residual 125 =
  `\endlx@list` "Attempt to close boxing group" across `\begingroup` (mode-frame
  adjacent, parked); manual needs `--timeout=300` (150-image `\incmultigraph`
  example executes, ~74 s).
- **Sweep 27 (batches 43-44) was running in the background** from
  `$SCRATCHPAD/sweep27_cwd/` with `WORKER_BIN=~/data/perfect_kernel/bin_sweep27
  JOBS=12 TIMEOUT_S=120`, ~885/2374 verdicts written when this session ended.
  The sweep skips docs that already have `verdict.tsv`, so if the restart killed
  it, RE-LAUNCH the same command (do NOT delete verdicts) and it resumes. Its
  `.latexml.log` files land in the launching CWD (that scratch dir, not the repo
  root). Tally: concat `~/data/perfect_kernel/*/*/verdict.tsv` → zero-error
  count, oracle slice (join `oracle_verdicts.tsv`, engine pdflatex|lualatex,
  exit 0, errs 0; sweep-26 slice = 1,049/1,548), regression diff vs
  `sweep26_all.tsv`; then LEDGER row #27.
- Previous-session leak cleanup done: sweep-26-era disk writes (`*.code`,
  `*-cnltx-N.tex`, `*.tmp`, `example000N.ml`, `*.tcbtemp`, `n.out/xml`) were
  the pre-batch-43 listings `writefile` path; all listing/tcolorbox writes are
  VFS-only now.

## Wave-3 investigations to (re)launch on `root-causer`

Shared preamble: ``docs/perfect_kernel/AGENT_PREAMBLE_W3.md` (copy also in the old scratchpad)` (binary, command, hard rules,
deliverable format). Witness paths are relative to
`/usr/local/texlive/2025/texmf-dist/doc/`. Sweep-26 oracle-clean ranking:

| tag | witnesses | signal |
|---|---|---|
| w3A | latex/numerica/numerica.tex | 100 malformed:ltx + Fatal Stomach:Recursion (`\lx@dual` ×200 nested in inline math) |
| w3B | brandeis-problemset/example (69 malformed + F), glosmathtools sample_en/fr (53 each; KPE #84 family?) | malformed:ltx |
| w3C | tagpdf/tagpdf (92 `_`), derivative/derivative (22 `_` + 39 malformed) | unexpected:_ (P15 everyeof stays unwired — quantify, don't re-wire) |
| w3D | pgfornament/tikzrput (37 malformed), makeshape/testsample (22), tabularray-abnt-pt-br (16 `_`), movie15/overlay-example (11 `_`) | mixed |
| w3E | Timeout:Recursion biblatex-apa-test, zugferd ×2, panda-doc; PushbackLimit europecv, istgame-doc, msc, tex-font-cheatsheet; Stomach:Recursion knowledge; aguplus `read_newline_args` assertion | loop fatals |
| w3F | bibarts (10+4), nmbib-sample (4), xskak_and_beamer (2), gaceta ×2, functional, pst-exa-doc | unexpected:fi (P10 rule: define the conditional, never touch the skipper) |
| w3G | sunpath ×5 (1000-cap), pmdraw (`undefined:usevalue` ×501), cahierprof-exemple (`unexpected:&` ×501), hobby (`latex:(tikz)` ×501), nicematrix ×2 (1000-cap) | runaway storms |
| w3H | polydemo, fspsample ×2, newcommand, bullcntr-man, calctab_manual, windycity, t-manual, synthslant-gauge, shipunov autolist-ex-ru/en, biblatex-gost, biblatex-sbl | TooManyErrors(100) singles |

Then: record each FINAL plan as a PLANS.md row, execute in risk order in
batches of 3-5 with guards (`latexml_oxide/tests/cluster_package_guards.rs`
pattern) + witness XML vetting, sweep every 2-3 batches.
