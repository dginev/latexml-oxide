# Perfect Kernel — progress ledger (living)

Protocol and status legend: [README.md](README.md). Newest sweep first.
Per-document artifacts live under `~/data/perfect_kernel/<bundle>/<name>/`
(out of repo); the sweep tally file is `~/data/perfect_kernel/sweep_verdicts.tsv`.

## Oracle pass (same-TL ground truth)

2026-08-31, TL2025, `tools/perfect_kernel/oracle.sh` (pdflatex, lualatex
fallback/detection, 90s): **1548 / 2374 oracle-clean** (1248 pdflatex + 300
lualatex), 801 DOCUMENT-STALE (the shipped .tex no longer compiles on this
TL — e.g. a4wide.tex vs siunitx v3), 25 timeout. **The S1 bar applies to the
1548 oracle-clean docs only.**

## S2 schema-validation baseline (sweep-5 XMLs)

2026-08-31, `tools/perfect_kernel/validate.sh` (jing, urn-resolved schema):
**1,409 / 2,329 produced core XMLs are RelaxNG-valid (60.5%)**; 10,899
validation error lines across the remainder (`validate_verdicts.tsv`).

**Sweep 27 re-measure (2026-09-01):** all completed docs **1,587 / 2,331
valid (68.1%, +7.6 pp)**; S0∧S1 slice **867 / 1,031 (84.1%)**. Top
signatures (whole set): `class` not NMTOKENs 2,157; `svg:foreignObject`
missing width/height 1,975; `xml:id` with colons 1,179; `stroke-linecap`
value 1,105; `text`/text inside `listing` (expects `listingline`) 375+116;
`block` misplaced 273; `tags` in inline context 221; `align` value 180;
`XMApp` outside Math 147. Worst docs: csvsimple-legacy 13,947,
chemplants-doc 3,659, optikz 2,024, xassoccnt 1,952, codeanatomy 1,825.
The first four signatures are mechanical attribute-shape fixes (≈63% of
the mass) — queued as lane S2a.

## S3 content-recall baseline (sweep-27 S0∧S1 slice)

2026-09-01, `tools/perfect_kernel/s3_audit.sh` (pdftotext word recall) over
the 1,031 S0∧S1 oracle-clean docs: **median 98.4%**; ≥95%: 763 (74.0%),
90-95%: 80, 80-90%: 59, 60-80%: 34, <60%: 95 (9.2%). No document has XML
> 2× the PDF word count (no duplication). 25 docs at **0.0%**: 10 have empty
PDFs (bclogo/syracuse, blochsphere, cartonaugh, comment/t1test, incgraph ×3,
babybeamer10, matrix-skeleton, pagelayout-guides — not bugs); 15 have
content the XML lacks entirely: knowledge (1,694 words), europecv (1,204),
figchild (925), ffslides-doc (871), cistercian-doc (240),
circularglyphs-doc (228), randintlist-l3-doc (172), frontespizio ×6,
fillwith, pgfPT.colorSchemes.info — queued as lane S3a (zero errors with
zero content is the worst kind of silent failure).

## Sweep history

| Date | Binary (commit, profile) | Corpus | Timeout | status 3/124/137 | status 2 | status 1 | status 0 | Notes |
|---|---|---|---|---|---|---|---|---|
| 2026-08-31 #1 | 1ef264a2bb release | 2374 | 120s | 40/5/0 | 1507 | 363 | 459 | **INVALIDATED** — run against the stale dual-TL dump (TL2023-vintage latex.ltx/expl3 labeled "2025"); kept for the before/after delta only |
| 2026-08-31 #2 | 9e347a3f15+161 release | 2374 | 120s | 34/10/0 | 1451 | 399 | 480 | fresh TL2025-pinned dump + OXIDIZED_DESIGN #161 DefPlain fix. Total error mass 145,375. **Oracle-clean slice (1548 docs): 424 clean + 310 warn-only (47%), 770 status-2, 8 fatal, 1 timeout; 58k error mass.** |
| 2026-08-31 #3 | +163/164/silence release | 2374 | 120s | 31/12/0 | 1449 | 400 | 482 | #163 makeindex, #164 raw-opt record, silence `\sl@StoreMessage` |
| 2026-08-31 #4 | 7ce3bb8005 release | 2374 | 120s | 28/16/0 | 1402 | 443 | 485 | + #165 `\@currsize`, beamer-hyperref parity, `\Hy@MakeCurrentHref`, KOMA minisec/labeling, memoir geometry, l3backend at begin-document. **Error-free 928 (39.1%), up from 822 baseline.** (Binary predates ltxdockit binding + `\@raw@classoptionslist`.) |
| 2026-08-31 #6 | b4aabe1e7e-era release | 2374 | 120s | 39/4/0 | 1362 | 471 | 498 | + \BreakableUnderscore, \glossary schema guard. **Error-free 969 (40.8%).** |
| 2026-08-31 #9 | 2486952cd1 release | 2374 | 120s | 26/29/0 | 1366 | 455 | 498 | **MIS-GATED luatex profile** — the harness matched every pdflatex-FALLBACK oracle row, profiling ~700 pdfLaTeX-authored docs as LuaTeX: +78k error mass. Kept as the lesson; gate fixed same day (clean-lualatex rows only). Timeouts also contention-inflated. |
| 2026-08-31 #10 | 00d89f0803 release | 2374 | 120s | 39/5/0 | 1353 | 476 | 501 | luatex profile correctly gated (264 clean-lua docs). **Error-free 977 (41.2%), error-mass 154,630.** METRIC NUANCE: the clean-lua slice reads *worse* on shallow error counts than with no profile (dead branches error less than live ones) while being strictly better on CONTENT — the LuaTeX branches now run and expose their real remaining surface (50k mass in that slice = the next worklist). |
| 2026-08-31 #11 | 5c390d3e5c **debug** | 2374 | 120s | 23/29/0 | 1347 | 474 | 501 | **Lua rebind-as-we-emulate banked** ([LUA_REBINDING.md](LUA_REBINDING.md)): live register mirror, babel Lua API up (locale_props class extinct), kpse require, `\lx@directlua` fidelity. Error-free 975 (41.1%), error-mass 154,615 (flat — the Lua wins are content-side, as predicted by the sweep-10 nuance); **fatals 39→23**. Oracle-clean slice: 446+373 = **819 error-free of 1,548 (52.9%)**, 71.9k mass. Timeouts 5→29 is a measurement artifact: full re-run (no cached verdicts) on the DEBUG binary under 12-way contention; spot-checks solo: tikz-cd-doc 25 s, spreadtab-en 63 s — both complete. Compare future sweeps at equal cache state. |
| 2026-08-31 #12 | 02ae925c2f debug | 2374 | 120s | 30/16/0 | 1306 | 523 | 499 | Banks batches 2-3 (14 fixes + surpasses #169-171). **Error-free 1,022 (43.1%), error-mass 136,228 (−18.4k)**; oracle-clean slice 444+412 = **856/1,548 (55.3%)**. Timeouts halved vs #11 (16); fatals 23→30 — includes pgf-spectra-class docs whose unlocked real work now exceeds the debug budget. |
| 2026-08-31 #13 | e7d056843d(batch4)+batch5 debug | 2374 | 120s | −/72/0 | 1298 | 489 | 515 | Banks batches 4-5. **Error-mass 115,478 (−20,750, −15.2%)**; zero-error 1,004 nominal but 72 timeouts (vs 16) are contention-inflated (agents+gates+builds ran concurrently) — 0 of the 72 were previously clean. 41 newly clean, 22 "regressions" of which the fontspec-family (source{code,sans}pro, hvfloat ×6, libertinus chain) are **oracle-faithful corrections**: the engine previously mis-took the `\ifxetex` TRUE branch inside these .sty files (old logs show xunicode+fontspec loading under pdfTeX) — real pdflatex also lacks `\setmonofont`/`\addfontfeature` there; docs move to the D6 lua/xe-profile slice. HARNESS NOTE: sweep ran with cwd=texmf (read-only) so logs failed to write (`Permission denied`) and first-pass verdicts counted STALE sweep-12 logs; rebuilt from `.stdout` "Conversion complete/failed" summaries (`sweep13_verdicts.tsv`). Future sweeps: pass `--log` into the writable outdir or cd to a writable dir with TEXINPUTS pointing at the source dir. |
| 2026-08-31 #14 | batch-10-era debug (bin_sweep14) | 2374 | 120s | 24/34/0 | 1242 | 548 | 526 | Banks batches 6-10 via the PROPER harness (tools/perfect_kernel/sweep.sh — stderr-captured logs, immune to the CWD-log trap). **Zero-error 1,075 (45.3%), error-mass 82,808 (−39% vs #12)**; oracle-clean slice **894/1,548 (57.8%)**, slice mass 30,095, slice fatals 6, slice timeouts 6. Fatals 30→24; timeouts 34 (suite ran concurrently — still partly contention). Batches 11+ (beamerswitch/mode, def_parser Match fallback) land after this binary. |
| 2026-08-31 #15 | batch-12-era debug (bin_sweep15) | 2374 | 120s | 26/31/0 | 1242 | 548 | 527 | Banks batches 11-12. Zero-error 1,076, error-mass 82,458, oracle-clean slice 896/1,548 (57.9%) — ≈flat vs #14: the batch-11/12 wins (beamerswitch, expl3 half-state, adtrees) were narrow at corpus level; the ctex/jlreq tail stays erroring on OTHER (CJK) causes. Batches 13-14 (listings write-file tee, tcbwritetemp/NewTCBListing, `\#`/`\&`/`\%` chardef restoration, catoptions stub removal) land after this binary. |
| 2026-08-31 #16 | batch-14-era debug (bin_sweep16) | 2374 | 120s | 28/22/0 | 1237 | 559 | 528 | Banks batches 13-14. **Zero-error 1,088 (45.8%), error-mass 81,327; oracle-clean slice 900/1,548 (58.1%)**, slice mass 29,908, slice timeouts 3. Session arc so far (batches 4-15): zero-error 1,022→1,088, slice 55.3→58.1%, mass 136k→81k (−40%). Batch 15 (beamer `<>` definition forms, star-prototype audit, beamer xcolor) lands next sweep. |
| 2026-09-01 #17 | batch-17-era debug (bin_sweep17) | 2374 | 120s | 11/29/0 | 1215 | 591 | 528 | Banks batches 15-17. **Zero-error 1,119 (47.1%), oracle-clean slice 930/1,548 (60.1%) — first crossing of 60%**; error-mass 78,735; fatals 28→**11** (the batch-17 pushback cap + protection gate: 4 OOM deaths now recoverable), slice fatals 2, slice timeouts 2. Session arc (batches 4-17): zero-error 1,022→1,119, slice 55.3→60.1%, mass 136k→79k (−42%), fatals 30→11. Batches 18-19 land next sweep. |
| 2026-09-01 #18 | batch-20-era debug (bin_sweep18) | 2374 | 120s | 10/30/0 | 1207 | 597 | 530 | Banks batches 18-20 (incl. the ExpandedSemiverbatim expl3-class root fix). **Zero-error 1,127 (47.5%), slice 937/1,548 (60.5%), mass 73,072** (slice mass 25,857; slice fatals 2, timeouts 2). Batches 21-22 (CJK surface — einfart 267→0 — and Form/hycolor/blx@kv) land next sweep. **Emerging decision point for the user**: the remaining 607 erroring oracle-clean docs are increasingly dominated by the USER-PARKED mode-frame family (tram/nih 33-bundle `unexpected:\CS`, mdframed/cnltx cascades, hep-math, windycity residuals, the nicematrix exemplar's last error) — unparking it is the single largest remaining lever. |
| 2026-09-01 #19 | batch-23-era debug (bin_sweep19) | 2374 | 120s | 19/21/0 | 1184 | 618 | 532 | Banks batches 21-23 (CJK/tagging surfaces, Form env, blx@kv, TooManyErrors latch). **Zero-error 1,156 (48.7%), oracle-clean slice 961/1,548 (62.1%); error-mass 72,939** — MASS CAVEAT: the batch-22 TooManyErrors latch caps per-doc logged errors at ~101, so mass is no longer comparable to pre-latch sweeps (compare zero-error and slice% instead). Fatal fingerprint shift: 156 docs now end in the explicit `Fatal:TooManyErrors:MaxLimit` (previously silent 100+-error logs). Batch 24 (`\DocumentMetadata` conditionals — ltx-talk 10/12→0) and batch 25 (align-ledger tex.web protocol — l3doc `{function}` stray-`&` family, coffin cluster) land next sweep. |
| 2026-09-01 #20 | batch-25-era debug (bin_sweep20) | 2374 | 120s | 22/34/0 | 1179 | — | — | Banks batches 24-25 (DocumentMetadata conditionals, tex.web align-ledger protocol, tag_ expl3 layer, rhai 1.26). **Zero-error 1,179 (49.7%), oracle-clean slice 981/1,548 (63.4%)**; error-mass 72,261 (latch-capped basis). Stray-`&` docs 54→25 (l3doc family dead; residual 25 = other mechanisms, re-rank). The deep gullet-protocol change shows NO corpus regression. Batch 26 (pgfornament TEX-format kpse fallback, beamer color model) lands next sweep. |
| 2026-09-01 #21 | batch-27-era debug (bin_sweep21) | 2374 | 120s | 18/35/0 | 1176 | — | — | Banks batches 26-27. Zero-error 1,176 (49.5%), oracle slice 974/1,548 (62.9%) — NET −3/−7 vs #20 despite the cluster wins: batch-26 theme raw-loading regressed 10 previously-clean theme docs (celestia ×6, gotham-simple, phnompenh, beamerstructure1) on missing beamer option/template surface while fixing 7 others; batch-28 patches the surface (re-verified 0-2 errs). pgfornament/updatemarks/rbt-mathnotes/unicodefonttable wins land in this sweep's logs. |
| 2026-09-01 #22 | batch-30-era debug (bin_sweep22) | 2374 | 120s | —/—/0 | 1188 | — | — | Banks batches 28-30. **Zero-error 1,188 (50.0% — the corpus half-way mark), oracle slice 982/1,548 (63.4%)**, mass 71,366. Only 2 regressions vs #21, both known-cause: dijkstra-fr 0→7 (pagegoal=max_dimen flips a dijkstra.sty branch → `\dijk_last_col_type` template tail) and elsdoc 0→14 (batch-29 verbatim interplay, pdfwidgets doc) — revisit. Batches 31-32 (ctex expl3 exit gating 1001→0 ×family, hyperref forms) land next sweep. |
| 2026-09-01 #23 | batch-34-era debug (bin_sweep23) | 2374 | 120s | —/—/0 | 1165 | — | — | Banks batches 31-34. Zero-error 1,165 (49.1%), slice 967/1,548 (62.5%), mass 89,271 — NET −23/−15 vs #22: the batch-34 `Until:` per-iteration Error regressed 63 docs (spath3/litetable/zref-check families at the 501 consecutive-error cap) while the headline fixes (ctex 1001→0 ×family, biblatex, elsdoc, willowtreebook, xint) landed. Batch 35 repairs the regression (quiet Until-misses; artificial-mouth-end insight) — re-verified 0/0 on the top regressed witnesses. |
| 2026-09-01 #24 | batch-35-era debug (bin_sweep24) | 2374 | 120s | —/—/0 | 1227 | — | — | Banks batches 31-35 (the fan-out arc). **Zero-error 1,227 (51.7%), oracle slice 1,015/1,548 (65.6%); error-mass 61,654.** Vs #22: +39 net (40 newly-clean, 1 regression: flowfram/ffuserguide 0→6 — was a MemoryBudget-fatal doc before, now runs into new surface). Session arc (batches 4-35): zero-error 1,022→1,227 (43.1→51.7%), slice 55.3→65.6%. Batches 36-37 (ulem internals, aomart completion) land next sweep. |
| 2026-09-02 #32 | batch-54f debug (`pk_bin/latexml_oxide.sweep32`, 83a347272d) | 2374 | 300s | 151/21/0 | 765 | 814 | 623 | Banks batches 54e-54f (waves 7-8). **S0∧S1 1,437 (60.5%), oracle-clean slice 1,198/1,548 (77.4%)**, mass 25,024 (slice 6,365); timeouts 67→21 (quiet run, `JOBS=8` alone — still throttling, `docs/THERMALS.md`); 87 newly clean, 17 "regressions" of which 11 are former timeouts now completing with errors; real: the l3doc `\SpecialMacroIndex` `\verbatimchar` (=`&`) delimiter (ltx-talk/postnotes/pythonimmediate 0→20/13/18, fixed in 54g by expanding a CS delimiter in `absorb_index_verb_runs`), amsldoc-it 3→fatal (54g: back to 3), neoschool-fr, xpicture (`\axeslabelmathversion` ×92, a former timeout). |
| 2026-09-02 #31 | batch-54d debug (`pk_bin/latexml_oxide.sweep31`, 2f525993d6) | 2374 | 300s | 125/67/0 | 829 | 788 | 565 | Banks batches 54c-54d. **S0∧S1 1,353 (57.0%), oracle-clean slice 1,130/1,548 (73.0%)**, mass 25,455 (slice 6,937); 105 newly clean, 17 regressions (interface3/source3 `\@part`+locked `\chapter`, frankenstein hook cascade — fixed in 54e; hepunits; timeouts that now run further). Ran concurrently with the test suite — throttled (`docs/THERMALS.md`), timeouts not comparable. |
| 2026-09-02 #30 | batch-54c debug (`pk_bin/latexml_oxide.sweep30`) | 2374 | 120s | 138/67/0 | 905 | 706 | 558 | Banks batch 54c. **S0∧S1 1,264 (53.2%), slice 1,050/1,548 (67.8%)**, mass 42,466 → 28,055 (−34%); 45 newly clean, 10 regressions (tikz-ext graphdrawing LuaTeX-only; derivative completes; rest = timeouts under suite load). |
| 2026-09-02 #29 | batch-54 debug (`pk_bin/latexml_oxide.b54r`-era) | 2374 | 120s | 217/29/0 | 909 | 655 | 564 | Banks batch 54 (15 roots). **S0∧S1 1,219 (51.3%)**, mass −36% vs #28 but zero-error flat — the per-witness plateau that motivated the class-level surpass directive (2026-09-02). |
| 2026-09-01 #28 | batch-50 release (bin_b50rel, ed2ebe74bd) | 2374 | 120s | 27/0/0 | — | — | 1246 | Banks batches 45-50 (release profile, quiet machine: timeouts 0). **S0∧S1 1,245 (52.4%), oracle-clean slice 1,032/1,548 (66.7%)**, mass 66,851 (slice 24,416), fatal/killed 27 (slice 2). Exemplar nicematrix 1001→662 (french 536). Vs #27: +4/+1 net only, because (i) the batch-34→b47 `Until:` honesty now reports every delimited miss at EOF (docs that loop at EOF pay one Error per line past it — cluster A below), and (ii) a HARNESS TRAP: the fresh outroot (`perfect_kernel_s14`) had no `oracle_verdicts.tsv`, so the 264 clean-lualatex docs ran as pdfTeX (cartonaugh/ukbill/elpres/fontsetup "0→N" are false regressions; `run_doc.sh` now reads the oracle from the corpus dir). Real regression clusters: **A** "Until … at end of input" = bibleref-german ×2 (amsgen `
ew@ifnextchar` space skip, KPE #113), docsurvey/rub-kunstgeschichte (biblatex `erb` leak after `\printbibliography`, KPE #114), tikzmarmots-doc 0→501+F / tikzlings-doc 35→536 / stex-doc (no `\everyeof` at `\input` file end, P15 file side); **B** `\endlx@list` egroup cascades under raw memoir `\list` (memman 144→1001, biblatex-oxref ×4 19→1001, verbatimcopy, dlfltxb; P58). Both clusters → batch 51. Not regressions: swfigure fatal (fp `\__fp_sep:` miss then `oom:alloc_failed` 9.2e15-byte request, exit 137 — same in #27, i64-overflow bug open), pas-crosswords (missing pas-doc.sty, lualatex oracle exit 1), nathguide 62 `\endget@@` (mode-frame family, parked R9). New: short-math-guide SIGSEGV exit 139 (open). |
| 2026-09-01 #27 | batch-44 debug (bin_sweep27) | 2374 | 120s | 17/26/0 | — | — | 1247 | Banks batches 43-44. **Honest bar (new `tally.sh`, S0∧S1 = completed with zero errors): 1,241 all (52.3%), oracle-clean slice 1,031/1,548 (66.6%)**, mass 62,577 (slice 21,979); legacy zero-error 1,247/1,035. Sweep 26 is INVALID as a comparison base — it ran under agent+build contention and timed out 266 docs (111 with 0 errs, counted as "clean" by the flip-only tally); vs sweep 25 (honest 1,231 / 1,018): +19 newly clean, 25 Δ-regressions of which 6 are 0-error contention timeouts and the rest form two clusters lane-assigned to w3P: the **cnltx family** (chemnum 111→295, tasks 73→414, modiagram 53→1001, ghsystem 104→226, embrac/fnpct/phonenumbers/schulmathematik/guitarchordschemes) and a tikz/pgf set (tikz-network 259→1001, pixelart 12→819, neoschool 11→272, circuitikz 0→56, tcolorbox-tutorial-poster 0→7, worldflags 0→fatal). Exemplar: nicematrix 1001 (since sweep 23 — batch 31-34 `\pgf@decorate…` csname runaway, lane w3I; base 61 `&` errors lane w3J). |
| 2026-09-01 #26 | batch-42 debug (bin_sweep26) | 2374 | 120s | —/—/0 | 1276 | — | — | Banks batches 40-42 (atableau, beamer newif+frame, amsopn, tcolorbox xparse/listing-file, curve2e ver@, pgfmath array, assoccnt/biblatex deps). Zero-error 1,276 (53.7%), slice 1,049/1,548 (67.8%), mass 61,607→51,487 (−16%). ZERO regressions, +45 newly-clean. Batch 43 (xkeyval presets, color-key symmetry, index sortas, amsldoc nobslash; 12 red/green guards `perfect_kernel_batch40_43`) lands in sweep 27. |
| 2026-09-01 #25 | batch-38-era debug (bin_sweep25) | 2374 | 120s | —/—/0 | 1231 | — | — | Banks batches 36-38. Zero-error 1,231 (51.9%), slice 1,018/1,548 (65.8%), mass 61,607. ZERO regressions, +4 clean (aomart ×2, algxpar…). Curve flattening: residual mass = USER-PARKED mode-frame family, math XMDual nesting (numerica), CJK D9, babel-shorthand tails, and the PLANS architectural queue (lthooks c_parameter_token labels, @pushfilename routing, everyeof interaction). |
| 2026-08-31 #8 | 9b3da268fc release | 2374 | 120s | 39/6/0 | 1346 | 484 | 499 | + showexpl LTXexample (code+result), piton binding (fatal removed — nicematrix manual runs FULL depth), nicematrix rule-opts strip + decorations. **Error-free 983 (41.4%).** |
| 2026-08-31 #7 | 83928ef584 release | 2374 | 120s | 40/3/0 | 1354 | 479 | 498 | + #166 filelist catcodes (hep-* cluster cleared), real pdfTeX utility surface (strcmp/md5/escapes), enumitem `\setlist*`, beamer `\subtitle`+OptionalAngled fix, book `\if@mainmatter`, nicematrix Array family, texlua bridge + luacode. **Error-free 977 (41.2%); S2 schema-valid 1435/2331 (61.6%).** Fatal/timeout counts remain contention-noisy (suite/S2 ran concurrently). |
| 2026-08-31 #5 | b2ff296e4b release | 2374 | 120s | 38/7/0 | 1382 | 456 | 491 | + ltxdockit_cls.rs, `\@raw@classoptionslist`. **Error-free 947 (39.9%).** Fatal count noisy across sweeps (a concurrent test-suite run contended for CPU/RAM); timeouts likewise. Binary predates `\BreakableUnderscore` + `\glossary` schema guard (sweep 6 material). |

## Fix log

One row per landed kernel/engine fix attributable to this mission. Guard test
names are the durable part.

| Date | Fix | Cluster addressed | Guard test |
|---|---|---|---|
| 2026-08-31 | `tools/make_formats.sh` pins TEXMF* to the ambient kpsewhich's TL root (dual-TL schism poisoned the dump: "2025" dump carried expl3 2024-01-22) | expl3 props 124 docs, tagging sockets 74, l3sys 66+28, `\prop_new_linked` 36+14, `\IfPackageLoaded*`/`\IfFormatAtLeast*` — representatives collapsed 124→2, 74→1, 66→7, 36→6 | zero-`Error:` `--init=latex.ltx` gate; re-sweep delta |
| 2026-08-31 | OXIDIZED_DESIGN #161: `DefPlain` skips blanks before its required `{` (surpass-Perl, user-approved, branch-contained) | `Expected opening '{'` — `\lstnewenvironment` bodies on following lines (~148 docs incl. ltxdockit/cnltx families) | `cluster_package_guards::defplain_skips_blanks_before_brace` |
| 2026-08-31 | OXIDIZED_DESIGN #162: listings raw-line capture keeps the first body line when an argument probe crossed the newline (`gullet::pushback_is_empty`) | First body line silently lost in every `\lstnewenvironment[1][]`-style example env (content-loss, shared with Perl) | same guard (data-attr assertion) |
| 2026-08-31 | OXIDIZED_DESIGN #163: `\makeindex` allocates `\@indexfile` (kernel-contract subset) | 14 bundles (l3kernel's own manuals, robustindex, postnotes) — saveenv 2→1 err (Perl fatals) | `makeindex_allocates_indexfile` |
| 2026-08-31 | OXIDIZED_DESIGN #164: loader records `\@raw@opt@<name>.<ext>` + `\@raw@classoptionslist` (kernel L18521/L18718) | every `\ProcessKeyOptions` package dropped its options (10 bundles, codedescribe…); babel global language options loaded nil.ldf (4+ French bundles) — both fixed, `\og`→« » works | `process_key_options_sees_load_options`, `raw_classoptionslist_recorded` |
| 2026-08-31 | OXIDIZED_DESIGN #165: `\@currsize` defaults to `\normalsize` | linguistics family (5 bundles) — linguex-doc 6 err → **0 err 0 warn** | `currsize_default` |
| 2026-08-31 | beamer requires hyperref (Perl L1311 parity) + `\Hy@MakeCurrentHref` internal | beamertheme-* `\url` mass; l3doc anchor internal (8 bundles) | beamer golden (84_slides) |
| 2026-08-31 | l3backend loaded at begin-document (`\@expl@sys@load@backend@@`, latex.ltx L9472) | `\__color_backend_*` families undefined in every dump-mode conversion | prettytok witness; suite |
| 2026-08-31 | KOMA `\minisec`→`\paragraph*`, `{labeling}`→`{description}` (semantic); memoir geometry stubs (justified) | 17+11+4 bundles | scr*/memoir bindings |
| 2026-08-31 | silence.sty binding: `\sl@StoreMessage` internal | hep-* doc family (5+ bundles) | hep-acronym witness |
| 2026-08-31 | **ltxdockit_cls.rs** — NEW binding (user-directed lock-conflict resolution): semantic titlepage keyvals → frontmatter | 12-bundle biblatex/etoolbox manual family — abraces-doc 10 err → **0 err** | corpus witnesses |
| 2026-08-31 | rawclasses protocol guards (no code change — verified existing behavior) | binding precedence + bindingless-class raw load without OmniBus | `cluster_package_guards::rawclasses_binding_precedence_and_no_omnibus` (3 tests) |
| 2026-08-31 | **Lua rebind-as-we-emulate** ([`LUA_REBINDING.md`](LUA_REBINDING.md)): live `tex.count`/`tex.dimen` mirror (`Q`/`A` protocol → Rust State), absorb shims (`pdf.set*`, `tex.primitives()`), kpse-backed `require` + lualibs, SOURCEDIRECTORY cwd | chunks branching on register stub-zeros; `require` of texmf Lua modules (newpax, tkz-elements); `pdf.*` runtime errors (derivative); `tex.primitives` (abntexto ×2) | `lua_state_mirror::directlua_reads_and_writes_live_registers`, `lua_bridge::tests::rebound_engine_intents_absorb_and_resolve` |
| 2026-08-31 | `\lx@directlua` fidelity (real-luatex probes as oracle): drop the double `Expand!` (XGeneralText already partial-expands, honoring `\noexpand` — the re-expansion ate babel's `[[\noexpand\csname bbl@error\endcsname{]]` long string), filter `\par` tokens (blank lines contribute NOTHING in real luatex), diagnostic now includes the chunk head | systemic: EVERY profiled clean-lualatex doc's babel Lua layer died (`attempt to index a nil value (field 'locale_props')`) | `luatex_babel_api::babel_lua_api_layer_initializes` |
| 2026-08-31 | graphicx `\Gin@boolkey` (graphicx.sty L137) + real `\newif` set (graphics.sty L55-579); KOMA classes require typearea, whose binding gains `\typearea`/`\recalctypearea` (typearea.sty L1596) | 34+9-doc Gin cluster (hvfloat/after1s1c 34→4 err, pagelayout/banner error-free); 26-doc recalctypearea cluster (bohr) | `graphicx_internals::gin_internal_names_defined` |
| 2026-08-31 | minted `\minted@outputdir` (minted.sty L273); beamer `\@@@affiliation` contact constructor; beamer requires ifpdf/keyval/graphicx (Perl beamer.cls.ltxml L30-32 parity) | 18-doc minted cluster (csvsimple 2 err left); 16-doc affiliation cluster (demo-cbernoulli 1 err); 9-doc `undefined:\includegraphics` (focus-demo) | beamer/minted guard tests + goldens (suite) |
| 2026-08-31 | **8-fix batch** (subagent-root-caused): pgfmath boolean/logic function forms return bare ints (live-pgf probe oracle); luaPST absorbers in luatex profile; scrlfile `\AfterPackage`(+!/+) binding; fontspec expl3 conditional layer + signature fixes; ifluatex consults LUATEX_PROFILE; unicode-math `\sym*`; `\suppressfontnotfounderror` | pgf-spectra 7k-err/doc; bardiag1+psgomanual error-free; cnltx 36-doc avalanche (bohr multicols/RaggedRight cleared); raleway/sourcecodepro/abnt-doc clean | pgfmath unit guards, `luatex_profile`, witnesses |
| 2026-08-31 | **Surpass batch** (user-approved 2026-08-31): OXIDIZED_DESIGN #169 `\ver@` fallback-only; #170 robust accent `\meaning`; #171 brace-hunt expands protected (tex.web scan_left_brace, pdflatex-probed) | toptesi 12-doc abort cluster → 2 residual errs; cahierprof 501→0, ipsum 103→0, colorblind 502→19, mirror-lens 502→38; ~16-doc xint `expected:{` cluster | `raw_provides_version_survives`, `accent_meaning_robust_shape`, `expanded_protected_brace_hunt` |
| 2026-08-31 | luatex profile: format parity `\bbl@luapatterns` no-op (lualatex format ships patterns → babel.def L1135 skips the patterns-only `\input luababel.def` whose loaded-flag suppressed the real API load at L2285); direction primitives eat their 3-letter keyword (were `{TLT}` macros leaking text); `\breakafterdirmode` register | same systemic babel class + `TLTTLT` text leak + luababel L1469 undefined | same guard + `luatex_profile` |
| 2026-09-02 | **batch 54e** (sweep-30/31 clusters): `\secdef` doubles the unstarred title (KPE #135); microtype drops its `{microtypecontext}` stub (raw env); beamer `\DeclareOptionBeamer`/`\ExecuteOptionsBeamer` + `framenumber` counter + `\headcommand`; numprint `\numprint` robust; showexpl state macros + `ltxexample` counter; NEW `newverbs` binding (`\newverbcommand` family over `read_verb_invocation`); `\endflushleft/right` (KPE #137); `\lstinline{…}` brace-balanced (listings golden re-blessed); `\centering`/`\raggedright` are macros (KPE #139); `\newcommand` defaults halved twice (KPE #134); `\ver@` from the installed file for binding loads; **a `\caption` with no float ancestor degrades to inline caption text** (OXIDIZED_DESIGN #182, P16 ii — pgfornament 111→2, xltabular-doc 2→0, memman −92); `\index` re-tokenizes the `\write` form of its argument (KPE #140: `\docpkg{\TIKZ}` → `\TIKZ@`); `\@part`/`\@spart`/`\@chapter`/`\@schapter`/real `\@sect` + `\chapter` unlocked after OD #179's undefine (KPE #141 — interface3/source3 101→5 regression fixed); load hooks skipped for a CS-named request (frankenstein 27→12); **German `"` shorthands moved to babel_support** — `\usepackage[ngerman]{babel}` (80 manuals) rendered `Sch"one` as `Sch”one`; `\germanTeX`/`\ngermanTeX`/`\umlautlow` (16 docs `undefined:\germanTeX` via the kernel first aid); non-shorthand `"x` prints the quote (pdflatex-probed); beamer `LoadClass!("article")` (its `RequirePackage!` missed silently: `\thesubsection` undefined in every deck with subsections); **backmatter placed where IT is legal when its `ltx:section` stand-in cannot be reached** (`backmatter_insertion_target`: `\bibliography` inside a beamer frame, 5 decks). WAVE 7 (10 root-causers on sweep-31 oracle-clean clusters): nameref `\NR@gettitle` (memoir `\M@gettitle`: srbook-mem ×3, serbian-apostrophe ×2); fancyhdr v4 `\f@nch@initialise` (ctex hook; the 5 witnesses are XeLaTeX-only, kept for completeness); biblatex loads the **bbx before the cbx** (oxref's `\newtoggle` vs `\providetoggle`) + data-model/setup declarations accepted (`\DeclareDataInheritance`, `\DeclareRangeChars`, `\NumCheckSetup`, `\DeclareBiblistFilter`, `\defbibnote`, `\defbibfilter`, `\ifbibmacroundef`; oxref ×4, biblatex-cse, musuos); titlesec **`\titleclass` defines a new heading** (regulatory ×4); caption `\DeclareCaptionType` + newfloat's trailing `[singular][listname]` (pygmentex ×2, hvpygmentex); **`\valign` consumes its alignment** (KPE #142, fancyvrb `showtabs`); **a second `\begin{document}` fires no hooks** (KPE #143; ltnews + l3news 2→0); NEW **pdfcomment binding** (annotations → `ltx:note`; `\pdfstringdef` global, `\HyColor@XZeroOneThreeFour` defines its target; example ×3 15→0); memoir `\thetitle`/`\theauthor`; the `\strippedat@` cluster (tikz-cd/tikz-dependency/pdfmarginpar) was already closed by KPE #140 (guarded for `@`=letter too). **scrlfile raw-loads** (its `\BeforePackage`/`\AfterPackage` are the kernel file hooks now firing around every load; the old begin-document approximation dropped scrbook.cls:5466-5477's `\@addchap` save/restore around hyperref — cleanthesis my-thesis, DEMO-BFHThesis 1→0; cnltx-doc's deprecated `\AfterPackage!` now fails exactly as pdflatex does — 26 manuals, none oracle-clean, +2 each); mathtools `\mathmakebox[<width>]` reads a <dimen> and calc's base-seam `\widthof` measures in its own box (optidef 58→0; kblocks/titlecaps = the parked mode-frame family, R9). Triaged, not fixed: examdesign locked `\section` (P68, 3 docs). WAVE 8 (10 root-causers, sweep-31 residue): makeidx binding no longer clobbers the allocating `\makeindex` (manyind/robustindex `\@indexfile`); **block content in an inline container that cannot auto-close gets an auto-opened `ltx:inline-block`** (P37 td half: listing-in-cell, engtlc ×2/lexref/expex-glossonly); **the `Variable` reader skips spaces/`\relax` and absorbs `\global`** (tex.web §1211: `\setlength { \x }`, xtab's `\setlength{\global\ST@toadd}` — modernposter 10, rec-thy 20, altverse); beamer `\inst`/`\partpage` + beamer.cls's dimension family (detlevcm, beamerstructure2, verona); tabu's `to`/`spread` grammar over tabularx (brandeis 41→0); xkeyval sets `\XKeyValLoaded` (expex re-input clobbered `\define@choicekey`: fragoli, rainbowbrackets); biblatex's 43 counters as real counters + the `\AtBegin*` hook family + hyperref `\Hy@AtBeginDocument` (biblatex-fiwi ×3, biblatex2bibitem ×2, arsclassica/sidenotes lines); `\title`/`\author`/`\date` frontmatter copies the halved macro (KPE #145, ulineno RCS idiom); pgfplots `\pgfplotsenablelua{0}` under the luatex profile (colorblind_doc); url makes `\dospecials` literal (latex4wp: `\path{…\}` swallowed 87% of the manual); index `\verb` followed by a CS is text (amsldoc-it/-vn). Deferred: circularglyphs 14th cell (author's stray `&`, faithful error), jwjournal whole-document `+b` env (SHARED, Rust 2 vs Perl 4), screenplay unclosed `\abstract{` (P69). 54g (sweep-32 regression): a CS in the `\verb` delimiter slot of an index entry is EXPANDED (doc.sty `\SpecialIndex` writes `\verb\quotechar*\verbatimchar…`; l3doc's `\verbatimchar` is `&`). | **sweep #31** (`~/data/perfect_kernel_s31`, binary 2f525993d6 `sweep31`, debug, 300 s, 10 jobs — ran concurrently with the test suite, see `docs/THERMALS.md`): S0∧S1 **1,353/2,374 (57.0%)** (s30: 1,264 / 53.2%), oracle-clean **1,130/1,548 (73.0%)** (67.8%), mass **28,055 → 25,455 (−9%)**, timeouts 67 (26 with 0 errs), fatal 125 (s30: 138); 105 newly clean, 17 regressions (l3kernel interface3/source3 + frankenstein ×4 fixed above; hepunits `\equal` 2→3; source2e/numerica/pgf-PeriodicTable timeouts with more errors = the raised token limit runs further). | `perfect_kernel_batch54::{secdef_doubles_the_title_for_the_unstarred_form, caption_without_a_float_ancestor_degrades_to_text, index_control_word_before_at_is_not_glued, secdef_part_and_chapter_workers_exist, babel_ngerman_umlaut_shorthands, german_sty_germantex_switch_is_defined, beamer_has_article_sectioning_counters, bibliography_inside_a_beamer_frame_stays_in_the_frame, nameref_gettitle_records_the_title, fancyhdr_initialise_is_defined, biblatex_loads_bbx_before_cbx, titleclass_defines_a_new_heading_command, declare_caption_type_makes_a_float, valign_swallows_its_alignment, second_begin_document_fires_no_hooks, pdfcomment_annotations_are_notes, memoir_title_defines_thetitle, mathmakebox_width_is_measured_not_typeset, scrlfile_before_and_after_package_hooks_fire, makeidx_keeps_the_allocating_makeindex, listing_in_a_tabular_cell_gets_an_inline_block, variable_reader_skips_spaces_and_takes_prefixes, beamer_inst_partpage_and_sidebar_dimens, tabu_to_width_and_x_columns, xkeyval_sets_the_loaded_sentinel, biblatex_counters_hooks_and_hy_atbegindocument, frontmatter_copies_the_halved_macro, pgfplots_lua_backend_is_off_under_luatex_profile, url_backslash_is_literal, index_verb_followed_by_cs_is_text, index_control_word_before_letter_at_is_not_glued, centering_is_expandable_for_expl3_v_expansion, lstinline_brace_delimiter_is_balanced, …}` + 84_slides beamer golden (article ids, Perl-parity) |
| 2026-09-02 | **batch 54h** (WAVE 9, 10 root-causers on sweep-32 residue): **keyval keys are read UNEXPANDED and brace-aware** (keyval.sty `\KV@do`; enumitem shortlabel `\setlist[test]{\@risp,…}` with `\@risp`=`\fbox{…}` was unfolded and cut at an inner `}` — verifica ×5; `\savevalue{key}` detected on the raw key as xkeyval.tex:140-146); **`\XKV@rm` is fetched ONE STEP** in `\setrmkeys` and the reader (xkeyval.tex:497/618 — chessboard's load-time `trimarea=\board`, `\board` \edef'd only inside `\chessboard`: chessboard-skakps 1→0); physics2/fontawesome7 are `INTERPRETABLE_SOURCES` (not versions of physics/fontawesome — `require_package` no longer forces `notex`); beamer `{frame}` sets `\ifbeamer@inframe` + the lecture layer; **`\maketitle` trapped in a capture box degrades to text** (`frontmatter_trapped_in_capture`); babel `\extras<lang>`/`\noextras<lang>` hooks in the ngerman/german/french bindings (cleveref's `\cref@addto` on a `\relax` hook made it expand into itself — homework/jwjournal -de/-fr); amsopn `\operatorfont` (KPE #146; glosmathtools ×2, ~108 lines); fontspec `\strong` (luatex profile: glossariesbegin, mfirstuc-manual 1→0); **scalerel loads graphicx + the documented low-level API** (`\ThisStyle`/`\SavedStyle`/`\@obj` re-enter math, `\scaleobj`/`\hstretch`/`\vstretch` via `\scalebox` — scalerel.tex 26→0, hwemoji, stackengine); cas `{graphicalabstract}`; babel-spanish `\deactivatetilden` (gaceta ×2); xcolor `\XC@@names` (doc-use-newpax); tabu `X[1,$]` math column (brandeis 5→0); biblatex `\if<test>` predicates are BRANCH-SELECTING MACROS (biblatex.sty:6205 `\iffieldundef{f}{t}{f}`), + `\DeclareLabeltitle`/`\DeclareLabelalphaTemplate`/`\letbibmacro`/`\uspunctuation`/`\footfullcite`; **`\ifx` never equates a closure body with a token body** (KPE #147 — biditools' `\bidi@ifscanable` "patched" the native `\begin`: crbox/ghab ×16); enumitem `\enitkv@key` + babel-italian `\setISOcompliance`/`\IntelligentComma`/begin-document `\unit` (verifica ×5 → 0); **itemize/enumerate locally reset `\makelabel`** (latex.ltx:16061/16072 — a global 2-arg `\makelabel`, mathfont-user-guide 3→0; SHARED surpass); Umath siblings `\Udelimiter`/`\Uradical`/`\Umathcodenum` + `\mathnolimitsmode`/`\scantextokens` (mathfont-symbol-list); beamer `\usefonttheme` loads its file + `\beamer@ifempty` (beamerthemeuantwerpenuserguide 11→0); mathtools small starred matrices / `multlined` assemble their key lists OUTSIDE the keyval read (the unexpanded key reader); **the lualatex FORMAT surface under the `luatex` profile** (latex.ltx:107-110/896-1058: `\attributedef` into a private `\attribute` bank, catcode-table switches, `\luadef`, `\newattribute`/`\setattribute`/`\newcatcodetable`/`\newprotectedluacmd`/`\now@and@everyjob`, `\prehyphenchar` family — 17 lualatex-oracle first-error clusters; luatexbase binding keeps only its own wrappers); **`\DeclareTextCommand`/`\ProvideTextCommand` install the call-time encoding dispatcher** (Perl pool:2588-2605; the port froze the first body — greek-fontenc "character beta symbol not available" ×~200), `\UseTextSymbol` runs the body INSIDE its encoding, `\DeclareTextSymbolDefault` goes through it, textalpha/alphabeta raw-load (the textgreek rerouting dropped), LGR slot `s` = σ (KPE #148); **the refnum formatter closes its `\csname` before an argument-taking `\p@<ctr>`** (ctex `\labelformat`; KPE #149 — caspervector 23→0, inkpaper-en 3→0, sduthesis 36→1, tabular2 28→2). Verified stale/fixed by earlier batches: ltx-talk (20→0), footer-text/titlepage-styling/unifront (`ltx:title` in capture), multiple-choice, bfh beamer decks. Triaged, not fixed: `\cl@<ctr>` is a State value not a macro (SHARED with Perl, contract ×2 — would need a macro twin kept in sync); pagelayout page-canvas `{picture}` (SHARED, HIGH risk); recipebook `\DeclareDocumentCommand{\titlepage}` vs the locked env (SHARED by design); harmony `\newpage` between rows (SHARED scanner quirk); tex-font-cheatsheet pgf `\matrix` (theme 3); numerica/mhchem `$…$` in align cells (theme 1, parked); skak `\mainline` move parser under beamer (separate root); uantwerpenexam-example2 babel `\selectlanguage` (`\aftergroup\bbl@pop@language`) at the start of a tabular cell (SHARED, theme 1); babel `greek.polutoniko` modifier syntax (SHARED, babel option split); bibarts self-recursive macros (SHARED); manyind index round-trip `\protect\def\nwletre` (RUST-ONLY, compound); ribbonproofs `\iffalse` inside an `\include`d tikz-externalized file; `\DeclareTextAccent` ignored (SHARED — `\accdasia` breathings). | — | `perfect_kernel_batch54::{keyval_key_is_brace_aware, setrmkeys_keeps_leftover_values_unexpanded, physics2_is_not_a_version_of_physics, beamer_inframe_flag_and_lecture_layer, maketitle_inside_a_box_degrades_to_text, babel_extras_hooks_are_defined, amsopn_operatorfont_is_defined, fontspec_strong_is_bold, scalerel_low_level_api_and_math_objects, cas_graphicalabstract_is_a_note, babel_spanish_deactivatetilden_is_defined, xcolor_names_hook_is_defined, tabu_math_x_column, biblatex_tests_are_branch_macros, biditools_env_patch_leaves_begin_end_intact, enitkv_key_and_babel_italian_extras, global_makelabel_does_not_reach_list_items, umath_delimiter_radical_and_codenum_under_luatex_profile, beamer_font_theme_loads_and_ifempty_is_defined, babel_italian_unit_under_iso_compliance, provide_text_command_dispatches_on_encoding, ctex_argument_taking_p_macro_keeps_the_refnum, ltluatex_format_surface_under_luatex_profile}` + expansion/etoolbox golden ("begin is not patchable", OD #183) |
| 2026-09-02 | **batch 54d** (sweep-30 clusters): package/class/file before+after **load hooks** fire around every binding or raw load (latex.ltx:18843-18875, P16 xii); **chemmacros raw-loads** (its stub's `\ch` overrode chemformula's; chemformula binding gains the l3 API `\chemformula_chcpd:nn`… and `\ch[opts]{}`) — chemformula manual 1001+F→~40; **datetime2 raw-loads** once `\pdfcreationdate` is pdfTeX's `D:YYYYMMDDhhmmss` stamp; **`\chapter` exists only where the class made a chapter counter** (OXIDIZED_DESIGN #179 — blindtext's `\@ifundefined{chapter}` under scrartcl: hvfloat ×50 + coseoul + xassoccnt `undefined:\thechapter`); **`\@trivlist` is the shared list opener** (OXIDIZED_DESIGN #180, P38 — memoir `adjustwidth`/raw `\list`: digiconfigs 5→0, memman 211→104, 28 docs); tcolorbox native listing envs run their `[init]` through `\tcb@proc@options@init` (`use counter from=` across envs); nicematrix Array family `[opts]{cols}[opts]` (exemplar 57 `&` → 25 residual); **a `\\` inside a brace group of a cell is an in-cell break** (OXIDIZED_DESIGN #181 — tabularray `\SetCell{l}{…\\…}`, ProfSio ×4); `run_doc.sh` truncates >50 MB logs (the sweep filled `/home` mid-run — verdicts from the outage window were re-run). AGENT-TRIAGED: derivative manual = protected env-end macro carrying `\endtabular` not expanded by the alignment scan (SHARED, P67); tabularcalc/fepslatex `\noalign` shapes not yet reduced; `\hour`/`\setmonofont`/`\pdftex_if_engine:TF` clusters = Japanese (parked) / lualatex-not-oracle-clean / pdflatex-fails-too. | **sweep #30** (`~/data/perfect_kernel_s30`, HEAD 9142adc6b7-era `sweep30` binary, debug, 120 s, 10 jobs): S0∧S1 **1,264/2,374 (53.2%)** (s29: 1,219 / 51.3%), oracle-clean **1,050/1,548 (67.8%)** (65.9%), mass **42,466 → 28,055 (−34%)**, timeouts 67, fatal 138 (s29: 217); 45 newly clean, 10 regressions (tikz-ext graphdrawing = LuaTeX-only; derivative now completes (P67); the rest timeouts under the concurrent test-suite load). | `perfect_kernel_batch54::{package_after_hook_fires_for_a_binding_load, chemmacros_raw_load_keeps_chemformula_ch, datetime2_raw_dates_render, chapter_is_undefined_in_a_chapterless_class, raw_list_opens_through_trivlist, tcblisting_init_counter_is_shared_by_use_counter_from, nicearray_takes_a_leading_option_list, newline_inside_a_cell_group_is_an_in_cell_break}` |
| 2026-09-02 | **batch 54c** (sweep-29 regression triage, 6 root-causers): `\lstnewenvironment` start code runs in the main stream and the end code after the display group (listings.sty `\lst@Init`/`\lst@DeInit` order) — the R9 "mode-frame" mass mostly collapses (cnltx_en 1001+F→66→2, chemnum 870→23, pixelart 819→11, tasks 342→30, modiagram 1001+F→18, dlrg 350→38); pdftex RNG (`\pdfuniformdeviate`/`\pdfnormaldeviate`/`\pdfsetrandomseed`, xorshift64*; randintlist-l3 TokenLimit→clean); l3sort allocators moved to etex_sty so `\loctoks` exists; `\csname` §370 discards an undefined cs; `\end` epilogue `\if@ignore\ignorespaces` with an EXPANDING `\ignorespaces` (§1045); begin-document L3 hook digests re-locked; ulem `\UL@on` family; `\fnum@` braces; memoir `\autorows`/`\autocols`/`ctabular`; **cycle guard**: the gullet guard ignores a DRAINING pushback (the `\prg_replicate` 10000× l3intarray body is data being consumed, maze manual; expansion/mutation-epoch gates rejected — they silence the stomach guard on every tikz loop); **real `\strutbox`** (plain.tex:588, latex.ltx:12596 rebuilt by `\@setfontsize`) and **`\pagegoal` follows `\vsize`** (fillwith/fullwidth); a zero-width `\vrule` is a strut, never a border (OXIDIZED_DESIGN #176, KPE #131 — halignatt with the real strut; `Digested::is_strut`); package `-h@@k` runs with `@` a letter (latex.ltx:18856 before `\@popfilename`; europecv Fatal→0, KPE #132); `\CatchFileDef`/`Edef` run the setup arg and read under its catcodes (catchfile.sty:251-296); `\usepackage{../tex/pkg}` falls back to the installed basename (OXIDIZED_DESIGN #177, P16 xv, 61 manuals); **`\lx@newpage` advances `\c@page`** (OXIDIZED_DESIGN #178; knowledge `\filluptopage` loop, P47); **`\vsplit` stores at the register's eq_level** (tex.web §977 `Scope::InPlace`; eledform box runaway→clean 2 s); biblatex `.bbl` commands scoped to the `.bbl` read like `\blx@bblstart` (KPE #133 — document-wide `\list{}{}{}` shadowed LaTeX's `\list`: every list env in a biblatex doc) + `\DeclareLabeldate[]{}`; fontspec definers DEFINE their target (papiergurvan → 0); token backstop 4G and ×1000/byte over EVERY opened file (circularglyphs xstring O(n²) ~2.5G, source2e l3doc ~2G complete under `LATEXML_TOKEN_LIMIT=0`). AGENT-TRIAGED, not fixed: rulercompass/broydensolve/circuitikz/tkz-grapheur = pgf curve-intersection subdivision + l3doc coffin O(n²) volume, Perl times out too (SHARED, perf lane); scratchx/unicodefonttable = raw fp.sty `\FPeval` ~143K tokens each + 131k-cell font table (SHARED; native fp binding = the lever, HIGH); source2e residual 22 = l3doc `\MakeShortVerb\"` unbalanced across `\end{macrocode}` (SHARED, R9-parked) — Perl "passes" only because it cannot find the `.dtx` files; `\tl_analysis_map_inline:nn` over explicit braces errors in the §442 backquote path (RUST-ONLY, rare, repro `~/data/pk_agents/w4/w5_misc/tlb.tex`). | regressed-34 rerun on `b54n`: fillwith/pkgloader/randintlist ×2/maze/europecv/seminar/knowledge/eledform/papiergurvan/cnltx back to clean; sweep #30 pending. | `perfect_kernel_batch54::{pagegoal_is_vsize_and_strutbox_is_real, fullwidth_frame_does_not_retry_forever, zero_width_vrule_is_a_strut_not_a_border, at_end_of_package_hook_runs_with_at_letter, catchfiledef_reads_under_setup_catcodes_and_edef_expands, relative_package_path_falls_back_to_basename, clearpage_advances_the_page_counter, vsplit_drain_survives_the_enclosing_group, biblatex_bbl_commands_do_not_shadow_list, fontspec_definers_define_a_font_switch}`, cycle_guard unit tests (`periodic_stream_with_draining_backlog_is_data`, `periodic_stream_with_growing_backlog_fires`), `gullet::token_limit_scales_with_cumulative_source_bytes`, `fatal_salvages_partial_document::recoverable_fatal_keeps_the_already_digested_document`, 53_alignment halignatt/cells |
| 2026-09-02 | **batch 54** (agent-fanned root-causing, 15 roots landed): eTeX `quotient` rounds half away from zero for `\numexpr` AND `\dimexpr` (`divideround`; pdflatex-probed `\dimexpr -1sp/2`=-1sp, KPE #124; xytest golden re-blessed) — l3fp's `\__fp_mul_cases_o:NnNnww` case index depends on it (wheelchart 1001→1, liftarm 502→1 fp errors); `\read` past EOF never emits a SPACE/IGNORE-catcode `\endlinechar` token (tex.web §345-349, KPE #125; liftarm/pmdraw `misdefined:\r` ×501); xkeyval `\usevalue` pointers resolved EAGERLY at `\setkeys` (xkeyval.tex:518-583) and a key saved with an EMPTY value replaces to nothing (:569 tests definedness; pmdraw 501+Fatal→64→**0**); `\angle` two-level (#170 shape) with `alias` so tex=/name stay `\angle` and tikzmath takes it as a variable (sunpath ×3 1001→0); `\meaning` of a DefMath atom is its cs (Perl TeX_Debugging.pool:110-113; was `Stored[??]`); listings write-file truncates on every fresh `\lst@Begin(Also)WriteFile` (lstmisc.sty:60-64; forest-doc's 441 stale-`\usepackage` storm); etoolbox `\AtEndPreamble` IS `\AddToHook{begindocument/before}` (etoolbox.sty:1743 — takes the `[label]`, queues in order after doc.sty's hypdoc chunk so ltxdoc+`\hypersetup` works; xassoccnt_doc 521→0); forest stub gains the bare `\forest…\endforest` form + `\bracketset` (neoschool 9→6); `\@setfontsize` lets `\@currsize` only under `\ifx\protect\@typeset@protect` (latex.ltx:14103-14107; tikz-network PushbackLimit Fatal — Perl OOMs); `\@listI` = pristine `\@listi` (size1x.clo:220); delarray delimited `\begin{array}({cc})` form (delarray.sty:43-58; memman 33 extra-tab errors); pgfsys `ensure_svg_context` guards the path/clip emitters like Perl's group opener (pgfsys-latexml.def:392-398; `\phantom{\draw…}` in a picture — pmdraw); tabularray `longtblr`/`talltblr` (panda 152+Fatal); **`\tl_set_rescan` core rescans atomically** (native `\__tl_set_rescan:nNN`, expl3-code.tex:3758-3790 — a rescanned `\cs_meaning:N` leaked its `#`s: substances 832→112, the P15 `\scantokens`-everyeof dead-end sidestepped); **tex.web §442** — a brace read as a backquote charcode undoes its align_state step, so `\group_align_safe_begin:` nets +1 (the P15 l3doc `\__tl_replace_next:w` loop root; l3doc `\marg` in `syntax`); plus b54-early: biblatex .bbx declarator gobblers (cnltx `#` family), xcolor `.` at load, hsb driver, `\NewTblrEnviron`, `\lst@UserCommand`, `\DocumentMetadata`→tagpdf, `\@listdepth` accounting, memoir `\endminipage`, xltabular page-break toggles, forest `\ProvidesPackage`, `\csname` back_error re-read (§373), backquote of a detokenized backslash (KPE #123 — hobby manual 373 curves, 0 errors), hobby stub retired, nicematrix `\NotEmpty`. AGENT-TRIAGED, not fixed: tikzpingus = `\usepackage{../tex/tikzpingus}` source-tree path, SHARED build-env (P16 xv, 61 manuals); mercatormap = shell-escape/network map data, out of scope (+cleveref `\Fullref` raw-key leak, PERL-ORIGIN); quiver = pgfmatrix-in-cell OOM (P60); twoxtwogame/liftarm/wheelchart/tikz-network = debug-build volume, correct output (P61); qworld = luatexja-corrupted `\if` (P62); bibleref-parse = accent composition under forced expansion (P16 xiv); neoschool `\neo@codenumbersep` = author bug (pdflatex errors too); ProfSio/cnltx/chemnum/pixelart/modiagram/tasks residual = R9 `\endmdframed`/`\endlx@list` mode frames (USER-PARKED). | 82-doc batch-54 witness slice (`~/data/pk_b54h`, HEAD before the last 4 roots): error mass 24,245→14,618 (−40%), 6 newly error-free (sunpath ×3, xassoccnt_doc, hobby, cahierprof-exemple); after the last roots: substances 832→112, pmdraw 64→0, panda Fatal→(pending sweep), zref-check-doc/spath3/calligraphy/litetable green on the P15 family (spath3.tex TokenLimit = separate pre-existing regression, agent in flight). Full sweep #29 in flight. | `perfect_kernel_batch54::{numexpr_division_rounds_half_away_from_zero, read_at_eof_drops_ignored_endlinechar, xkeyval_usevalue_is_replaced_eagerly_at_setkeys, xkeyval_usevalue_of_empty_saved_value_is_empty, listings_writefile_truncates_on_fresh_begin, etoolbox_atendpreamble_accepts_hook_label, etoolbox_atendpreamble_runs_after_earlier_begindocument_before_chunks, forest_bare_cs_form_discards_body, setfontsize_is_inert_inside_protected_edef, meaning_of_defmath_atom_is_its_cs, delarray_delimited_array_form, pgf_bare_path_inside_phantom_stays_in_its_box, hobby_shortcut_draws_a_cubic_path, tabularray_longtblr_and_talltblr_are_tblr, tl_set_rescan_captures_param_tokens, backquote_brace_charcode_keeps_align_state, l3doc_marg_inside_syntax_env, backquote_charcode_of_other_backslash, fpeval_register_right_operand_of_comparison, hobby_code_tex_raw_loads, documentmetadata_loads_tagpdf, endlist_decrements_listdepth, bbx_declaration_bodies_are_absorbed, …}` + 22_fonts abxtest / graphics xytest goldens |
| 2026-09-01 | **batch 53** (sweep-28 KOMA cluster + raw-class option plumbing + hvfloat/xltabular): eTeX `scan_expr` re-inserts a `\noexpand`'d macro at the head of a `\numexpr` factor as the PLAIN cs, so `scan_int` expands it (etex.ch "Scan a factor", `back_input`; pdflatex-probed `\numexpr\noexpand\one+1`=2) — tocbasic.sty:2688 `\@nameuse{sectiontocdepth}+\@ne` no longer warns Missing number; `\IfFormatAtLeastTF` compares the REAL `\fmtversion` (was a constant true); the KOMA classes are raw-interpreted end-to-end — scrartcl/scrbook/scrreprt + typearea/scrkbase/scrbase/tocbasic/scrlogo bindings become `InputDefinitions!` shims over the real files with a small `koma_post_load` re-targeting (`\KOMAoptions`, fonts, `\maketitle` pieces reach `<frontmatter>`), scrlayer/scrlayer-scrpage raw too; `\@startsection` reads its level as a TeX `<number>` (KPE #118, was string-coerced so every `\numexpr` level read 0) and unknown section types open the element of their LEVEL (OXIDIZED_DESIGN #175, surpass-Perl: Perl warns `malformed` and opens `ltx:section`); `\@declaredoptions` lists the declared options (latex.ltx:8230); class options keep letter catcodes and brace groups through `\@classoptionslist`/reversion; `\def` parameter delimiters keep ADJACENT space tokens (KPE #119, tex.web §474 — a raw `\def\x#1 #2 {…}` matched wrongly); `\@raw@opt@` is a single merged list and `\PassOptionsTo*` splits brace-aware, so `\ProcessKeyOptions` sees passed options; `\addcontentsline` takes its title Undigested (KPE #120, latex.ltx:2049 `\protected@write` — the digested form hung on `\protect` idioms); `\pagestyle`/`\thispagestyle` are macros (KPE #121: scrlayer's `\expandafter` freeze recursed on the primitive); geometry `\Gm@bindingoffset` register; xltabular IS a longtable (`\lx@longtable@bindings`/`\@@longtable`, xltabular.sty:86-96 — the old `\tabularx` alias ran the class `\caption` outside any float and under raw tocbasic's e-expanded `\@captype` cascaded fatally); hvfloat: empty float type = `nonFloat,onlyText` (hvfloat.sty:630-636), `hvFloatEnv` minipage (L1264), `[fbox,hyperref]` options, `\hvDefFloatStyle`, `\figcaption`/`\tabcaption` → `\captionof`, registers/saveboxes; ifoddpage raw shim (pure kernel TeX, hvfloat.sty:55). SHARED (not fixed): l2tabu's DVI branch (`\pdfoutput`=0 in both engines vs the pdflatex oracle, P16 vii); `\endflushleft`-in-group mode frames (R9, user-parked); listings evaluates `backgroundcolor=\color{…}` EAGERLY at `\lstset` time (Perl `lstExtractColor` digests too; real lstmisc stores `\lst@bkgcolor` unexpanded — hvfloat.tex:29 sets it before `\usepackage{xcolor}`, P16 viii) | glossaries-user 586→5 (+TokenLimit fatal, agent-triaged), neoschool-fr 270→68 (+EoF fatal, agent-triaged), tagpdf 113→21, DEMO-TUDaExercise 22→3, TUDaPhD 20→6, TUDaPub 17→2, TUDaReport 17→0, TUDaThesis 17→3, tikzlings-doc 35→0, contract-example 85→60, socialscienceshuberlin 19→14, bohr 63→57, yquant 136→116, worldflags 3→0, URpagestyles 5→1, fnpara/shadethm 1→0, tutodoc 15→9, tikzpingus 589→494 (still fatal), xltabular-doc 36→3, hvfloat 79→7 (6 R9 + 1 listings-color SHARED). **b53-late** (same batch, 5 more roots): collcell binding (scanner over the cell, raw `\collectcell#1#2\ignorespaces` needs the kernel cell template our alignment never inserts — onedown-ref Fatal→0); `\opt@<file>` bodies are re-tokenized with STANDARD catcodes (`tokenize_internal`; `ExplodeText!` had made the braces OTHER so l3clist split `thesis={type=dr,dr=rernat}` at the inner comma and glossaries' `stylemods={mcols,bookindex}` loaded `glossary-{mcols.sty`) — DEMO-TUDaPhD 6→3 (`\g_ptxcd_pubType_tl=thesis`, abstract + affidavit sections present; residual = 2× P38 `\endlx@list` + `\@addchap` from the P16 (xii) hook gap), glossaries-user 5 errors; `\NewTCBListing` signature → `\lstnewenvironment` arity counts ONLY `m/r/R` as mandatory and a LEADING `O/o/D/d` as the one optional (neoschool.cls:5168 `{ O{} m !O{} !O{x} !O{y} }` had become `[5][]`, eating the box body and cascading into 251× `every box on layer`) — neoschool 68+EoF-Fatal→9 (residual: `\setmathfont`/`\IfFontExistsTF` fontspec probes, `\forest`, `\neograph`, XMApp); siunitx loads `translations` (siunitx.sty:5014-5016 → etoolbox+pdftexcmds → `\AtEndPreamble`); `\typeout` runs under `\set@display@protect` (`\protect`=`\string`, latex.ltx:1185-1188/1438) so KOMA's `\typeout{…\small…}` prints the name instead of re-expanding `\@setfontsize` to PushbackLimit (pdflatex-probed `SIZE:[fontsize=\small \add@extra@listi{sml},…]`) — hvextern PushbackLimit-Fatal→1 (`\myFont` = fontspec). Agent-triaged, NOT fixed: neoschool-fr `\text_titlecase` EoF-Fatal = SHARED (P16 xiii: `\sys_if_engine_opentype:TF` false while tokens are per-codepoint, so `\__text_codepoint_process:nN` treats `é` as a UTF-8 lead byte; Perl 101 errors identical); glossaries-user TokenLimit = SHARED pgf/tcolorbox `enhanced`+`attach boxed title` frame-drawing volume (≈0.45M tokens × ~706 boxes; `LATEXML_TOKEN_LIMIT=0` completes; Perl would time out), lever = native pgfsys SVG scope protocol; final-binary re-sweep 2026-09-02 (`~/data/pk_b53final`): TUDaPhD/Thesis/Exercise 3, TUDaPub 2, TUDaReport 0, onedown-ref 0, hvextern 1, hvfloat 7, xltabular-doc 3, neoschool 9, neoschool-fr 7+EoF-Fatal, glossaries-user 3+TokenLimit, glossaries-extra-manual 13→5+TokenLimit (same pgf/tcb volume), tikzpingus-doc 494+PushbackLimit; hyperref `\@pdftitle`/`\@pdfauthor`/… info-string macros now default to `\@empty` and `\hypersetup` defines them per key (hyperref.sty:3979-3992, 3543-3596; nlctuserguide.sty:1630 `author={\@pdfauthor}` — glossaries-extra-manual, RUST-only, guard `hypersetup_defines_pdf_info_macros`); xkeyval `\ProcessOptionsX` (non-star) now routes unknown options to the `\DeclareOptionX*` handler (xkeyval.tex:496-502 — the star only adds class options; Perl xkeyval.sty.ltxml:355 has the same `defined $star` gate, KPE #122); hyperref's `\usepackage` option loop strips keyval's one brace level (`pdftitle={…}` had reached the RDF `content=` braced once `\opt@` kept braces — hypertest golden) | `perfect_kernel_batch53::{numexpr_factor_reexpands_noexpanded_macro, ifformatatleast_compares_real_fmtversion, koma_declaresectioncommand_heading_is_a_subsection, koma_redeclaresectioncommand_and_deftocheading, koma_title_pieces_reach_frontmatter, newkomafont_survives_scrlayer_scrpage_and_usekomafont_expands, raw_typearea_defines_areaset_conditionals, raw_scrlayer_scrpage_loads_and_sets_keys, startsection_level_is_a_tex_number, declaredoptions_lists_declared_options, classoptionslist_has_letter_catcodes, def_delimiter_keeps_adjacent_spaces, process_key_options_sees_passed_options, expkv_blank_entry_does_not_leak_markers, addcontentsline_title_is_not_digested, pagestyle_expandafter_freeze_terminates, xltabular_is_a_longtable, hvfloat_only_text_env_options_and_ifoddpage, collcell_hands_cell_to_macro, opt_macro_keeps_braced_option_values, tcb_listing_trailing_optionals_not_mandatory, siunitx_loads_translations, typeout_writes_robust_commands_by_name, hypersetup_defines_pdf_info_macros, processoptionsx_unknown_option_reaches_star_handler}` |
| 2026-09-01 | **batch 52** (Until-at-EOF abort, xcolor target model, xspace, memoir `\@currbox`, `\input` re-read + file substitution, tagpdf role tables): a `Until:` miss at true end-of-input reports ONCE then raises `Fatal:Mouth:EoF` (tex.web §338 abandons the call, §360 aborts the job with no legal `\end`; Perl re-runs the empty-arg call to `too_many_errors`) — stex-doc 513→1+Fatal, tagpdf 523/pgfPT 501 `Until:` storms gone; `\selectcolormodel` is the real xcolor.sty:137-146 macro and `\definecolor` converts into the target model (`convert_to_target_model`), so `hsb` colors under `\selectcolormodel{rgb}` reach pgf as rgb (tikzlings' 29 pgf errors); `\extractcolorspecs` stores the BARE spec (xcolor.sty:1033-1036; Perl xcolor.sty.ltxml:808 braces it, KPE #117) — pgfPT.colorSchemes.info 501→0; xspace treats a pending SPACE token as an exception (xspace.sty:49 `\@sptoken`, KPE #116; glossary golden `\gls{potato} and`); `\@currbox` is a `\newbox` register (latex.ltx:12099; Perl's empty macro made dpfloat/memoir `\csname …\@currbox` scan to EOF, KPE #115) — memman 1001→478; `\input`/`\tex_input:D` of an already-`\usepackage`d `.sty` re-reads the file (Package.pm:2270 `reloadable`; the binding-only probe in `input_definitions` was a silent no-op — tikzlings `\CommentInput{tikzlings-bears.sty}` harvested nothing) and `\input`/`\IfFileExists`/`\InputIfFileExists` honor `\declare@file@substitution` (latex.ltx:19677-19810 `\@file-subst@<name>`; Perl `\input` bypasses it) so the doc's `lines.tex` shadow of the `.sty` is what gets read (pdflatex-probed `[REPLACEMENT ] [REPLACEMENT ] [ORIGINAL ]`); tagpdf binding fills `\g__tag_role_NS_<ns>_prop` from the TL `tagpdf-ns-<ns>.def` CSVs with tagpdf.sty:1594-1665's line reader (the manual lists the PDF 1.7/2.0 structure names by iterating them) — tagpdf 523→21; harness `run_doc.sh`: fatal grep class `[A-Za-z]` (only `Fatal:oom:` is lowercase — sweep 28's fatal column counted 25 of ~290; status was right only via the exit code) | stex-doc 513→4+Fatal (stex archive Fatal = out-of-scope, babel `\bbl@savetoday/date` residual); tagpdf 523→21 (listings `\lstrenewenvironment` internals SHARED, `\bookmark`, `\meta`); pgfPT.colorSchemes.info 501→0 (status 1); tikzlings-doc 36→6 (KOMA `\deftocheading`/`\RedeclareSectionCommand`/`\TOCLineLeaderFill`, hyperref `\hyper@linkstart/end`/`\Hy@tocdestname`) but now runs to the 400M TokenLimit — ~250 raw-pgf animal pictures at ~1.6M tokens each (marmot alone ≈0.7M; preamble 6.5M) = legit volume, see P59; memman 1001→478 (`&` 130, `\endlx@list` 94, `\GenericError` 88, caption 46, `\@sharp` 28); easybook 100; verbatimcopy 12; fancyvrb-doc 3 | `perfect_kernel_batch52::*` (`until_miss_at_eof_is_fatal_once`, `selectcolormodel_converts_definecolor`, `extractcolorspecs_plural_is_unbraced`, `xspace_pending_space_token_is_an_exception`, `currbox_is_a_box_register`, `input_rereads_loaded_sty`, `input_honors_file_substitution`, `tagpdf_role_namespace_props_are_populated`); batch-48 `until_miss_at_true_eof_errors_like_perl` now asserts the Fatal |
| 2026-09-01 | **batch 51** (sweep-28 clusters A/B + P15 file side): eTeX §362 — the CURRENT `\everyeof` list is inserted at the end of EVERY `\input` file mouth (`load_tex_content` marks the mouth; `cross_marked_mouth` leaves an EMPTY payload to the mouth's owner so the `at_end_of_all_input` verdict is unchanged for the 99% case), so line-reader idioms across `\tex_input:D` terminate (tikzlings `\CommentInput`, stex smsmode, expl3 `\file_get:nnN`; pdflatex-probed `[alpha beta ]`); amsgen `\new@ifnextchar` is the real amsgen.sty:54-62 macro — NO space skip (Perl Lets it to `\@ifnextchar`, KPE #113; bibleref `\bibleverse{Psalms} (remark)` scanned to EOF); contrib `\printbibliography` saves/restores `\verb`/`\endverb` around the `.bbl` read (ar5iv-bindings :410 leak, KPE #114; docsurvey's 7 `\verb+.dtx+` after the bibliographies had swallowed ~500 lines); `\endlx@list` pops ONLY a frame `\lx@list` opened (groupInitiator) and otherwise reports Perl's `endMode` "Attempt to end mode internal_vertical" WITHOUT popping (Stomach.pm:524-531) — the unconditional egroup had popped the enclosing `{` frame under raw memoir `\list` (= latex.ltx `\list`, ends in `\@trivlist`) and cascaded; the one-error-per-list residual is P38 (raw `\list`/`\@trivlist` semantics, pending P16 approval) | docsurvey 7→0; en/de-bibleref-german 14→2 each (residual `\libertineGlyph`, `{addmargin}`); rub-kunstgeschichte 8→4 (biblatex `\printbibheading`/`\defbibcheck`/`\DeclareRedundantLanguages`); tikzmarmots-doc 501+F→1; tikzlings-doc 536→36 (29× pgf "Unsupported color model `hsb`" → batch 52); memman 1001→1001 and biblatex-oxref ×4 1001→1001 (egroup cascade gone, NEW unbounded expanding `\csname` scan after the no-pop `\endlx@list` error — root-causing in flight); verbatimcopy 12; stex-doc 513 (508× `Until:\prg_break_point:Nn`), tagpdf 523, pgfPT 501 (`Until:]`) — unchanged, delegated | `perfect_kernel_batch51::*` (`input_file_end_inserts_everyeof`, `new_ifnextchar_keeps_space`, `verb_survives_printbibliography`, `endlist_without_lx_list_frame`) |
| 2026-09-01 | **batch 50** (P50 + calc/mhchem + P44's real cascade): `\filename@parse` is the latex.ltx:228-281 macro chain verbatim (UNIX `\filename@path`/`@simple`/`@dots`) instead of a Perl-shaped `pathname::split` + `ExplodeText` re-letter (Perl pool:980) — the argument's catcodes now survive into `\filename@area/base/ext`, so `\ifx` against a sanitized string works and the currfile re-sanitize workaround (b48) is DELETED; calc `\widthof`/`\heightof`/`\depthof`/`\totalheightof` digest their box in `restricted_horizontal` (Perl calc.sty.ltxml:140 `readBoxContents(…,'restricted_horizontal')`) — in math mode the bare `digest` ran the box IN the math frame, so mhchem's `\ce{^{227}Th}` (mhchem.sty measures the pre-script with `\widthof`) closed the outer `$` from inside; `\verbatim@start` consumes one control-sequence/EOL token like verbatim.sty:107-112 (`\if\noexpand#1\noexpand~`), so the `\verbatim@start\relax` idiom (curve2e-manual.tex:95, newfile.sty:131) no longer writes a literal `\relax` first line into the verbatim (RUST-ONLY: the Rust `\verbatim@` reads raw lines and had serialized the pending `\relax` pushback; pdflatex oracle: no `\relax` line) — that, not `\verbatiminput`, was the P44 "missing-file cascade" | mhchem 67→13 (rest = `\ce` in `align*`, SHARED R3c); curve2e-manual 100+Fatal → 41 (all 26 `missing_file` gone; rest = curve2e/pict2e stubs → P57); digiconfigs 9→5 (rest = memoir `\renewcommand*{\list}` → P58); milsymb 48 = same on bin_b48 (stale baseline 37, not a regression) | `perfect_kernel_batch50::*` (`filename_parse_keeps_argument_catcodes`, `calc_widthof_in_math_measures_own_box`, `verbatim_start_relax_idiom`); `currfile_ifcurrfilename_compares` still green without the workaround |
| 2026-09-01 | **batch 49** (P53/P51/P54a): `\@currenvir` for `DefEnvironment!` envs = the name's CHARACTER tokens (Perl Package.pm:1927 `DefMacroI`→TokenizeInternal, latex.ltx:15350) instead of one multi-char letter token — `\ifx\reserved@a\@currenvir` consumers (`\@checkend`, collectbox, storebox, nag, powerdot …) took the wrong branch silently (listings' `lstlisting` had the same shape); xcolor `[ps]` colors register as the model's white (xcolor.sty:531-533) instead of vanishing (KNOWN_PERL_ERRORS #111); CJK binding supplies `\CJK@uniPunct`/`\CJK@punctchar` (CJK.enc:291 + the Unicode reduction of CJKpunct.sty:451-474; #112) | xcolor/xcolor2 101+Fatal → 0 (5 warn); jnuexam 2→0; joinbox 2→0; 16 more ctex manuals −2 each; `\@currenvir` 0 lines today (silent wrong branch) | `perfect_kernel_batch49::*` (`currenvir_is_character_tokens`, `xcolor_ps_color_registers_as_model_white`, `ctex_cjkpunct_unicode_punctuation`); full suite 2250 green |
| 2026-09-01 | **wave-3 verdicts (no code)**: kblocks-doc 97 = SHARED (Perl 102; `$\bm{\hat{m}$}` kblocks-doc.tex:207 poisons the picture via the parked mode-frame family + raw tikz `\node`/`\draw`) STOP; yquant-doc 200 = SHARED out-of-scope (XeLaTeX+fontspec, minted2 shell-escape, KOMA scrbase `\KOMA@key` key machinery unbound → 30× `#` PARAM + 45 undefined; the 84 `\end{minipage}` are an unclosed `\begingroup` the doc's minted patch was to close — NOT mode-frame; Perl fatals at ~10) STOP — general lever noted: a KOMA scrbase key-machinery binding; fancybox-doc 46+Fatal: fatal = short-verbatim `"\begin"` inside `\footnote` (fancybox-doc.tex:310, the `\VerbatimFootnotes` family), ~40 more = the same catcode-live verbatim family leaking `\Verbatim@Space` into the verbtable (`&`/`\vcenter` cascades), 4 = `\caption` inside `\fbox{\begin{minipage}…}` (SHARED, Perl 2 — float-only element in a block; `\@makecaption` inline form would be a surpass); the frame boxes/`{Sbox}`/B-environments are all correct in isolation | — | repros under `$SC/b48-{kblocks,yquant,fancybox,vfoot}/` |
| 2026-09-01 | **batch 48** (P43/P44 + `{VerbatimOut}` zero-diffs trap + VFS abort): currfile raw-loads (filehook `INTERPRETABLE_SOURCES` for filehook-2020/2019; `\ifcurrfilename` family compares against the sanitized `\filename@parse` pieces); **all fancyvrb/fancybox `{VerbatimOut}` overrides DELETED** — the raw `\FVB@VerbatimOut` layer (fancyvrb.sty:1053-1090, fancybox.sty:1000-1020) works as-is, and the override read `[` as the FILE NAME for `\begin{VerbatimOut}[keys]{file}` and swallowed the rest of the document into it (`Cached VerbatimOut for [`) — 18 docs reported clean with nothing rendered; fancybox binding = raw load first + Perl's 4 frame boxes/`{Sbox}` over it; virtual file store rewritten as ONE owned `HashMap` under `VIRTUAL_FILES` mutated in place (`vfs_append_line` re-pinned the whole file per `\write` line in the never-freed interner → offset past `u32` → `string-interner get_unchecked` ABORT on an unterminated `{VerbatimOut}`); `Until` reader reports a miss at `gullet::at_end_of_all_input()` as `Error:expected:Until:` (tex.web §338; Perl Parameter.pm:93-97) instead of an empty arg — the `\FV@Scan` loop now ends in `Fatal:TooManyErrors` like Perl instead of spinning; LSP overlay stores through `vfs_store`/`vfs_remove` | pythontex/pythontex ×2 → 1/0/0; fancyvrb-doc 5→3 (rest = `\VerbatimFootnotes` live-body, below); **unmasked 18 docs (old→new errors, honest)**: chinesechess 3→3+TokenLimit Fatal, circledtext 4→7, epigraph-keys 0→0, joinbox 0→2, kblocks-doc 0→97, mhchem 0→67, pascaltriangle 0→0, pygmentex_demo 1→4, simples-matrices-eng/fra 14→13, spath3 6s→executes all 21 tikz examples (timeout in the DEBUG binary — re-time with release), suanpan-l3 2→4, tablor 3→3, tikzPackets 0, tiscreen-doc 0, xcolor2 0→101+Fatal (`lambda` ps color — P51), yquant-doc 136→200 (84× `\end{minipage}` mode-frame, 30× `#` PARAM, 5× `\slnc@name`); these were never clean, the old numbers were the trap | `perfect_kernel_batch48::*` (7 guards: `currfile_ifcurrfilename_compares`, `verbatimout_wrapper_environment_round_trips`, `verbatimout_custom_fvb_hook_is_honoured`, `verbatimout_optional_keys_do_not_swallow_document`, `fancybox_verbatim_layer_raw`, `until_miss_at_true_eof_errors_like_perl` (red = abort on bin_b47), `virtual_file_many_line_append_reads_back`) |
| 2026-09-01 | **batch 47** (P40/P41/P32/P33/P45): `read_alignment_template` expands token-bodied macros only (b33 regression: primitive expandables — `\csname` — read to EOF via the SHARED Alignment.pm:884-912 safety valve); `read_newline_args` drops the Rust-only `Err` when no Alignment is active (Perl pool:557-571; aguplus.cls:305 `\let\\\@tabularcr\@tabarray`); nicematrix `V{width}` varwidth column + `NiceTabular` family grows the preamble for `first-col`/`last-col` (incl. trailing `[opts]`, nicematrix.sty:2007) + `\EmptyColumn`/`\EmptyRow`/`\RoundedRectangleColor`/`\RowColors`/`\ShowCellNames`/`\TikzEveryCell` `\CodeBefore` recorders (nicematrix.sty:1790-1809); empty VFS files count as existing (`vfs_read`/`vfs_exists` in content.rs load/open/dep-scan/`find_file_aux`; tcbposter.code.tex:168-171 writes an empty `.tcbtemp`) | nicematrix/nicematrix 1001+Fatal(MaxLimit) → 160 (residual: TokenLimit at EOF after a `readBalanced` over-read past line 3061 — P49; 58× `&` at lines 409-411 pre-b33 residual); aguplus/aguplus Fatal → 24 errors (P48); tcolorbox-tutorial-poster 7→0; bfh-ci DEMO-BFHSciPoster 5→4; tuda-ci DEMO-TUDaPoster 87→87 | `perfect_kernel_batch47::*` (4 guards: `nicematrix_v_column_and_macro_colspec`, `newline_outside_alignment_is_not_fatal` (red on 8c525a6be4), `nicetabular_first_col_growth_and_codebefore_surface`, `empty_virtual_file_exists`) |
| 2026-09-01 | **batch 46** (P27/P28/P29/P23/P24): memoir raw-load shim (both preload modes interpret the real class); stale nicematrix/tabularray `missing_file` warnings deleted; `\index` entries EXPANDED before `@`/`!`/`|` split (latex.ltx:17720 `\protected@write`) with sort keys sanitized + keyed off source tokens; `NiceTabularX`/`NiceTabular*` route to tabularx/tabular*; tabularray template API (`\DeclareTblrTemplate`, `\UseTblrTemplate`, `\MapTblrRemarks`, `\InsertTblrRemarkTag`…) | tagpdf 113→21 (residual: `misdefined:#` ×7, pgfkeys `/tikz/alt`, `\tagtool`/`\meta`/`\lst@XConvert`); titlepages 4→0, dlfltxbmarkup 3→0; cahierprof/nicematrix NiceTabularX `&` storm gone; tabularray-abnt (still blocked by biblatex-abnt×babel runaway) | `perfect_kernel_batch46::*` (6 guards) + `memoir_output_streams` |
| 2026-09-01 | **batch 45** (wave-3 plans P18-P22, all LOW): `\DeclareMathOperator` expands its text before the String round-trip (expl3 `\cs_to_str:N` names shattered under sty-state catcodes; numerica Fatal+100→21+F, residual = `\eval` in align* env lookup, lane w3Q); `\PassOptionsToPackage/Class` spread options one `Stored::String` each (Perl Package.pm:2435; a nested `Stored::Strings` read back EMPTY — RUST-ONLY kernel primitive bug); amsmath `\if@display` (amsmath.sty:649); natbib's remaining `\newif` surface (@longnames/@openbib/@par/@full/@stdbst); `\@enumctr` defined by enumerate (latex.ltx:16057) | bullcntr-man 101→**0**; gaceta ×2 12→1 (`\deactivatetilden`); brandeis example 86→41 (tabu internals, SHARED); nmbib-sample 40→23 (natbib internals, SHARED); numerica | `declaremathoperator_expands_expl3_name`, `passoptions_spreads_options`, `amsmath_if_display_defined`, `natbib_newif_surface_complete`, `enumerate_defines_enumctr` |
| 2026-09-01 | **batches 40-44** — see PLANS P9-P17 rows (atableau xparse leading optional + stub removal, beamer `\newif` surface, `\frame{}` command form, amsopn operator re-assert, amsldoc `\@nobslash`, curve2e `\ver@`, `\pscircle` optional pair, listings write-file VFS, `\newtcbinputlisting`, xkeyval presets, color-key symmetry, index sortas) | sweep rows #26/#27 | `perfect_kernel_batch40_43::*` |
| 2026-09-01 | **batch 39**: xkeyval `\XKV@checkchoice` chain (xkeyval.tex L249-321 verbatim — choice-key checker style authors call directly: regulatory, glossaries-extra, keyreader) | regulatory example1-en 5→1 | xkeyval guards |
| 2026-09-01 | **batch 38**: kernel macro layering — `\tableofcontents`/`\listoffigures`/`\listoftables` are MACROS delegating to `\lx@kernel@*` constructors (real latex.ltx shape), so etoolbox `\expandonce`-based patching (`\appto{\tableofcontents}{…}`, packdoc) gets a patchable body instead of building a self-recursive def (Perl shares the constructor-bound shape) | **algxpar-doc 315→0** (TOC restored) | suite 2212/2212 |
| 2026-09-01 | **batch 37**: aomart binding completed — manuscript-review surface (HSelect/ECSelect/Highlight/EditorialComment noops-or-passthrough per the class's non-manuscript branches), review gobbles (proposed/seconded/corresponding), contact frontmatter (urladdr/contrib), cross-ref sugar (fullref family L745-750), `\funding[text]{sponsor}{grantid}` (L778) | **aomsample ×2: 101 → 0** | witness re-runs |
| 2026-09-01 | **batch 36**: real-ulem word-machinery internals (`\UL@word`/`\UL@start`/`\UL@stop`/`\UL@hook`/`\UL@end *`…) provided inertly — raw add-on code wires into them by name (xeCJKfntef; the reimpl-internal-name-mismatch shape); xeCJK expl3 no-break/symbol surface noops. fixdif residual loop = raw xeCJK's OWN load failure (XeTeX engine, D9 — parked) | fixdif partial, ulem raw-wiring class | guards |
| 2026-09-01 | **batch 35** (VFS abstraction + sweep-23 regression repair): `latexml_core::binding::virtual_files` — the named in-memory VFS owning the `{name}_contents` convention (openout/write streams, filecontents, both VerbatimOuts, memoir writeverbatim, verbatiminput and all content-cache readers route through it); one shared `capture_raw_lines_until` retires the 3 duplicated scanners; sweep-23's 63 regressions (`Until:` Missing-argument on ARTIFICIAL mouth ends — our isolated-arg mouths are not TeX file ends, spath3/litetable/zref l3tl sentinels live past them) repaired: Until-misses deliver empty quietly again, loop-termination stays with the cycle guard; `\everyeof` mechanism implemented (token-identity insertion at close, scantokens transparency in read_token) but left UNWIRED — enabling it fixes tl_set_rescan exactly but loops spath3-family (PLANS open item) | spath3 0/0, zref-check-doc 0/0, litetable 1/0, pstool 0/0, algxpar 1, elsdoc 0, willowtreebook 8/0 | suite 2212/2212 |
| 2026-09-01 | **batch 34** (plans P2/P3/P6/P8): `\verbatim@start` = pure line pump, env macros own the element (elsdoc 14→0; sweep-21 "0" was fatal-masked — signal-integrity trap); `writable_tokens` honors `\escapechar` (tex.web §1594/§262; xint two-stage capture — tkz-grapheur ×4 fatals→0, esc3 twin pdflatex-identical); `\expandafter` terminates on stream-end with ONE error (was: spin to cap); `\verb` applies `\dospecials` (csquotes-registered active chars neutralized — biblatex 101+F→6); `read_until` → distinguishable EOF + `Until:` Missing-argument per iteration (Perl-shape; ends zero-progress delimited-scan loops via the latch); memoir `\writeverbatim` = raw-line capture with env redirect (willowtreebook 511+F→8) | elsdoc, biblatex, tkz-grapheur, willowtreebook, min5/esc3/d4 repros | suite 2212/2212 |
| 2026-09-01 | **batch 33** (plans P1/P4/P7 + refactors): `\newtheorem` absorbs+discards a leading style optional (aomart.cls L676 ground truth; locked-CS wrapper no-op made `[` a theorem NAME whose csname form clobbered `\[` — aomsample 101→10; Perl shares; KPE #82); index-phrase splitter separators act at brace depth 0 only (Perl flat-scan shreds groups — packdoc/algxpar 315→1; KPE #83); alignment-template reader expands macro-valued column ops (latex.ltx `\@xexpast` edef ground truth; dijkstra repro 2→0); filecontents leading optional absorbed (algxpar-lzw cache); refactors: with_meaning swaps, unread_mut removed (dead + mis-flavored), retract_scanned_brace singular helper ×8 sites, dead expl3 locals dropped, stray latexml.log un-tracked | aomsample ×2, algxpar, numerica family, dijkstra | suite 2212/2212 |
| 2026-09-01 | **batch 32**: hyperref PDF-form field model — \TextField/\CheckBox/\ChoiceMenu/buttons render readable placeholders; the `Field` keyval family stores into real `\Fld@<key>` macros ([true] defaults, hyperref-init'd numerics); driver internals stubbed (HyField@Flags*, Hy@escapeform/escapestring, HyAnn@AbsPageLabel). Perl leaves the whole family as comments (L442-451) | hyperbar example 60→1 (residual = parked mode family at \end{Form}) | suite |
| 2026-09-01 | **batch 31**: expl3 exit-`\ExplSyntaxOff` cleanup now fires ONLY for handleoptions frames (\usepackage/\documentclass — where the kernel's \@popfilename would restore); a plain `\input` restores NOTHING in real TeX, and firing mid-class fought ctexhook's own `\ctex_push_file:`/`\ctex_pop_file:` expl-status stack — full expl3 state leaked past `\documentclass{ctexart}` and garbled pgfutil-common's `\def\: {…}` space-trick | **ctexart+tikz 1001→0**, hfutexam 1001→4, document-level catcodes correct (space=10/`: `=12/`_`=8); xsavebox chain + all 68 package guards green | suite (3 contention-artifact FAILs re-verified green solo+binary) |
| 2026-09-01 | **batch 30**: implicit parameter tokens — a CS `\let` to a catcode-6 `#` participates in `#1`/`##` pairing during macro-definition scanning (tex.web get_next meaning-resolution; docstrip-generated code writes `\c_parameter_token 1` literally: ctexart.cls L1194) — pack_parameters resolves them (bare ctexart 2→0, unblocks the 50-doc ctex family's first domino); env-gated LATEXML_EXPL_TRACE exit-decision trace; CLUSTERS records ctex part-2 (expl3 state leak past \documentclass → pgfutil space-trick garble → 1001-cascades; deep fix = kernel \@pushfilename/\@popfilename expl-hook routing, own session) | ctexart bare 2→0 | suite |
| 2026-09-01 | **batch 29**: Pair parameter unwraps a leading brace group (latex.ltx L16768 `\long\def\picture#1{\pictur@#1}` — the kernel's braced-form tolerance; Perl ReadPair lacks it; axodraw2-man `\begin{axopicture}{(90,140)(-10,0)}` 101→0); fancyvrb/fancybox `{VerbatimOut}{file}` captures to the filecontents cache, `\VerbatimEnvironment` redirects the end-scan to the wrapping env, end-marker matches the WHOLE line (fancybox-doc defeats mid-line matches with `\end{VerbatimOut}%.`); `\verbatiminput` expands its arg + serves cached captures; fancybox `\LVerbatimInput`/`\VerbatimFootnotes` | axodraw2 101→0, fancybox-doc 101→92-with-real-pipeline (rest = fancybox's own \SaveVerb API, recorded) | suite |
| 2026-09-01 | **batch 28**: `\pagegoal` = max_dimen while page empty (tex.web §982; Perl's Dimension(0) makes free-space probes loop — fullwidth eject-retry ∞); `\vsplit` actually SPLITS (tex.web §977-979 approximated at box boundaries: return top-D worth, store remainder, void when done — Perl returns all + keeps register: short-math-guide column-drain looped ∞ AND duplicated); beamer theme-load surface completions (DeclareOptionBeamer/ExecuteOptionsBeamer/defbeamertemplateparent/AtBeginSubsubsection/ifbeamer@compress — sweep-21 caught 10 theme docs regressed by batch-26 theme loading, now recovered) | fullwidth F→0, short-math-guide F→0 E→0, tzplot 249→56 F→0, dijkstra F→0; celestia/gotham-simple/phnompenh 4-6→0-2 | suite |
| 2026-09-01 | **batch 27**: autoload-trigger identity snapshot — `\@ifundefined{X}` sees the kernel dump's real def over the stale `:autoload` flag (Rc identity; updatemarks 101→2, 21-doc `unexpected:_` cluster); listings `\lst@true`/`\lst@false`/`\lst@if` internals for raw add-on styles (lstfiracode et al.; rbt-mathnotes 101→4); `\iffontchar` eTeX conditional implemented (Perl leaves it a comment — kernel's `\tex_iffontchar:D` was a dangling let; unicodefonttable-samples 100→0); Umath\* primitives moved POST-dump (pre-format defs made latex.ltx's `\ifx\Umathcode\@undefined` probe bake TU into the format — greek_test caught the poisoned dump; probe-safety class of the \directlua rule) | updatemarks, rbt-mathnotes, unicodefonttable, greek | suite + trigid guard |
| 2026-09-01 | **batch 26**: kpse TEX-format fallback on extension-guess miss (pgfornament `\@@input vectorianN.pgf`, 401 errs → 0; Perl reaches these via its ls-R cache); beamer color model — `\setbeamercolor` palette storage + parent/use resolution + eager `<name>.fg/.bg` xcolor registration, `\usebeamercolor`, `\ifbeamercolorempty`, default palette via raw beamercolorthemedefault.sty, `structure`/`fg`/`bg` aliases (beamerbasecolor L387); `\usetheme`/`\usecolortheme`/`\useinner`/`\useoutertheme` now RAW-LOAD their theme files; `\defbeamertemplate` registers `\beamer@@tmpop@` markers; beamer requires pgf | metropolis demo 51→4, epyt 66→2, trigon 66→2, gotham 41→10, amurmaple 102→33, albi 436→62 | suite + slides/tikz goldens |
| 2026-09-01 | **batch 24**: `\DocumentMetadata` flips the kernel's declared conditionals (latex.ltx L9167-9175 `\IfDocumentMetadata{TF,T,F}`) so `\NeedsDocumentMetadata` classes pass (ltx-talk "This file needs \DocumentMetadata" ×12 docs) | ltx-talk family; any DocumentMetadata-gated class | suite |
| 2026-09-01 | **batch 23**: hvFloat multiFloat form (`+{type}{obj}[short]{cap}`×N + one trailing {label} — the naive prototype grabbed the bare `+` as an env name, `undefined:{+}` ×15 docs) via a var-length closure; kernel-level tagging absorb (`\DocumentMetadata{tagging=on}` activates the latex-lab surface WITHOUT tagpdf.sty ever loading); enumitem `\setlistdepth`/`\renewlist`; KOMA `\newkomafont`/`\addtokomafont` (scr* ×3) | hvfloat multi-* 15 docs →0; tagpdf manuals + tex-vpat →0; homework/jwjournal, beautybook | suite |
| 2026-09-01 | **batch 22**: hyperref `{Form}` env + hycolor `\HyColor@*` 4-arg absorbers (pdfcomment); biblatex keyval-layer aliases `\blx@kv@defkey`/`setkeys` (biblatex.sty L76-90 — loaded .bbx styles call them); glossaries-extra label-prefix noops | pdfcomment example →**0**; hyperbar/tdclock Form docs; philosophy/sidenotes bbx chains | suite |
| 2026-09-01 | **batch 21**: CJK/xeCJK/ctex SURFACE absorbed in cjk_sty.rs (`\CJKaddEncHook` 14-bundle cluster; font selection = the D6-fontspec shape, hooks = engine bookkeeping; deep CJK typesetting stays in DIFFICULT_CASES) | **einfart 267→0** (with batch 20); bfh-ci 9 residual; the CJK-surface tail across non-CJK docs pulling CJKpunct/CJKspace raw | suite |
| 2026-09-01 | **batch 20 — expl3-class file-name arguments (the [[project_explsyntax_midload]] family root)**: `\documentclass`/`\usepackage`/`\RequirePackage`/`\LoadClass` name args now read as `ExpandedSemiverbatim` — tokenize PLAIN at the CALLER's catcodes (the real kernel's `\LoadClass` takes a plain `{}` arg), expand only when a CS is present, build the name from RAW token chars, and keep the semiverbatim DIGEST protection (Parameter::digest ASCII-neutralize; without it OTHER `_` OT1-decoded to `˙` — omnibus_references + neurips guards caught both regressions live). OLD behavior: Semiverbatim catcode-flip at tokenize split `\l_my_base_tl` into `\l`+`_my`+… → class "ł_my_base_tl" → OmniBus with garbage name → expl3 regime lost (Perl SHARES it — its SPECIALS include `_`) | **einfart 267→9, bfh-ci 18→9**; homework/jwjournal/verifica expl3-class tail | neurips + omnibus_references guards (regression-caught twice), suite |
| 2026-09-01 | **batch 18** (undefined-CS tail, true-name re-rank of the scanner's collapsed bucket): luatexbase real surface replacing the Warn-stub (catcode-table/attribute allocators, `\newprotectedluacmd` — 8-bundle `\initcatcodetable` tail); Unicode-engine math-code primitives `\Umathcode`/`\Umathchardef`/`\Umathcharnumdef`/`\Udelcode` absorbed with faithful assignment syntax (fontsetup fspdefault.tex; recorded follow-up item); tagpdf_sty.rs NEW absorb binding (our XML IS the accessible structure); biblatex `\blx@inputonce` (style .def files call it directly); beamer goto/skip/return buttons; typearea `\ProvideUnknownPaperSizeError` | keytheorems-doc 20→**0**; texlive-vpat, biblatex-software; elpres/fontsetup family | suite + witnesses |
| 2026-09-01 | **batch 17** (OOM agent — 6 fatals → 3 families): **optional-arg protection gate** — real LaTeX defines every `\newcommand[opt]` through `\@protected@testopt` (emits `\protect\cs` UNEXPANDED when `\protect` ≢ `\@typeset@protect`); our binding-built Optional params had no such indirection, so titlecaps' recursive `\textnc`→`\titlecap[s]` chain expanded forever under `\protected@edef` → 2 GB pushback OOM. Gate added at both gullet expansion points (leading-Optional + non-typeset protect = protected); engine-independent red/green fixtures p1/p2. titlecaps OOM→18 errs. **Default 5M pushback cap in the CLI** (env escape `LATEXML_NO_DEFAULT_PUSHBACK_LIMIT`) — msc/spath3/srdp now Fatal:PushbackLimit gracefully WITH a written document; **cortex_worker's existing cap was a NO-OP** (set on the main thread, gullet is thread-local — moved inside the worker closure; production bug); OOM message now attributes by alloc alignment (align 4 = pushback; 8/1 = other — quiver/eledform misattribution cost triage time) | titlecaps, msc, spath3/knots, srdp-mathematik (4/6 OOM deaths recoverable); quiver (tblr×tikzcd re-scan) + eledform (string buffer) = distinct, recorded | suite; p1/p2 fixtures in scratch |
| 2026-09-01 | **batch 16**: hvfloat_sty.rs NEW binding (raw-impossibility: `\hvFloat` builds floats manually from boxes with `\@captype` set — `\caption`'s `^^` float-up finds no legal ancestor, `malformed:ltx:(toc)caption` ×43 docs = sweep-16's largest malformed cluster; the box choreography is page layout, the semantics ARE float+object+caption+label → mapped to the real `{figure}`/`{table}` environment, + caption/graphicx dep chain) | hvfloat right1s2c/after1s1c → **0 errors**; 40+-doc family expected clear in sweep 17 | suite + witnesses |
| 2026-08-31 | **batch 15**: beamer overlay-aware DEFINITION forms — `\newcommand<>`/`\renewcommand<>`/`\newenvironment<>`/`\renewenvironment<>` (overlay maps to the always-true policy: spec param dropped, declared args shift); star-in-prototype COLLISION audit ×6 (`\setbeamercolor*{}{}` etc. hit the literal-char fallback and CLOBBERED the base definition — surfaced by batch-11's Match fallback; IEEE starred env handlers now use the literal `T_CS!("\IEEEeqnarray*")` form per [[feedback_defmacro_starred]]); beamer requires etoolbox + xcolor (Perl L412/L1054 parity — `\colorlet` undefined in every beamer doc) | cursolatex 55→**7** (misdef 27→0); tuda-ci/chuushaku beamer-demo family; every beamer doc using \colorlet | suite + cursolatex witness |
| 2026-08-31 | **batch 14** (param-text agent — `#{` scanner EXONERATED, tex.web §476-faithful): `\#`/`\&`/`\%` restored as latex.ltx L626-628 CHARDEFS (the IN_MATH-dispatch DefPrimitives preserved rendering but destroyed the numeric value — `\catcode\#14` read 0, varindex.dtx's `#`-as-comment block leaked 62 misdefined:#; Rust-only). CharDef invoke now mode-aware (Perl CharDef.pm L61: text \chardef IN math attaches unicode math props — the `$\&\&$` alignment-leak the overrides guarded) + Perl's local-vs-CS reversion choice (engine chardefs revert as `\&`, not `\char 38\relax`); harvmac round-trip golden + ifthen golden green. catoptions stub REMOVED (stale `\special_relax` rationale — raw catoptions loads; concepts misdef 43→0; new residual = our dont_expand marker corrupting `\cpt@csvdepth` one-token checks, recorded). tcolorbox `\tcbwritetemp`/`\tcbusetemplisting` + `\NewTCBListing` xparse flavor (keytheorems 56→20, verbatim-leaks 0); underoverlap = SHARED failure w/ real pdflatex (with.sty package-rot `\int_case:nnn`) — closed | varindex 62→0 misdef, concepts, keytheorems, calculatoritems/fenetrecas | ifthen/at_in_cs goldens + suite |
| 2026-08-31 | **batch 13**: listings write-file aspect (lstmisc.sty L30-70) as a display-time tee of the captured raw text (+ suppress-display for the `\lst@BeginWriteFile` variant) — the write-then-`\input`-back execute pattern (cnltx-example.sty L702-718) now round-trips | bohr/cnltx family root undefined cleared (residual = parked mode-frame `\endmdframed` cascade); also serves lstdoc.sty | suite + bohr witness |
| 2026-08-31 | **batch 12** (misdefined:# agent bisect — 4 causes): **(C, Rust-only)** partial-ExplSyntax leak normalized — l3backend under a raw class (jlreq/ctex → expl3.sty → l3backend) left space=catcode 9 with `_` already restored, an invalid half-state no legit flow produces; every later raw file lost its space tokens (pgfkeys' `\def\: {…}` broke, handler bodies EXECUTED, `#1`→stomach). ctex+tikz min-repro **1001→0**; xsavebox/tasks witnesses re-verified 0 (the naive bidirectional restore REGRESSED xsavebox — entry snapshot reads post-push state; kept the grandparent guard + added the narrow normalization). **(A, Perl-origin)** siunitx stubbed `\ProvidesExplFile` to nothing but the real macro (expl3.sty L33-47) ends in `\ExplSyntaxOn` — stub now turns syntax on; + autoload trigger. latex-via-exemplos min-repro 94→**0**. **(D, shared w/ Perl)** `\@ifnextchar` re-scans branches as macro bodies collapsing `##`→`#` (latex.ltx L1756) — ours replayed doubled params. adtreesdoc **2→0**. (F/E families deferred — likely covered by D's primitive, re-measure.) | gckanbun/ctex/jlreq tail, latex-via-exemplos 186, adtrees | `expl3_state_and_param_replay::*` (2 tests) |
| 2026-08-31 | **batch 11** (agent-root-caused, Perl=0-errors oracle): class deps-scan skipped after a SUCCESSFUL raw .cls load (Perl Package.pm L2731 scans only in the missing-binding-alternate branch; ours harvested `\LoadClass{beamer}` from beamerswitch's never-taken branches and loaded beamer_cls.rs AFTER raw beamerarticle, clobbering `\mode`); beamer `\mode` faithful shape (the `\mode<>{}` prototype hit the def_parser literal-char fallback whose Token reader eats one ARBITRARY token per literal — `\meaning\mode` was `macro:#1#2#3->`, ate exactly `<pr`); `\only`/`\alt`/`\visible`/`\uncover`/`\invisible`/`\temporal` consume their OptionalAngled overlay specs (identity forms executed never-taken `\only<handout>{\pgfpages…}` payloads); def_parser literal-char fallback now emits a `Match` parameter (consumes the literal) instead of `Token` (eats anything) | beamerswitch-example 6→**0** (Perl-parity); bundled-cls deps sim stays clean (raw `\RequirePackage` triggers bindings directly — 2202.11535 shape re-verified) | beamerswitch witness; suite |
| 2026-08-31 | **batch 9**: biblatex style-author API absorbed (Declare{Delim,Sorting,Uniquename,Nosort,MultiCite}* gobblers, hooks, punctuation — apa.bbx loads CLEAN) + `\DeclareCiteCommand` keeps EXISTING commands (a raw .cbx restyle of `\parencite` was aliasing it to plain `\cite`, dropping parens — caught by suite guards `cluster_biblatex_*`); tcolorbox `documentation` library `{dispExample}`/`{dispExample*}`/`{dispListing}` showexpl-style (whole family LOCKED — raw lib re-wires the END macros back into `\tcbwritetemp` otherwise); listings line-number stepping via locked `\lx@lst@stepnumber` internal (real listings advances `\c@lstnumber` at internals level — user-level `\stepcounter` emission let assoccnt's raw wrapper expand inside `<ltx:listing>`); **assoccnt_sty.rs** NEW binding (raw pkg wraps ALL kernel counter commands through \setkeys bookkeeping) | assoccnt manual 39-truncated→385→**10**; apa-style docs; dispExample family (assoccnt/calculatoritems/commalists-tools clusters) | `cluster_biblatex_*` (69 green), dispExample repros |
| 2026-08-31 | **batch 8** (sweep-13 ranking): alphabeta `\math<greek>` alias block (alphabeta.sty L109-160 — `alphabeta` maps to textgreek_sty.rs, raw let-block never ran); KOMA `\ifpdfoutput{}{}`→#1 + `\KOMAScript` logo (scr* ×3); color raw-storage fallback — read xcolor's `\color@<name>` internal macro before "assuming Black" (ydoc-desc L22 defines `\color@none` = empty color; reimpl-internal-name-mismatch shape); listings `\lst@xleftmargin`/`@xright` defaults (raw styles read them, 8 bundles); datetime format selectors (`\mmddyyyydate` family, L80-150, 4 bundles); biblatex category surface (6 bundles) | hep-paper 3→**0**; l2tabuen 5→1; macros2e 'none' ×5 bundles cleared; assoccnt/bohr targeted errors cleared (residuals = verbatim-leak + cnltx/mdframed mode families) | suite + witnesses |
| 2026-08-31 | **batch 7**: document-body `\input{x.sty}` reads raw files as CONTENT at current catcodes (`notex` gate in content.rs — binding dispatch still wins; the definitions mouth's forced `@`=letter corrupted doc.sty's `\CharacterTable` on every `\DocInput` re-read; Perl "passes" only by skipping the re-read and dropping the doc body); biblatex `style=`/`bibstyle=`/`citestyle=` load the raw `.bbx`/`.cbx` chain (declaration gobblers first, once-guard, native styles skipped) + `\AtNextCitekey` runs code in place + `\mkbibquote`/parens/brackets | frankenstein ×10 → abbrevs/titles **0 err**; pkgloader; windycity toggles 55→2, biblatex-ext 10→1 (residual mass = separate mode/tag family) | `input_routing_and_bbx::*` (2 tests) |
| 2026-08-31 | **batch 6** (agent-root-caused): babel assigns `\language=\l@<main>` at load (babel.sty L1136/L828 — preamble `\iflanguage` mis-branched in EVERY non-English doc); `\@endpart` kernel contract (report.cls L318/book.cls L339, toptesi.sty L448 calls it); titleps_sty.rs NEW binding + titlesec `pagestyles`-option loads it (titlesec.sty L1392-1422); datetime `\monthname`/`\shortmonthname` real names (were content-losing noops); schooldocs_sty.rs NEW binding (`\subtitle` hidden in a discarded `\fancypagestyle` body — shared failure w/ Perl fancyhdr noop); etoolbox `\robustify` second native sentinel `Stored[` (DefMath `\bmod`; real latex.ltx L15556 is a robust macro so real \robustify succeeds — ours must noop not error; yquant-doc L244) | ufrgscca 6→**0**; toptesi topfront manuals −34; schooldocs `\subtitle` cleared; yquant robustify line | `kernel_language_and_part_contracts::babel_language_register_and_endpart` |
| 2026-08-31 | **batch 5 — xkeyval deep port**: front-end scaffolding verbatim (`\XKV@testopta/b/c/d`, `\KV@@sp@def` space-trick, `\@selective@sanitize`, makepf/makehd, ifcmd/g@tkeyname, addtolist, `\XKV@setkeys`) with `\XKV@s@tkeys` as thin shim onto the Rust `\setkeys` (now private `\lx@xkv@setkeys` so a package overriding `\setkeys` — xkeymask's dispatcher — can't loop); **pointer system for real** (`\savekeys` family verbatim + `\XKV@<header><key>@value` store in the Rust reader + `\usevalue` closure via XKV@ptr@* context); **KNOWN_PERL_ERRORS #80** `\define@cmdkey` stray `T_PARAM` (Perl KeyVal.pm L131, author's own `!?!??!`) — code now sees the bare value; **token defaults** (`default_tks`) — a `[\xskak@val@defaultid]` default survived as ONE token instead of being string-round-tripped through the standard cattable (`\xskak`+`@val@defaultid` split = the 1000-error csname cascade); **xkeymask_sty.rs** NEW binding (raw pkg relies on `\XKV@resa` surviving `\KV@@sp@def`'s futurelet — self-referential macro real TeX tolerates, our/Perl recursion guard flags; pdflatex-probed) | xkeymask 7→**0/0/0**; xskak 1001+fatal→**5**; chessboard 1001-truncated→1 err (renders to L1627/2049, then `\chessboard[boardfontencoding=LSBC4]` 400M-token churn — recorded residual); every `\define@cmdkey` user corpus-wide | `xkeyval_internals::setkeys_frontend_clone_reaches_rust_path`, `xkeyval_internals::pointer_system_cmdkey_and_token_defaults` |
| 2026-08-31 | **7-fix batch 4**: `\if@openright` kernel contract (classes.dtx: book TRUE/report FALSE, options before ProcessOptions); scrlfile aux-hook noops (`\BeforeClosingMainAux[]{}`); scrlayer-scrpage page-style family; memoir → `LoadClass!("book")` + REAL output streams (newwrite/openout round-trip, `\writeverbatim` via `Until:\endwriteverbatim`+`\detokenize`) + ~30 geometry noops + setsecnumdepth table; beamer `\titlepage`/`\maketitle`→`\lx@frontmatter@fallback`, `\sectionpage` noops; mouth `^^^^hhhh`/`^^^^^^hhhhhh` caret forms (longest-match-first, \Ucharcat precedent); textgreek alphabeta letters + greek-fontenc combining accents | memoir 20+-doc family (dlfltxbmarkup stream round-trip); scr* aux cluster; beamerauxtheme; eigo/Celestia error-free; greek residual 91→46; ucharcat docs | `openright_kernel_contract`, `memoir_output_streams`, `unicode_caret_notation`, `accent_meaning_robust_shape` |

## Named exemplar: nicematrix (the mission's reference bundle)

`nicematrix.tex` (6,954 lines, LuaLaTeX-authored manual; tikz, siunitx,
tcolorbox, enumitem, fancyvrb, titlesec, varwidth, adjustbox…).

| Date | Binary | status | errors | fatals | warnings | secs | Note |
|---|---|---|---|---|---|---|---|
| 2026-08-31 | 1ef264a2bb test | 2 | 102 (capped) | 0 | 79,166 | 8.5 | first baseline; dominant noise: `Unrecognized tabular template` from `{NiceTabular}`-family preambles hitting the alignment template reader (`alignment.rs:997`); error heads: `Extra alignment tab '&'` ×26, `\noalign cannot be used here` ×11, nested-sectioning schema errors |
| 2026-08-31 (session 3) | 9b3da268fc release | 124→n/a | 108 | 2 | — | 9.6 | piton binding removed the 'LuaLaTeX mandatory' FATAL that had been TRUNCATING the manual — the full 8,334-line doc now runs (9.6s), all Code examples execute, exposing the real remaining surface: 108 errors, S3 word-recall 56.6%. Structural wall: an itemize whose items hold live tables breaks list mode (`\end{itemize} Attempt to end mode internal_vertical`) → section nesting cascades → content stops at manual line ~3050. That is the parked R9 mode-frame family — the exemplar's completion now BLOCKS on it (user-parked; not entered unilaterally). |
| 2026-08-31 (session 2) | 83928ef584 release | 2 | **1** | 0 | 1 | 36.9 | + real NiceArray family, `\automatichyphenmode`, version scan, texlua bridge (luacode executes). Residual: one `\noalign`. |
| 2026-08-31 (end of session 1) | 42dee14566 test | 2 | **8** | 0 | **3** | 31.7 | after the dual-TL dump fix + session fixes the 79k-warning template noise and the `&`-cascades are GONE. Residual: nicematrix STUB binding limits (`\Block`, `{bNiceArray}` "no support … stub binding"), LuaTeX-only bits (`\automatichyphenmode`, luacode), own-dtx `\myfileversion`. Next lever = complete the nicematrix binding (bindings outrank raw by policy). |
