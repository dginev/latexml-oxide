# Perfect Kernel — progress ledger (living)

Protocol and status legend: [README.md](README.md). Newest sweep first.
Per-document artifacts live under `~/data/perfect_kernel/<bundle>/<name>/`
(out of repo); the sweep tally file is `~/data/perfect_kernel/sweep_verdicts.tsv`.

## Oracle pass (same-TL ground truth)

2026-08-31, TL2025, `tools/perfect_kernel/oracle.sh` (pdflatex, lualatex
fallback/detection, 90s): **1548 / 2374 oracle-clean** (1248 pdflatex + 300
lualatex), 801 DOCUMENT-STALE (the shipped .tex no longer compiles on this
TL — e.g. a4wide.tex vs siunitx v3), 25 timeout. **The S1 bar applies to the
1548 oracle-clean docs only.**

## S2 schema-validation baseline (sweep-5 XMLs)

2026-08-31, `tools/perfect_kernel/validate.sh` (jing, urn-resolved schema):
**1,409 / 2,329 produced core XMLs are RelaxNG-valid (60.5%)**; 10,899
validation error lines across the remainder (`validate_verdicts.tsv`).

## Sweep history

| Date | Binary (commit, profile) | Corpus | Timeout | status 3/124/137 | status 2 | status 1 | status 0 | Notes |
|---|---|---|---|---|---|---|---|---|
| 2026-08-31 #1 | 1ef264a2bb release | 2374 | 120s | 40/5/0 | 1507 | 363 | 459 | **INVALIDATED** — run against the stale dual-TL dump (TL2023-vintage latex.ltx/expl3 labeled "2025"); kept for the before/after delta only |
| 2026-08-31 #2 | 9e347a3f15+161 release | 2374 | 120s | 34/10/0 | 1451 | 399 | 480 | fresh TL2025-pinned dump + OXIDIZED_DESIGN #161 DefPlain fix. Total error mass 145,375. **Oracle-clean slice (1548 docs): 424 clean + 310 warn-only (47%), 770 status-2, 8 fatal, 1 timeout; 58k error mass.** |
| 2026-08-31 #3 | +163/164/silence release | 2374 | 120s | 31/12/0 | 1449 | 400 | 482 | #163 makeindex, #164 raw-opt record, silence `\sl@StoreMessage` |
| 2026-08-31 #4 | 7ce3bb8005 release | 2374 | 120s | 28/16/0 | 1402 | 443 | 485 | + #165 `\@currsize`, beamer-hyperref parity, `\Hy@MakeCurrentHref`, KOMA minisec/labeling, memoir geometry, l3backend at begin-document. **Error-free 928 (39.1%), up from 822 baseline.** (Binary predates ltxdockit binding + `\@raw@classoptionslist`.) |
| 2026-08-31 #6 | b4aabe1e7e-era release | 2374 | 120s | 39/4/0 | 1362 | 471 | 498 | + \BreakableUnderscore, \glossary schema guard. **Error-free 969 (40.8%).** |
| 2026-08-31 #9 | 2486952cd1 release | 2374 | 120s | 26/29/0 | 1366 | 455 | 498 | **MIS-GATED luatex profile** — the harness matched every pdflatex-FALLBACK oracle row, profiling ~700 pdfLaTeX-authored docs as LuaTeX: +78k error mass. Kept as the lesson; gate fixed same day (clean-lualatex rows only). Timeouts also contention-inflated. |
| 2026-08-31 #10 | 00d89f0803 release | 2374 | 120s | 39/5/0 | 1353 | 476 | 501 | luatex profile correctly gated (264 clean-lua docs). **Error-free 977 (41.2%), error-mass 154,630.** METRIC NUANCE: the clean-lua slice reads *worse* on shallow error counts than with no profile (dead branches error less than live ones) while being strictly better on CONTENT — the LuaTeX branches now run and expose their real remaining surface (50k mass in that slice = the next worklist). |
| 2026-08-31 #11 | 5c390d3e5c **debug** | 2374 | 120s | 23/29/0 | 1347 | 474 | 501 | **Lua rebind-as-we-emulate banked** ([LUA_REBINDING.md](LUA_REBINDING.md)): live register mirror, babel Lua API up (locale_props class extinct), kpse require, `\lx@directlua` fidelity. Error-free 975 (41.1%), error-mass 154,615 (flat — the Lua wins are content-side, as predicted by the sweep-10 nuance); **fatals 39→23**. Oracle-clean slice: 446+373 = **819 error-free of 1,548 (52.9%)**, 71.9k mass. Timeouts 5→29 is a measurement artifact: full re-run (no cached verdicts) on the DEBUG binary under 12-way contention; spot-checks solo: tikz-cd-doc 25 s, spreadtab-en 63 s — both complete. Compare future sweeps at equal cache state. |
| 2026-08-31 #12 | 02ae925c2f debug | 2374 | 120s | 30/16/0 | 1306 | 523 | 499 | Banks batches 2-3 (14 fixes + surpasses #169-171). **Error-free 1,022 (43.1%), error-mass 136,228 (−18.4k)**; oracle-clean slice 444+412 = **856/1,548 (55.3%)**. Timeouts halved vs #11 (16); fatals 23→30 — includes pgf-spectra-class docs whose unlocked real work now exceeds the debug budget. |
| 2026-08-31 #13 | e7d056843d(batch4)+batch5 debug | 2374 | 120s | −/72/0 | 1298 | 489 | 515 | Banks batches 4-5. **Error-mass 115,478 (−20,750, −15.2%)**; zero-error 1,004 nominal but 72 timeouts (vs 16) are contention-inflated (agents+gates+builds ran concurrently) — 0 of the 72 were previously clean. 41 newly clean, 22 "regressions" of which the fontspec-family (source{code,sans}pro, hvfloat ×6, libertinus chain) are **oracle-faithful corrections**: the engine previously mis-took the `\ifxetex` TRUE branch inside these .sty files (old logs show xunicode+fontspec loading under pdfTeX) — real pdflatex also lacks `\setmonofont`/`\addfontfeature` there; docs move to the D6 lua/xe-profile slice. HARNESS NOTE: sweep ran with cwd=texmf (read-only) so logs failed to write (`Permission denied`) and first-pass verdicts counted STALE sweep-12 logs; rebuilt from `.stdout` "Conversion complete/failed" summaries (`sweep13_verdicts.tsv`). Future sweeps: pass `--log` into the writable outdir or cd to a writable dir with TEXINPUTS pointing at the source dir. |
| 2026-08-31 #8 | 9b3da268fc release | 2374 | 120s | 39/6/0 | 1346 | 484 | 499 | + showexpl LTXexample (code+result), piton binding (fatal removed — nicematrix manual runs FULL depth), nicematrix rule-opts strip + decorations. **Error-free 983 (41.4%).** |
| 2026-08-31 #7 | 83928ef584 release | 2374 | 120s | 40/3/0 | 1354 | 479 | 498 | + #166 filelist catcodes (hep-* cluster cleared), real pdfTeX utility surface (strcmp/md5/escapes), enumitem `\setlist*`, beamer `\subtitle`+OptionalAngled fix, book `\if@mainmatter`, nicematrix Array family, texlua bridge + luacode. **Error-free 977 (41.2%); S2 schema-valid 1435/2331 (61.6%).** Fatal/timeout counts remain contention-noisy (suite/S2 ran concurrently). |
| 2026-08-31 #5 | b2ff296e4b release | 2374 | 120s | 38/7/0 | 1382 | 456 | 491 | + ltxdockit_cls.rs, `\@raw@classoptionslist`. **Error-free 947 (39.9%).** Fatal count noisy across sweeps (a concurrent test-suite run contended for CPU/RAM); timeouts likewise. Binary predates `\BreakableUnderscore` + `\glossary` schema guard (sweep 6 material). |

## Fix log

One row per landed kernel/engine fix attributable to this mission. Guard test
names are the durable part.

| Date | Fix | Cluster addressed | Guard test |
|---|---|---|---|
| 2026-08-31 | `tools/make_formats.sh` pins TEXMF* to the ambient kpsewhich's TL root (dual-TL schism poisoned the dump: "2025" dump carried expl3 2024-01-22) | expl3 props 124 docs, tagging sockets 74, l3sys 66+28, `\prop_new_linked` 36+14, `\IfPackageLoaded*`/`\IfFormatAtLeast*` — representatives collapsed 124→2, 74→1, 66→7, 36→6 | zero-`Error:` `--init=latex.ltx` gate; re-sweep delta |
| 2026-08-31 | OXIDIZED_DESIGN #161: `DefPlain` skips blanks before its required `{` (surpass-Perl, user-approved, branch-contained) | `Expected opening '{'` — `\lstnewenvironment` bodies on following lines (~148 docs incl. ltxdockit/cnltx families) | `cluster_package_guards::defplain_skips_blanks_before_brace` |
| 2026-08-31 | OXIDIZED_DESIGN #162: listings raw-line capture keeps the first body line when an argument probe crossed the newline (`gullet::pushback_is_empty`) | First body line silently lost in every `\lstnewenvironment[1][]`-style example env (content-loss, shared with Perl) | same guard (data-attr assertion) |
| 2026-08-31 | OXIDIZED_DESIGN #163: `\makeindex` allocates `\@indexfile` (kernel-contract subset) | 14 bundles (l3kernel's own manuals, robustindex, postnotes) — saveenv 2→1 err (Perl fatals) | `makeindex_allocates_indexfile` |
| 2026-08-31 | OXIDIZED_DESIGN #164: loader records `\@raw@opt@<name>.<ext>` + `\@raw@classoptionslist` (kernel L18521/L18718) | every `\ProcessKeyOptions` package dropped its options (10 bundles, codedescribe…); babel global language options loaded nil.ldf (4+ French bundles) — both fixed, `\og`→« » works | `process_key_options_sees_load_options`, `raw_classoptionslist_recorded` |
| 2026-08-31 | OXIDIZED_DESIGN #165: `\@currsize` defaults to `\normalsize` | linguistics family (5 bundles) — linguex-doc 6 err → **0 err 0 warn** | `currsize_default` |
| 2026-08-31 | beamer requires hyperref (Perl L1311 parity) + `\Hy@MakeCurrentHref` internal | beamertheme-* `\url` mass; l3doc anchor internal (8 bundles) | beamer golden (84_slides) |
| 2026-08-31 | l3backend loaded at begin-document (`\@expl@sys@load@backend@@`, latex.ltx L9472) | `\__color_backend_*` families undefined in every dump-mode conversion | prettytok witness; suite |
| 2026-08-31 | KOMA `\minisec`→`\paragraph*`, `{labeling}`→`{description}` (semantic); memoir geometry stubs (justified) | 17+11+4 bundles | scr*/memoir bindings |
| 2026-08-31 | silence.sty binding: `\sl@StoreMessage` internal | hep-* doc family (5+ bundles) | hep-acronym witness |
| 2026-08-31 | **ltxdockit_cls.rs** — NEW binding (user-directed lock-conflict resolution): semantic titlepage keyvals → frontmatter | 12-bundle biblatex/etoolbox manual family — abraces-doc 10 err → **0 err** | corpus witnesses |
| 2026-08-31 | rawclasses protocol guards (no code change — verified existing behavior) | binding precedence + bindingless-class raw load without OmniBus | `cluster_package_guards::rawclasses_binding_precedence_and_no_omnibus` (3 tests) |
| 2026-08-31 | **Lua rebind-as-we-emulate** ([`LUA_REBINDING.md`](LUA_REBINDING.md)): live `tex.count`/`tex.dimen` mirror (`Q`/`A` protocol → Rust State), absorb shims (`pdf.set*`, `tex.primitives()`), kpse-backed `require` + lualibs, SOURCEDIRECTORY cwd | chunks branching on register stub-zeros; `require` of texmf Lua modules (newpax, tkz-elements); `pdf.*` runtime errors (derivative); `tex.primitives` (abntexto ×2) | `lua_state_mirror::directlua_reads_and_writes_live_registers`, `lua_bridge::tests::rebound_engine_intents_absorb_and_resolve` |
| 2026-08-31 | `\lx@directlua` fidelity (real-luatex probes as oracle): drop the double `Expand!` (XGeneralText already partial-expands, honoring `\noexpand` — the re-expansion ate babel's `[[\noexpand\csname bbl@error\endcsname{]]` long string), filter `\par` tokens (blank lines contribute NOTHING in real luatex), diagnostic now includes the chunk head | systemic: EVERY profiled clean-lualatex doc's babel Lua layer died (`attempt to index a nil value (field 'locale_props')`) | `luatex_babel_api::babel_lua_api_layer_initializes` |
| 2026-08-31 | graphicx `\Gin@boolkey` (graphicx.sty L137) + real `\newif` set (graphics.sty L55-579); KOMA classes require typearea, whose binding gains `\typearea`/`\recalctypearea` (typearea.sty L1596) | 34+9-doc Gin cluster (hvfloat/after1s1c 34→4 err, pagelayout/banner error-free); 26-doc recalctypearea cluster (bohr) | `graphicx_internals::gin_internal_names_defined` |
| 2026-08-31 | minted `\minted@outputdir` (minted.sty L273); beamer `\@@@affiliation` contact constructor; beamer requires ifpdf/keyval/graphicx (Perl beamer.cls.ltxml L30-32 parity) | 18-doc minted cluster (csvsimple 2 err left); 16-doc affiliation cluster (demo-cbernoulli 1 err); 9-doc `undefined:\includegraphics` (focus-demo) | beamer/minted guard tests + goldens (suite) |
| 2026-08-31 | **8-fix batch** (subagent-root-caused): pgfmath boolean/logic function forms return bare ints (live-pgf probe oracle); luaPST absorbers in luatex profile; scrlfile `\AfterPackage`(+!/+) binding; fontspec expl3 conditional layer + signature fixes; ifluatex consults LUATEX_PROFILE; unicode-math `\sym*`; `\suppressfontnotfounderror` | pgf-spectra 7k-err/doc; bardiag1+psgomanual error-free; cnltx 36-doc avalanche (bohr multicols/RaggedRight cleared); raleway/sourcecodepro/abnt-doc clean | pgfmath unit guards, `luatex_profile`, witnesses |
| 2026-08-31 | **Surpass batch** (user-approved 2026-08-31): OXIDIZED_DESIGN #169 `\ver@` fallback-only; #170 robust accent `\meaning`; #171 brace-hunt expands protected (tex.web scan_left_brace, pdflatex-probed) | toptesi 12-doc abort cluster → 2 residual errs; cahierprof 501→0, ipsum 103→0, colorblind 502→19, mirror-lens 502→38; ~16-doc xint `expected:{` cluster | `raw_provides_version_survives`, `accent_meaning_robust_shape`, `expanded_protected_brace_hunt` |
| 2026-08-31 | luatex profile: format parity `\bbl@luapatterns` no-op (lualatex format ships patterns → babel.def L1135 skips the patterns-only `\input luababel.def` whose loaded-flag suppressed the real API load at L2285); direction primitives eat their 3-letter keyword (were `{TLT}` macros leaking text); `\breakafterdirmode` register | same systemic babel class + `TLTTLT` text leak + luababel L1469 undefined | same guard + `luatex_profile` |
| 2026-08-31 | **batch 9**: biblatex style-author API absorbed (Declare{Delim,Sorting,Uniquename,Nosort,MultiCite}* gobblers, hooks, punctuation — apa.bbx loads CLEAN) + `\DeclareCiteCommand` keeps EXISTING commands (a raw .cbx restyle of `\parencite` was aliasing it to plain `\cite`, dropping parens — caught by suite guards `cluster_biblatex_*`); tcolorbox `documentation` library `{dispExample}`/`{dispExample*}`/`{dispListing}` showexpl-style (whole family LOCKED — raw lib re-wires the END macros back into `\tcbwritetemp` otherwise); listings line-number stepping via locked `\lx@lst@stepnumber` internal (real listings advances `\c@lstnumber` at internals level — user-level `\stepcounter` emission let assoccnt's raw wrapper expand inside `<ltx:listing>`); **assoccnt_sty.rs** NEW binding (raw pkg wraps ALL kernel counter commands through \setkeys bookkeeping) | assoccnt manual 39-truncated→385→**10**; apa-style docs; dispExample family (assoccnt/calculatoritems/commalists-tools clusters) | `cluster_biblatex_*` (69 green), dispExample repros |
| 2026-08-31 | **batch 8** (sweep-13 ranking): alphabeta `\math<greek>` alias block (alphabeta.sty L109-160 — `alphabeta` maps to textgreek_sty.rs, raw let-block never ran); KOMA `\ifpdfoutput{}{}`→#1 + `\KOMAScript` logo (scr* ×3); color raw-storage fallback — read xcolor's `\color@<name>` internal macro before "assuming Black" (ydoc-desc L22 defines `\color@none` = empty color; reimpl-internal-name-mismatch shape); listings `\lst@xleftmargin`/`@xright` defaults (raw styles read them, 8 bundles); datetime format selectors (`\mmddyyyydate` family, L80-150, 4 bundles); biblatex category surface (6 bundles) | hep-paper 3→**0**; l2tabuen 5→1; macros2e 'none' ×5 bundles cleared; assoccnt/bohr targeted errors cleared (residuals = verbatim-leak + cnltx/mdframed mode families) | suite + witnesses |
| 2026-08-31 | **batch 7**: document-body `\input{x.sty}` reads raw files as CONTENT at current catcodes (`notex` gate in content.rs — binding dispatch still wins; the definitions mouth's forced `@`=letter corrupted doc.sty's `\CharacterTable` on every `\DocInput` re-read; Perl "passes" only by skipping the re-read and dropping the doc body); biblatex `style=`/`bibstyle=`/`citestyle=` load the raw `.bbx`/`.cbx` chain (declaration gobblers first, once-guard, native styles skipped) + `\AtNextCitekey` runs code in place + `\mkbibquote`/parens/brackets | frankenstein ×10 → abbrevs/titles **0 err**; pkgloader; windycity toggles 55→2, biblatex-ext 10→1 (residual mass = separate mode/tag family) | `input_routing_and_bbx::*` (2 tests) |
| 2026-08-31 | **batch 6** (agent-root-caused): babel assigns `\language=\l@<main>` at load (babel.sty L1136/L828 — preamble `\iflanguage` mis-branched in EVERY non-English doc); `\@endpart` kernel contract (report.cls L318/book.cls L339, toptesi.sty L448 calls it); titleps_sty.rs NEW binding + titlesec `pagestyles`-option loads it (titlesec.sty L1392-1422); datetime `\monthname`/`\shortmonthname` real names (were content-losing noops); schooldocs_sty.rs NEW binding (`\subtitle` hidden in a discarded `\fancypagestyle` body — shared failure w/ Perl fancyhdr noop); etoolbox `\robustify` second native sentinel `Stored[` (DefMath `\bmod`; real latex.ltx L15556 is a robust macro so real \robustify succeeds — ours must noop not error; yquant-doc L244) | ufrgscca 6→**0**; toptesi topfront manuals −34; schooldocs `\subtitle` cleared; yquant robustify line | `kernel_language_and_part_contracts::babel_language_register_and_endpart` |
| 2026-08-31 | **batch 5 — xkeyval deep port**: front-end scaffolding verbatim (`\XKV@testopta/b/c/d`, `\KV@@sp@def` space-trick, `\@selective@sanitize`, makepf/makehd, ifcmd/g@tkeyname, addtolist, `\XKV@setkeys`) with `\XKV@s@tkeys` as thin shim onto the Rust `\setkeys` (now private `\lx@xkv@setkeys` so a package overriding `\setkeys` — xkeymask's dispatcher — can't loop); **pointer system for real** (`\savekeys` family verbatim + `\XKV@<header><key>@value` store in the Rust reader + `\usevalue` closure via XKV@ptr@* context); **KNOWN_PERL_ERRORS #80** `\define@cmdkey` stray `T_PARAM` (Perl KeyVal.pm L131, author's own `!?!??!`) — code now sees the bare value; **token defaults** (`default_tks`) — a `[\xskak@val@defaultid]` default survived as ONE token instead of being string-round-tripped through the standard cattable (`\xskak`+`@val@defaultid` split = the 1000-error csname cascade); **xkeymask_sty.rs** NEW binding (raw pkg relies on `\XKV@resa` surviving `\KV@@sp@def`'s futurelet — self-referential macro real TeX tolerates, our/Perl recursion guard flags; pdflatex-probed) | xkeymask 7→**0/0/0**; xskak 1001+fatal→**5**; chessboard 1001-truncated→1 err (renders to L1627/2049, then `\chessboard[boardfontencoding=LSBC4]` 400M-token churn — recorded residual); every `\define@cmdkey` user corpus-wide | `xkeyval_internals::setkeys_frontend_clone_reaches_rust_path`, `xkeyval_internals::pointer_system_cmdkey_and_token_defaults` |
| 2026-08-31 | **7-fix batch 4**: `\if@openright` kernel contract (classes.dtx: book TRUE/report FALSE, options before ProcessOptions); scrlfile aux-hook noops (`\BeforeClosingMainAux[]{}`); scrlayer-scrpage page-style family; memoir → `LoadClass!("book")` + REAL output streams (newwrite/openout round-trip, `\writeverbatim` via `Until:\endwriteverbatim`+`\detokenize`) + ~30 geometry noops + setsecnumdepth table; beamer `\titlepage`/`\maketitle`→`\lx@frontmatter@fallback`, `\sectionpage` noops; mouth `^^^^hhhh`/`^^^^^^hhhhhh` caret forms (longest-match-first, \Ucharcat precedent); textgreek alphabeta letters + greek-fontenc combining accents | memoir 20+-doc family (dlfltxbmarkup stream round-trip); scr* aux cluster; beamerauxtheme; eigo/Celestia error-free; greek residual 91→46; ucharcat docs | `openright_kernel_contract`, `memoir_output_streams`, `unicode_caret_notation`, `accent_meaning_robust_shape` |

## Named exemplar: nicematrix (the mission's reference bundle)

`nicematrix.tex` (6,954 lines, LuaLaTeX-authored manual; tikz, siunitx,
tcolorbox, enumitem, fancyvrb, titlesec, varwidth, adjustbox…).

| Date | Binary | status | errors | fatals | warnings | secs | Note |
|---|---|---|---|---|---|---|---|
| 2026-08-31 | 1ef264a2bb test | 2 | 102 (capped) | 0 | 79,166 | 8.5 | first baseline; dominant noise: `Unrecognized tabular template` from `{NiceTabular}`-family preambles hitting the alignment template reader (`alignment.rs:997`); error heads: `Extra alignment tab '&'` ×26, `\noalign cannot be used here` ×11, nested-sectioning schema errors |
| 2026-08-31 (session 3) | 9b3da268fc release | 124→n/a | 108 | 2 | — | 9.6 | piton binding removed the 'LuaLaTeX mandatory' FATAL that had been TRUNCATING the manual — the full 8,334-line doc now runs (9.6s), all Code examples execute, exposing the real remaining surface: 108 errors, S3 word-recall 56.6%. Structural wall: an itemize whose items hold live tables breaks list mode (`\end{itemize} Attempt to end mode internal_vertical`) → section nesting cascades → content stops at manual line ~3050. That is the parked R9 mode-frame family — the exemplar's completion now BLOCKS on it (user-parked; not entered unilaterally). |
| 2026-08-31 (session 2) | 83928ef584 release | 2 | **1** | 0 | 1 | 36.9 | + real NiceArray family, `\automatichyphenmode`, version scan, texlua bridge (luacode executes). Residual: one `\noalign`. |
| 2026-08-31 (end of session 1) | 42dee14566 test | 2 | **8** | 0 | **3** | 31.7 | after the dual-TL dump fix + session fixes the 79k-warning template noise and the `&`-cascades are GONE. Residual: nicematrix STUB binding limits (`\Block`, `{bNiceArray}` "no support … stub binding"), LuaTeX-only bits (`\automatichyphenmode`, luacode), own-dtx `\myfileversion`. Next lever = complete the nicematrix binding (bindings outrank raw by policy). |
