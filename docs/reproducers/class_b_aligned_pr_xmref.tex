% Class B (expected:id dangling content-XMRef) MINIMAL repro — see
% docs/parity/diagnostics/EXPECTED_ID_XMREF_DESIGN_2026-06-08.md (2026-06-26m).
% Rust: 2 dangling content XMRefs (A1.E1.m1.1a/1b) — the \Pr argument's
%   content-arm refs strand. With a plain \section (S-scheme) instead of
%   \appendix the SAME refs are silently DROPPED by the `^S\d+` prune sweep
%   (Document::prune_dangling_split_xmrefs) => content loss (the \Pr arg
%   vanishes from content MathML). Appendix `A`-scheme dodges the regex => dangles.
% Perl: content refs A1.E1.m1.1 AND A1.E1.m1.1.mf BOTH resolve (correct
%   parallel markup; the .mf is amsmath.sty.ltxml L664's ID_SUFFIX clone).
% Trigger = `aligned` (>=2 rows) + \Pr(a,b|c) conditional arg; a SIMPLE
%   (non-aligned) equation resolves fine. Converts in <1s — use to iterate the fix.
\documentclass{article}
\usepackage{amsmath}
\usepackage{physics}
\begin{document}
\appendix\section{a}
\begin{equation}\begin{aligned}
\rho &= \Pr(s_A,s_B | \Omega) \\
\sigma &= x
\end{aligned}\end{equation}
\end{document}
