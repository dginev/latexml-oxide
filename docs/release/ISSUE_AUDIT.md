# GitHub Issue Audit

> **Why this file exists.** GitHub issue access requires a working `gh`
> login plus (in the sandbox) network escalation, so offline agents
> routinely miss issue-tracker context. Worse, issue numbers collide with
> unrelated local numbering — e.g. the `#47` in [`WISDOM.md`](../parity/WISDOM.md) is
> **not** GitHub issue #47. This file mirrors the open issues with a local
> interpretation so planning does not depend on live tracker access.
>
> **Refresh** before milestone planning:
> `gh issue list --state open --limit 100 --json number,title,labels,createdAt`.
> Last refreshed: **2026-07-26** — **10 open** (386 reopened after an accidental keyword close; see its row). Trust the heading count against the
> row count, never a running narrative; that arithmetic has drifted twice.
> Everything closed since is in Recently closed, with its outcome. This file
> drifts fast — re-run the one-liner rather than incrementally editing rows.

Tracker: <https://github.com/dginev/latexml-oxide/issues>

## Open issues (10)

| # | Title | Labels | Local status / interpretation |
|---|---|---|---|
| **47** | [Feature] Accurate latex linting | enhancement | **Prioritized beyond-Perl showcase.** Live source ↔ preview over a shared locator substrate, two clients: the **ar5iv-editor** (CodeMirror web UI) and a **VSCode extension** (webview). Accurate linting falls out of the same substrate. Design: [`SOURCE_PROVENANCE.md`](../performance/SOURCE_PROVENANCE.md). *Not* purely post-1.0 — Tier A is near-term and parity-neutral. |
| **92** | Superior debugging and error-reporting for document authors | enhancement | Same source-provenance substrate as #47 ([`SOURCE_PROVENANCE.md`](../performance/SOURCE_PROVENANCE.md)): construct-start + macro-origin locators give Rust-compiler-grade author errors, fixing TeX's "error points at the end of the environment". |
| **143** | Switch to rust stable, when `#[thread_local]` is stabilized | enhancement, performance | Toolchain-longevity risk for a public-domain tool. Pin a known-good nightly; track stabilization. [`RELEASE_CRITERIA.md`](RELEASE_CRITERIA.md) §3. |
| **94** | Document model: RelaxNG vs Rust data-type trade-offs | enhancement, question, documentation | Doc debt; relates to the (closed) #199 HTML-dialect schema and [`SCHEMA_DOCUMENTATION.md`](../performance/SCHEMA_DOCUMENTATION.md). |
| **297** | latexml_oxide 0.7.4 binding transition: `nowrap.sty.ltxml` | documentation, packages | Filed 2026-07-18. A user-supplied `.ltxml` binding on `--path` is ignored: `Warning:missing_file:nowrap … No dispatcher entry and no raw file found on disk` (`latexml_core/src/binding/content.rs`). Perl finds it silently. This is the **user-supplied `.ltxml` discovery** path — `.ltxml` is Perl, so the Rust answer is either the Rhai `runtime-bindings` front-end or a clear diagnostic; today it is neither. |
| **303** | Precompiled kernel (dump) release and update strategy | enhancement | Filed 2026-07-18, follow-up to #299/PR #300. The dump is keyed to the TL **year**, but the LaTeX format is not frozen within a year (`\fmtversion`, `tlmgr` `fmttriggers` rebuilds) — a false positive was traded for a rarer false negative. Candidate signals: key on `\fmtversion` + L3 date, or compare against the ambient `latex.fmt` mtime; warn-not-fail. |
| **80** | space XMhints as elided arguments | enhancement | **Open — still reproduces (verified 2026-06-16).** `$[D_{0},\ ]$` → the escaped space is dropped, so the grammar sees a dangling `,]` and rejects. Fix = emit an XMHint for the in-math space and teach the marpa grammar to treat it as an elided argument slot. Real grammar work, not a quick win. Backlog. |
| **354** | `\verb` inside `\index{...}` yields an empty `<verbatim/>` and mis-tokenized text | — | Filed 2026-07-25 (reporter nasser1), split out of #347 and reproducing with **no `\cprotect` anywhere**. **SHARED with Perl** — same-host 0.8.8 emits the byte-identical `Error:expected:delimiter Verbatim argument lost`; pdflatex is the ground truth and passes the phrase through literally (`\indexentry{\verb|\delta|}{1}`). Parity, so not a Rust-only bug; a fix would be a surpass-Perl item (`\index` must read its argument without letting `\verb` see pre-tokenized text). |
| **361** | 0.7.5 rc2: fatal errors compiling (232K-line / 7.9 MB book, `--splitat=subsection`) | — | Filed 2026-07-24. **Not a conversion bug** — the document converts error-free and beats same-host Perl; the reported fatals were the fleet-tuned default resource guards. Follow-up performance work **landed**: M1+M2+M4 took peak RSS **9.05 → 5.99 GB** (−34 %) at unchanged wall time, and `--max-memory` became the single knob (#363/#368). M3 (stream boxes→DOM) is a **settled dead-end**. Profile + box-type census: [`ISSUE_361_MEMORY_TIME_PROFILE_2026-07-24.md`](../performance/ISSUE_361_MEMORY_TIME_PROFILE_2026-07-24.md). |
| **386** | Refactor: build XML through libxml, not string concatenation | — | Filed 2026-07-26 by the owner, out of the #383 review. Four hand-rolled XML escapers exist, one (`latexml_core/src/document.rs::serialize_string`) silently missing `"`; `make_bibliography.rs` builds markup by string concatenation. **Read the issue's "Out of scope" section first** — a naive `grep '"<'` returns ~750 hits and ~700 are `DefConstructor!` templates, a declarative DSL mirroring Perl's binding syntax, NOT this problem. **Scope narrowed 2026-07-26:** the `make_bibliography.rs` portion is subsumed by the R5 re-port and must not be attacked separately — that file returns a *serialized XML string* only because it tokenizes fields EAGERLY, so routing them through the pool path makes the output DOM-native as a side effect (one-way dependency; see [`BIBLIOGRAPHY_WORKLIST.md`](../parity/BIBLIOGRAPHY_WORKLIST.md)). The other three sites stay in scope. **Item 3 DONE 2026-07-26** with the R5 re-port: the `.bib` string builder, its `xml_escape` copy and the serialize/reparse round-trip are deleted (-727 lines) — the bib document now crosses as a node tree (`PostDocument::new(XmlDoc)`). Remaining: item 1 (unify the three surviving escapers), item 2 (EPUB OPF/NCX — the one demonstrated correctness gap, though the issue flags its premise as untraced), item 5 (schema_docs), and item 4, which the issue itself asks to scope separately. **Two of the issue's premises did not survive checking (2026-07-26).** (a) `document.rs::serialize_string` is **not** a defective fourth escaper: it is the TEXT-node half of a correct pair, and `serialize_attr` (document.rs:5112, used at :1567) layers `"`/`\n`/`\t` on top for attribute values — escaping `"` in text content would be wrong XML, so it must NOT be folded into the shared helper. Item 1 therefore reduces to the two genuinely byte-identical copies, which also subsumes item 5. (b) Item 2's untraced premise is HALF true and moot: a `&` really does reach a split filename (`--splitnaming=label` with `\label{Fisher&Yates}` produces a file named `Fisher&Yates.epub`), but `generate_opf` has **zero callers** — the whole `manifest/epub` module is unreferenced and `--format=epub` emits XHTML via the epub3 XSLT with no OPF/zip packaging. The demonstrated correctness gap was unreachable dead code — but it is fixed anyway (the OPF is now built through the libxml DOM, so escaping is no longer a question for any of the 12 interpolations), because it costs little and the module has no tests to protect it once packaging IS wired. **Items 1, 2, 3 and 5 are DONE**; item 4 is split out to its own issue 398, exactly as the issue asks. Wiring EPUB packaging up remains a feature, not part of this refactor. *Was auto-closed 2026-07-26 by a `resolves #386` phrase in PR #392's doc text and reopened — keep closing keywords out of prose that merely cross-references an issue.* |

## Recently closed (since the 2026-05-24 refresh — outcomes)

| # | Closed | Outcome |
|---|---|---|
| **ar5iv#607** | 2026-08-05 (PR) | rc5: an equation's `\autoref` tag rendered `() 1.16` instead of `(1.16)` (reporter Jess Riedel via dginev; arXiv **2607.12124**). Triggered by the documented hyperref idiom `\def\equationautorefname~#1\null{(#1)\null}`. **SHARED-FAILURE** — same-host Perl LaTeXML emits the byte-identical `() 1.1` plus `Missing argument Match:~ / Until:\null` errors; the user sanctioned surpassing it. Root cause: LaTeXML's `\lx@autorefnum@@` fed `\<type>autorefname \nobreakspace \the<counter>` — a control-sequence separator and no trailing `\null` — so the delimited-arg idiom's active-`~` delimiter never matched and its `\null` terminator was never found. Fix: mirror the real hyperref kernel (`hyperref.sty` L8211-8268) — feed the **active `~`** (catcode 13) as the separator and append `\null` (`hyperref_sty.rs` `\lx@autorefnum@@`). Default path is byte-identical (active `~`→`\lx@NBSP`, same nbsp; `\null`=`\hbox{}` digests to nothing). Surpass-Perl divergence [OXIDIZED_DESIGN #98](../parity/OXIDIZED_DESIGN_DIVERGENCES.md). Guards: `50_structure::autoref_delimited_test` (idiom → `(1.1)`/`(1.2)`, sibling `section` autoref stays `section 1`), `50_structure::autoref_test` (default path unchanged). |
| **508** | 2026-08-05 (PR) | rc5: a Rhai `DefEnvironment("{nowrap}", "<ltx:block class='nowrap'>#body</ltx:block>")` failed with `Error:malformed:ltx:block Attempt to close </ltx:block>, which isn't open` whenever the body held a blank line (a `\par`) (reporter nasser1). **GENUINE-RUST-ONLY** — same-host Perl (equivalent `.ltxml` binding) converts clean, `<para><block><p>A</p><p>B</p></block></para>`. Root cause: a Rust-only `Tag!("ltx:block", auto_close => true)` (`latex_constructs.rs`); Perl has no such Tag. So `\par`'s `maybe_close_element("ltx:para")` climbed *through* the author's block (block was auto-closeable) and over-closed it, splitting the body and leaving the constructor's `</ltx:block>` with nothing open. Fix: remove the blanket Tag — `insert_block`'s fallback rename (`base_utilities.rs:3220`) is the faithful port of Perl `insertBlock` (TeX_Box.pool L516), which produces the same plain `<ltx:block>` with no auto-close, so the Tag was an unfaithful band-aid, not parity. Follow-up to #297 (same `nowrap` package, now on the `.sty.rhai` runtime-bindings path). Guard: `51_structure_rhai::nowrap_test`. **Known side-effect:** the band-aid was masking the deferred `\vskip` vertical-break gap ([WISDOM #38](../parity/WISDOM.md)) for one IEEEtran float (arXiv 2302.11635, `\hrulefill\vspace*{4pt}`), which now surfaces 4 `malformed:` errors — the same class Perl emits on the reduced `\vspace`-less construct; the clean-figure path Perl takes needs the separately-tracked `\vskip` fix. |
| **500** | 2026-08-04 (PR) | rc5: `\DefineVerbatimEnvironment` reports `Error:undefined:\KV@do` when `standalone` is loaded before `fancyvrb` (reporter nasser1). **GENUINE-RUST-ONLY as reported, shared defect at the root.** `\KV@do` lives only in raw `keyval.sty` (L31) — no binding in either engine defines it; both read it via `InputDefinitions('keyval', noltxml => 1)`. The xkeyval binding only *pretended* keyval was loaded (Perl `xkeyval.sty.ltxml` L23 `AssignValue('keyval.sty_loaded' => 1)`), and that flag is what BOTH load paths gate on (`Package.pm:loadLTXML` L2328-2330, `loadTeXDefinitions` L2363) — so the raw file was never read and raw `fancyvrb.sty` L112-117 `\FV@UseKeyValues` expanded a `\KV@do` that did not exist. Rust reached it on a plain preamble because `standalone_sty.rs` carries real `standalone.sty` L107's `\RequirePackage{xkeyval}`, which `standalone.sty.ltxml` omits; with an explicit `\usepackage{xkeyval}` **Perl 0.8.8 fails identically** (KNOWN_PERL_ERRORS #73). Fixed at the root, faithful to real `xkeyval.sty` L39 (`\input xkeyval` → the bundle's `keyval.tex`, `\KV@do` at L52): `xkeyval_sty.rs` now really `RequirePackage`s keyval first, then overrides with its extended `\setkeys`/`\define@key` — so xkeyval still wins and a later `\RequirePackage{keyval}` stays a no-op. Surpass-Perl divergence [OXIDIZED_DESIGN #95](../parity/OXIDIZED_DESIGN_DIVERGENCES.md). Guard: `06_cluster_standalone_subfiles::keyval_internals_survive_xkeyval_preloading_it` (issue MWE, explicit-xkeyval variant with valued options, and the reporter's working order). |
| **498** | 2026-08-04 (PR) | rc5: `\includegraphics[width=…]` ignored for SVG sources — the `<object>` lacked `width`/`height` and the aspect class, so images rendered at intrinsic size (reporter nasser1). **GENUINE-RUST-ONLY vs *correct* Perl; same-host Perl is a FALSE oracle here** (WISDOM #62 shape): without Image::Magick it warns `imageprocessing:imagesize` and omits sizes too, and its Image::Size chokes on an xml-prolog SVG — the reporter's host has Image::Magick and Perl there writes `width/height + ltx_img_portrait`. Core XML was already identical (`options="width=137.9979pt,keepaspectratio=true"`); the defect was post-only: `graphics.rs` Phase-3 `Plan::Copy` (the path every web-native SVG takes) sized via the raster-only `imagesize` crate → `None` → `set_graphic_src` wrote nothing. Fix: `read_source_dimensions` — the port of Perl `Util/Image.pm:image_size` L86-97's type dispatch — routes `.svg` through the existing `read_svg_dimensions` (viewBox, then width/height attrs), plus Perl's `expected:image` warn (Graphics.pm L310-312) when sizing fails. Transform math untouched (accepted port, pre-existing ±1px ceil-order nuance vs Perl's pt→bp detour). Guard: `integration::test_svg_source_sized_and_aspect_classed` (both extraction shapes, same `width=` ⇒ same 191px display width, portrait class). **Three defects surfaced by promoting `read_svg_dimensions` from our own generated SVGs to arbitrary user SVGs, all fixed in the same PR.** (1) It measured wrong: double-quoted attributes only, `stroke-width="2"` matched as `width`, units truncated (`10cm` → 10 px), `6.34e2` → 6 — so a viewBox-less SVG with no graphicx options rendered as a 10-pixel thumbnail, *worse* than the pre-fix behaviour of writing no attributes and letting the browser use the intrinsic size. Now one shared reader in `latexml_core::util::image` (`svg_root_tag` / `svg_attr_value` / `svg_len_px` / `read_svg_viewport_px`), as Perl shares one `Util::Image` between `Core` and `Post`; SVG `pt` is now a big point (1/72"), not a TeX pt. **viewBox keeps precedence over the root lengths** in the viewport reader — `pdftocairo -svg` emits `width="612pt" … viewBox="0 0 612 792"`, so preferring lengths would rescale every PDF-derived figure by 96/72 (guard: `viewport_px_is_stable_for_our_own_converter_output`). (2) A relative length (`%`) now yields `None` rather than a bogus pixel count. (3) Perl `Post/Graphics.pm` L264-271's non-raster transform guard was unported — `TypeProperties.raster` (`svg` → `Some(false)`) was write-only dead data, so `angle=` on an SVG handed `convert` a vector source and a `.svg` destination, replacing the drawing in the bundle with a raster under the same name; now warns `limitation` and drops the rotation, scaling unaffected. Guards: `svg_geometry_tests::*` (9, in `latexml_core`), `graphics::tests::rotation_is_dropped_for_non_raster_sources_but_kept_for_raster`, `integration::test_unit_bearing_svg_without_viewbox_is_converted_not_truncated`. |
| **431** | 2026-08-02 (PR) | rc3: fancyvrb `Verbatim` renders as reflowing text (reporter nasser1). **Two layers.** (1) GENUINE-RUST-ONLY: `fancyvrb_sty.rs` ported only the raw-load and dropped Perl `fancyvrb.sty.ltxml` L22-25's `\FancyVerbFormatLine` hack that adds the per-line `ltx_verbatim` class — now ported faithfully (fvextra/minted inherit it via their `\RequirePackage{fancyvrb}`). Golden `tokenize/fancyvrb.{tex,xml}`; Rust serializes the class merged onto the one line box where Perl nests two wrappers (noted in divergence #93). (2) PERL-ORIGIN: vanilla CSS `.ltx_verbatim{white-space:nowrap}` beats the UA `pre` rule — a plain `{verbatim}` `<pre>` collapses to ONE 15px line, fancyvrb lines lose indentation/spacing and flow two-up at wide windows (all measured, Perl's own artifacts included; KNOWN_PERL_ERRORS #71). Resolved by CSS local delta [OXIDIZED_DESIGN #93](../parity/OXIDIZED_DESIGN_DIVERGENCES.md): `white-space:pre` + `display:block` per fancyvrb line. Measured after: one line per row, 4-space indent kept, `<pre>` 4 lines. Guards: `fancyvrb_test` (00_tokenize), `witnessed_css_delta::verbatim_whitespace_delta_stays_present`. Witnesses 2605.01024/2605.00468 re-converted: identical outcomes pre/post (4 pre-existing alignment-tab errors / 0 errors; 0 fatals both). |
| **473** | 2026-08-01 (PR) | rc4: missing vertical space between two adjacent display equations (reporter nasser1). **PARITY with Perl, resolved as a surpass-Perl CSS delta** — same-host Perl 0.8.8 emits the same body markup (identical elements/classes, modulo whitespace and the sanctioned #18 invisible-operator) and the same vanilla CSS (which has no display-container margin; upstream master unchanged), so both render the MWE's two `\[…\]` touching, where pdflatex inserts `\abovedisplayskip`/`\belowdisplayskip`. Fix: bundled-CSS local delta `.ltx_eqn_table, .ltx_eqn_div { margin-top:1em; margin-bottom:1em; }` — `1em` chosen empirically (headless-Chrome gap measurement: text→eqn/eqn→eqn/eqn→text 16/0/16 px → uniform 16/16/16 px; the margin collapses with UA `<p>` margins through `ltx_para`, so text↔display does not double; ar5iv's 0.65rem gives a non-uniform 10.4 px). Divergence [OXIDIZED_DESIGN #92](../parity/OXIDIZED_DESIGN_DIVERGENCES.md), Perl-origin record KNOWN_PERL_ERRORS #70. Guard: `witnessed_css_delta::equation_display_margin_delta_stays_present`. |
| **347** | 2026-07-24 | how to use `\cprotect` in latexml? — Filed 2026-07-23 (reporter nasser1). **Binding coverage, not a defect — addressed 2026-07-23 (`feat-347-cprotect`).** Triaged SHARED-FAILURE: same-host Perl 0.8.8 has no `cprotect.sty.ltxml` either and emits the identical `missing_file:cprotect` + `undefined:\cprotect` + `undefined:\tensor`. New `latexml_contrib/src/cprotect_sty.rs`. cprotect exists only to defeat a TeX tokenization order problem (a braced argument is tokenized before `\verb` can set catcodes); it reads the argument under `\makeallother`, **writes it to `\jobname-N.cpt`** and substitutes `\protect\input <file>`, the re-`\input` being what re-tokenizes it. The port keeps the reading half (catcodes 12 for `\ % # $ & ^ _ ~`, space, and `\r` — braces stay 1/2 so `read_balanced` does cprotect's manual brace count) and replaces the file half with eTeX `\scantokens`, which opens a `Mouth` over the string: `\cprotect\section{<raw>}` → `\section{\scantokens{<raw>%}}` (the trailing `%` eats the re-read's end-of-line, cprotect's `^^E^^L` sentinel trick). `\cMakeRobust` and `\cprotEnv` ported too; `\ReadVerbatimUntil` and the `gobbling-*` options are not (they only tune the file round-trip). One judgement call: `\cMakeRobust\section` runs under `local_state_unlocked_guard(true)`, since sectioning commands are `locked => true` — this is a wrap that preserves the old meaning under `\CPT@old@…`, the same "addition, never replacement" case Perl unlocks for in `Package.pm` L2527. Guard: `00_contrib::cprotect_test` (7 variations). That suite also had to move from the runtime `latexml_tests_internal` glob to `tex_tests!` — its single-#[test]-for-all-pairs shape SIGABRTs on a *second* conversion in the same thread. |
| **350** | 2026-07-23 | export an XML parser to Rhai bindings (?) — Filed 2026-07-23 by the BookML author. **Addressed 2026-07-23** (`feat-350-rhai-xml-parser`): `document.insertXML(markup)` parses a snippet and splices the parsed subtree at the current point — the runtime half of `\bmlRawHTML` → `<ltx:rawhtml>` — over the existing `Document::append_tree`. Two latent defects it exposed were fixed with it: namespaces now resolve by **URI** through the registered prefix map (an `xmlns=`-declared xhtml snippet was being relabelled `ltx:`, which the XSLT `copy-foreign` would have dropped), and wildcard schema entries (`xhtml:*`) now govern concrete tags, so attributes survive. See [`script_bindings_plan.md`](../parity/script_bindings_plan.md). |
| **356** | 2026-07-24 (PR) | **Bug (Rust-only) — math in a section title rendered as flattened token text in the table of contents** (reporter nasser1); same-host Perl 0.8.8 keeps the `<math>`. The ObjectDB stored only the title's flattened *text*, so `CrossRef::generate_ref_aux` could never carry markup. Fix (faithful to Perl `Scan.pm`/`CrossRef.pm`): store the title **node** and fill `<ltx:ref>` content with a deep clone of its children (`prepRefText` = `cloneNodes(trimChildNodes)`) — the clone happens before the MathML pass, so the cloned `<ltx:Math>` converts to `<math>` in the TOC exactly as in the body. `add_xml_node` was made a faithful `cloneNode` (uniquify `xml:id`, remap `fragid`, remap `idref`, drop `labels`) since its id branch was dead (`get_properties` keys the namespaced id by localname `id`), which had it copy ids verbatim; ref display copies additionally have `fragid` stripped (they are not anchors — Perl's snapshot predates `fragid`), so output is byte-identical to Perl. Guard: `103_toc_math_ref::toc_entry_keeps_section_title_math`. |
| **311** | 2026-07-23 (PR #348) | **Shared upstream bug, fixed ahead of Perl (surpass-Perl, RELEASE_CRITERIA §8).** A package loaded in a `standalone`/`\import` subfile's preamble lost its definitions while the document hooks it registered survived, ending an otherwise complete conversion with `Error:undefined:\ifpgf@external@grabshipout`; same-host Perl 0.8.8 errors identically. Fixed at the load seam, bindings' Perl-derived semantics unchanged: `content.rs::require_package` hoists the load's meaning-delta past brackets LaTeXML itself opened (the `subfile:<depth>` named scope, so an author's group nested inside a subfile preamble is not hoisted either); an author's own `{\usepackage{…}}` is untouched. Mechanism, refuted alternatives and guard names: OXIDIZED_DESIGN #65. Perl framing + trigger: KNOWN_PERL_ERRORS #55. |
| **345** | 2026-07-22 (PR #346) | **Bug (Rust-only) — `\usepackage{X}` didn't find `X.sty.rhai` on `$TEXINPUTS`** (reporter nasser1). `converter.rs::rhai_dispatch` resolved `<pkg>.sty.rhai` with `find_file(..., search_paths_only: true)`, which searches only `--path` + source dir and skips kpsewhich — the layer honouring `$TEXINPUTS`. (the non-standard extension is irrelevant to kpsewhich's `//` search.) Fix: `rhai_dispatch` takes a `RhaiScope`, and the chain gained a **fourth tier**, the TeX-tree probe (`search_paths_only=false`), running **last** after every compiled dispatcher declines; general across `.sty.rhai` and `.cls.rhai`. Not at tier 1, on two settled grounds: **cost** — tier 1 runs on every package/class request (64 on a plain acmart paper, each a distinct name so the per-name memo never hits), +26 ms/conversion linked and ~1.6 s on the subprocess backend; and **authority** — a stray `amsmath.sty.rhai` anywhere on the TeX tree silently replaced the compiled binding (`\begin{align}` → undefined `\align`). A local `.rhai` still overrides; a texmf one only fills a gap. Chain detail: `script_bindings_plan.md`; trust boundary: `SAFETY.md`. Guards: `16_texinputs_usepackage::{usepackage_finds_rhai_binding_on_texinputs, package_is_undefined_without_texinputs, texmf_rhai_does_not_shadow_a_compiled_binding, local_rhai_still_overrides_a_compiled_binding}`. |
| **341** | 2026-07-21 (PR) | **Bug (Rust-only) — split HTML pages dropped the default `LaTeXML.css`/`ltx-book.css` `<link>`s**; with `--splitat` only the root page loaded them (same-host Perl `latexmlc --format=html5 --splitat` links them on all pages). Root cause was one post-processing defect shared across *callers*: with no context node, `PostDocument::findnodes` left rust-libxml's `evaluate_checked` XPath context unset, so **relative** paths (`descendant::…`, `.//…`) silently matched nothing (unlike XML::LibXML's `$doc->findnodes`, which has document-node context) — `newDocument`'s `descendant::ltx:resource` / `.//processing-instruction(...)` copies returned empty, losing CSS resources AND `<?latexml?>` PIs. Also fixed: `add_nodes`→`add_xml_node` reused the source document's `xmlNs` in the sub-document (cross-document pointer → SIGSEGV). Fixes: bind the **root element** as context (not the document node — rust-libxml SIGSEGVs on it); resolve namespaces in the **target** document; use absolute `//` for the resource/PI copies (before-root PIs); plus Perl's `addDate` (Post.pm L774) and dedup of a duplicated class-copy block. Same repair applied to Split navigation, CrossRef glossaryref/decl_id and the graphics/documentclass PI reads. Guards: `13_split_css_links::split_children_load_default_stylesheets`, `document::tests::findnodes_resolves_relative_axes_without_context_node`. |
| **312** | 2026-07-21 (PR) | **Bug (Rust-only) — a stale/empty stylesheet in the destination was truncated to 0 bytes, not overwritten.** REVISED: the first "environmental" verdict was wrong — the reporter's files *were* empty, but we emptied them, and the browser therefore rendered flush-left (the filed not-centered complaint). With a `LaTeXML.css`/`ltx-article.css` already in the destination dir (which is on the resource search path — e.g. left empty by a failed run), `XSLT::copy_resource` found that stale dest file as its own source and `fs::copy`'d it onto itself: the `path != dest` guard was a string compare that misses the same file reached via a different spelling (relative vs absolute). Fixed with a canonicalize-based `same_file` check at all three copy sites (`copy_resource`, `copy_param_resources`, `copy_css_imports`), rewriting the embedded canonical bytes so a stale leftover is repaired (idempotent). PR #326's vanilla-CSS re-sync + `.htm` inference is orthogonal; MathJax-4 screenshot out of scope. Guard: `004_stale_css_overwrite::stale_css_in_dest_is_overwritten_with_bundled`. |
| **321** | 2026-07-21 (PR) | **Enhancement — `LookupDefinition` + push/unshift hooks.** Perl `LookupDefinition($t)` returns an installed definition; BookML then mutates its hook lists in place (`push(@{ $$def{afterConstruct} }, sub{…})`). Added `LookupDefinition(cs)` → a `Definition` proxy (`()` when undefined, mirroring Perl undef) with `push`/`unshift` for all hook families: `before/afterDigest` (DefConstructor/DefPrimitive/DefMath) and `before/afterConstruct` + `afterDigestBody` (DefConstructor-only — a construct/body push onto a Primitive/MathPrimitive/Macro is a clear script error, since MathPrimitive's construct fields never execute). Each push clones the front def, splices the hook and re-installs at **`Scope::InPlace`** — same-level, without touching the save stack (Perl `State.pm:175` `'inplace'`), so the patch is the active meaning immediately, sequential pushes accumulate, and it rides exactly as long as the existing binding. (An earlier `Scope::Global` re-install promoted a locally-bound def to global — harmless for BookML but a real divergence; corrected in PR #333, @xworld21. Detail: `script_bindings_plan.md`.) Guards: `script_bindings::tests::{lookup_definition_pushes_and_accumulates_digest_hooks, lookup_definition_construct_hook_on_primitive_errors}`, `30_script_bindings::lookup_definition_pushes_construct_hooks_end_to_end`. Completes the #314–#321 Rhai cluster. |
| **320** | 2026-07-21 (PR) | **Enhancement — expose the version.** Perl's `$LaTeXML::VERSION`, which BookML gates on numerically in both `.ltxml` (`$LaTeXML::VERSION <= v0.8.8`) and `utils.xsl` (`b:version-leq($LATEXML_VERSION,…)`), had no Rhai equivalent. Maintainer decision: expose **our own** `latexml_oxide` crate version, not the emulated Perl one, as a bare `X.Y.Z` (Cargo's `_MAJOR`/`_MINOR`/`_PATCH` drop the `-rc1`, e.g. `0.7.5`). New Rhai `LaTeXMLVersion()` reads `core_interface::LATEXML_VERSION`, published to state at session init (reverse-dep-safe, mirroring Perl's `$LaTeXML::VERSION`). Guards: `latexml_version_is_bare_xyz`, `latexml_version_binding_reads_published_version`. The XSLT `LATEXML_VERSION` parameter follow-up **landed 2026-07-21** (guard `tests/10_xslt_generator_version.rs`; injected at `post.rs:577`, before the user `--xsltparameter` loop, so an override or Perl's `LATEXML_VERSION:TEST` sentinel wins) — see SYNC_STATUS. |
| **318** | 2026-07-21 (PR) | **Enhancement — external commands from bindings.** BookML shells out to `latexmk`/`dvisvgm` during digestion (needs image size/depth back); Perl `.ltxml` runs `system()` freely, Rhai was sandboxed. Added `Command`, a thin mirror of `std::process::Command` (`Command(program)` + `.arg`/`.args`/`.env`/`.current_dir`/`.output()` → `#{status, success, stdout, stderr}`) so users read the std docs; runs during digestion via #316's `call_deferred_body`. **Security:** a `.rhai` beside an untrusted source auto-loads (`converter.rs::rhai_dispatch`), so command exec is a real RCE surface — allowed by default (Perl-faithful) but blockable via `LATEXML_DISABLE_SHELL_ESCAPE`, the pdflatex-shell-escape idiom. Any untrusted-input deployment shipping `--features runtime-bindings` must set it, or build without the feature; `SAFETY.md` updated. No hidden per-call timeout (like `system()`); bounded by the conversion wall-time. Guard: `command_output_runs_by_default_and_is_blockable_via_env`. |
| **316** | 2026-07-20 (PR) | **Enhancement — registration from inside a definition body.** Calling `DefPrimitive` (or any `Def*`/`DeclareOption`) from inside a definition body failed `registration called outside a script load` and aborted digestion. Registration wiring resolves the script's `(engine, AST)` from the `CURRENT_SCRIPT` stack, which was pushed only during `load_script` — so a body running during **digestion** found it empty. Perl has no such restriction. Fix: a shared `call_deferred_body` helper (mod.rs) pushes the body's own captured `(engine, AST)` for the duration of every deferred `FnPtr` call — all 14 sites in `wire.rs` (primitive/macro/constructor/conditional/option/columntype/reversion/sizer/rewrite/matcher). Per-body, not a single ambient: each script has its own engine/AST (`SCRIPT_CACHE`). Guard: `def_primitive_nested_inside_a_primitive_body`. |
| **317** | 2026-07-20 (PR) | **Enhancement — RequireResource option map.** The Rhai `RequireResource` accepted only a single string (the resource path). Added the Perl `RequireResource($resource, %options)` form: a second Rhai object-map carrying `type` (mime), `media`, and `content`. Perl's option name `type` maps to the `Resource`'s `mimetype`; unknown keys are ignored. The **core `require_resource` already faithfully ported the full logic** (mime inference from extension, empty/mime checks, pending-resource push) — the gap was purely the binding, which only set `name`. Single-arg form unchanged (still infers mime). Guard: `require_resource_option_map_sets_type_media_content`. |
| **319** | 2026-07-20 (PR) | **Enhancement — diagnostics surface completed.** Rhai bindings had only `Warn`/`Error`. Added the rest of Perl's `Common/Error.pm` surface: `Info(cat,obj,msg)` (mirrors `Warn`), `Fatal(cat,obj,msg)` (surfaces as a Rhai error so the script/conversion aborts, like Perl `Fatal` dies — `Fatal!(Internal, Unexpected, "cat:obj msg")`), `NoteSTDERR`/`NoteLog` (→ `Note!`/`NoteLog!`), and the progress trio `ProgressStep`/`ProgressSpinup`/`ProgressSpindown` (→ `error::progress_step`/`note_begin`/`note_end`). Also fixed a macro-hygiene bug in `NoteLog!` (bare `println_stderr!` → `$crate::println_stderr!`, so it's callable cross-crate like `Note!`). Guard: `diagnostics_surface_info_notes_progress_and_fatal`. |
| **315** | 2026-07-20 (PR) | **Bug (Rust-only; internal-representation leak).** `LookupString("class_options")` leaked the internal `VecDequeStored[…]` Debug repr. The first fix (#325, comma-join) was **reverted** (#327), then re-fixed (#328) — an over-general heuristic in the *generic* `VecDequeStored` reversion, and Perl has no canonical list→string anyway (an arrayref stringifies to `ARRAY(0x…)`; `join(',')` is applied only at option sites). Correct fix: `LookupString` is **scalar-only** (`state.rs`), returning `""` for a list value (gated by new `Stored::is_list`) and never the Debug repr, plus a Perl-style `LookupValue` binding returning the list AS a Rhai array (`["a4paper","12pt"]`, mirroring Perl's arrayref) so callers iterate/join structurally. `LookupTokens` unchanged (still reverts to concatenated tokens; #314). Foundational for #321. Guards: `lookup_string_on_list_value_is_empty_not_leaked`, `lookup_value_on_list_returns_rhai_array`. |
| **314** | 2026-07-20 (PR) | **Bug (Rust-only; no Perl analogue — RefCell borrow model).** `LookupTokens("class_options")` from a Rhai binding panicked `RefCell already borrowed`. `class_options` is a `Stored::VecDequeStored`; `state::lookup_tokens` (`state.rs:1696`) held its immutable `state!()` borrow across the queue→Tokens conversion, which reverts each `String` item through `mouth::tokenize_internal` — a *mutable* STATE borrow → `borrow_mut` under a live `borrow`. The adjacent `Stored::String` branch already dropped the borrow before tokenizing; the VecDeque branch did not. Fixed by cloning the queue and dropping the borrow first (the general root — `lookup_tokens` is the only fn producing `Option<Tokens>`, hence the sole reentry site; arena is re-entrant so unaffected). Guard: `script_bindings::tests::lookup_tokens_on_vecdeque_value_does_not_panic`. |
| **309** | 2026-07-20 (PR #310) | **Shared upstream bug, fixed ahead of Perl.** `\subimport`ing a child whose preamble is `\documentclass[12pt]{article}` warned `missing_file:12pt`: `standalone.sty.ltxml` L24-33 comma-splits the intercepted `\documentclass`'s optional argument and `RequirePackage`s each element, but that argument holds class *options*, not packages. Same-host Perl warns identically, so this is a documented divergence (OXIDIZED_DESIGN #63, KNOWN_PERL_ERRORS #54). The loop is now gated as `standalone.sty` L604-614 gates it (class must be `standalone`) and limited to the options `standalone.cls` turns into a **same-named** package load (`tikz`, `pstricks`, `preview`, `varwidth`, `multido`), preserving upstream LaTeXML#1432's `\documentclass[tikz]{standalone}`. Also fixes `[border=2pt]{standalone}`. Follow-on to #293. Guard: `06_cluster_regressions::standalone_subimport_documentclass_no_spurious_require`. |
| **192** | 2026-07-17 | Compile-time string interning. (Was still listed as open in the 2026-07-19 refresh.) |
| **82** | 2026-07-17 | Manually copy perldoc over as rustdoc. (Was still listed as open in the 2026-07-19 refresh.) |
| **304** | 2026-07-19 | **Not reproducible — environment, not latexml_oxide.** "TEXINPUTS ignored for `\input{my_core}`" on a Linux Mint VirtualBox guest with the tree on a `/mnt/g` shared folder. Unreproducible here across five matched factors (multi-part `TEXINPUTS`, trailing/empty colons, `//` recursion, a real `st_nlink=1` ntfs-3g mount, the bundled TL2025 lib); resolved by a VM reboot on the reporter's side (their `KPATHSEA_DEBUG=32` log then showed the file resolving) — positively excluding our resolver paths. **Two real defects were found while investigating** (PR #308): (a) `select_kpaths()` discarded a failed `Kpaths::new()` with `.ok()?`, silently disabling ALL file resolution — fixed upstream in kpathsea 0.3.4 (dginev/rust-kpathsea#25, degrading program-name anchor) plus a subprocess fallback here; (b) settled dead-end, added mid-investigation then **removed**: a per-lookup subprocess fallback cost one `kpsewhich` spawn per distinct missing file (30 spawns / 20 missing packages; 2.54 s against a 0.20 s conversion). **Durable outcome:** every log now records the resolved kpathsea backend, so this report class is diagnosable from an ordinary log. Guard: `003_kpathsea_backend_resolution.rs`. |
| **305** | 2026-07-18 | "Where is latexml.sty?" — **out of scope by design.** Raw `.sty` files are not shipped; the compiled `latexml_sty.rs` binding always applies, needs no path, and cannot be overridden by a raw `latexml.sty` on the search path even under `--includestyles` (precedence is decided by `input_definitions`'s `_loaded` gate, not `find_file_aux`'s `notex` gate — see WISDOM #63). |
| **307** | 2026-07-19 | **Downstream of #304, not an `\iflatexml` bug.** `\iflatexml\else\usepackage[fit]{truncate}…\fi` took the `\else` branch. `\iflatexml` is defined only by loading `latexml.sty` — identical in Perl LaTeXML (`latexml.sty.ltxml:27`); neither predefines it, and both emit the same `undefined` error and take `\else` for a bare `\iflatexml` (verified same-host). The reporter's log proves `\usepackage{latexml}` lives inside `my_core.tex` (`latexml_sty.rs` loads the moment that file is processed), so while #304 made it unresolvable the conditional was never defined. Their exact preamble converts cleanly on released 0.7.4 here. No code change. |
| **301** | 2026-07-18 (branch) | Build: `cargo test` on the published `latexml` crate panicked in the `exemption_keys_have_unique_stems` unit test — "no .tex fixtures found under tests/ — wrong CWD?" (reported by a Nixpkgs `buildRustPackage` consumer). Root cause: that `#[cfg(test)]` test lives under `src/` (so it *ships* in the package) yet scanned the **CWD-relative** `Path::new("tests")` at run time, while `tests/` is deliberately EXCLUDED from the tarball (Cargo.toml `exclude`, crates.io 10 MiB cap). Fixed by locating the corpus via `env!("CARGO_MANIFEST_DIR")` (CWD-independent) and treating an absent corpus as a legitimate skip, not a failure (the audit helper returns `None`). Guards: `util::test::exemption_audit::{audit_skips_when_corpus_absent, audit_detects_duplicate_stems}`. |
| **299** | 2026-07-17 (branch) | Bug (Rust-only distribution machinery; no Perl analogue): the standalone 0.7.4 binary printed a `latex_dump:mismatch` TeXLive warning on every run under TL2026. Two defects: `build.rs`'s generated stamp check (then named `compare_stamp_to_ambient`, since replaced) compared the exact kpathsea version *string*, so a within-year patch bump (6.4.1 vs 6.4.2) tripped it even though the embedded TL2026 dump is correct for a TL2026 install — and that string is not a reliable cross-year discriminator anyway (TL2023/TL2025 share it); and the remedy named `tools/make_formats.sh`, which a standalone-binary user has no source tree for. Fix: compare TeX-Live **years** (already how the dump is selected) via a testable `dump_paths::dump_year_mismatch_warning(dump_year, ambient_year, from_embedded)` — warn only on a genuine year mismatch, stay silent when the years match or the ambient year is undetectable; the embedded-path message drops the `tools/` reference. Guard: `dump_paths::stamp_check_tests` (5 tests / 6 scenarios, incl. the reporter's TL2026/TL2026 silent case). |
| **293** | 2026-07-17 (branch) | Bug: `\subimport`ing a `standalone` child with a `\documentclass{article}` preamble warned `missing_file:article`. Root cause: `standalone_sty.rs`'s `\@standalone@documentclass[]{}` intercept required the **mandatory** class-name arg, whereas Perl `standalone.sty.ltxml` L24-33 requires the **optional `[]`** list (`$packages = $_[1]`) and ignores the class. Fixed by binding the optional arg; guard `06_cluster_regressions::standalone_subimport_documentclass_no_spurious_require`. |
| **291** | 2026-07-17 (branch) | Bug: `\setcounter{tocdepth}{0}` ignored — the full ToC rendered instead of chapters only. `Post::CrossRef::gen_toc` ignored the `<ltx:TOC>` `select`/`lists` attributes (hardcoded `NORMAL_TOC_TYPES` + `inlist=="toc"`), so `tocdepth`, which rides on `select`, had no effect; fixed by porting Perl `CrossRef.pm::gentoc` L246-261 (type filter from `select`, `inlist_match` on `lists`). The same defect silently broke the LaTeXML#2316 arXiv-fork "abstract exempt from `\tableofcontents`" half, which the fix completes. **Also hardened:** (a) a *negative* `tocdepth` (`\setcounter{tocdepth}{-1}` = parts only) panicked — `select` cast `tocdepth+1` through `as usize`, overflowing on negatives (debug panic; release over-full ToC); now computed in signed space per `latex_constructs.pool.ltxml` L727-733 (`0 .. $td` is empty for negative `$td`). (b) Honoring `lists` also repaired `\listoffigures`/`\listoftables`, which the hardcoded `"toc"` bucket had listed a document *section* in. Guards: `06_cluster_regressions::{tocdepth_select_restricts_the_toc, nav_toc_includes_abstract_issue_2316, tocdepth_negative_is_parts_only_no_panic, listoffigures_lists_figures_not_toc_sections}`. |
| **292** | 2026-07-17 (branch) | Bug: a user `--stylesheet` that `<xsl:import>`s the engine via `urn:x-LaTeXML:XSLT:LaTeXML-html5.xsl` failed to parse ("unable to load urn:…"). Root cause: `xslt.rs`'s embedded-XSLT libxml2 input callback matched only the `embed:///` scheme, and was installed only on the embed fallback path — so a disk-loaded user stylesheet using the LaTeXML-canonical `urn:` scheme (which Perl resolves via an XML catalog) had no resolver. Fixed by resolving `urn:x-LaTeXML:XSLT:` (and libxml2's relative-composed `urn:X`) against the embedded table by basename, and installing the callback at the parse chokepoint. Guard `63_xslt_custom_stylesheet::custom_stylesheet_imports_engine_by_urn`. |
| **191** | 2026-07-09 | CLI options — `clap` 4 derive adopted + every `latexmlc` option whose engine feature works end-to-end wired. Closed via #276. Which options remain unimplemented and why (option C: absent features stay hard parse errors) is the rationale record below. |
| **101** | 2026-05-26 | Binary size accepted as structural (~47 MB maxperf; ~60k binding functions, no fat generic to shrink). [`RELEASE_CRITERIA.md`](RELEASE_CRITERIA.md) §2, [`PERFORMANCE.md`](../performance/PERFORMANCE.md) build-pipeline. |
| **217** | 2026-06-08 | macOS portable use RESOLVED — suite green on macos-15 arm64; kpathsea 0.3 subprocess fallback; text-node UAF fixed (WISDOM #58). [`archive/PORTABILITY_MACOS_PROBE_2026-06-07.md`](../archive/PORTABILITY_MACOS_PROBE_2026-06-07.md). |
| **93** | 2026-06-09 | Declarative binding dialect → superseded by #247's runtime interpreter + shared `ConstructorBuilder` spine. [`BINDING_DSL_ARCHITECTURE.md`](../parity/BINDING_DSL_ARCHITECTURE.md). |
| **199** | 2026-06-12 | HTML-dialect RelaxNG schema — see [`SCHEMA_DOCUMENTATION.md`](../performance/SCHEMA_DOCUMENTATION.md). |
| **247** | 2026-06-12 | Runtime (Rhai) binding interpreter landed (`runtime-bindings` feature, on by default; the `script-bindings` alias was dropped pre-publish). [`script_bindings_plan.md`](../parity/script_bindings_plan.md). |
| **183** | 2026-06-16 | String-literal max-width style item. |
| **171** | 2026-06-16 | XML-replacement parser — resolved as a component of #247 (`ReplacementOp` AST + winnow, decision recorded in [`BINDING_DSL_ARCHITECTURE.md`](../parity/BINDING_DSL_ARCHITECTURE.md)). |
| **127** | 2026-06-18 | 64-bit numbers — fixed by exact `xn_over_d`-style fixed-point unit conversion (`numeric_ops::fixpoint_unit`), bit-exact vs pdftex. The storage was never the bug; f64 unit *conversion* was. |

## Rationale record: CLI option coverage (from #191, closed 2026-07-09 via #276)

Authoritative Perl spec = `getopt_specification` in `Common/Config.pm`
(~82 canonical omni options; the `latexmlc` union). Existing Perl-name
aliases: `--destination`, `--noparse`, `--presentationmathml`,
`--contentmathml`, `--xmath`, and (2026-07-09) `--nopresentationmathml`,
`--nocontentmathml`, `--nokeepXMath`, `--navtoc`.

**Landed 2026-07-09** — every flag whose engine feature already exists is now
wired (option C: wire real features, keep the parser strict — see below).
- *Batch 1:* `--strict` (State `STRICT`), `--includestyles` (State
  `INCLUDE_STYLES`/`INCLUDE_CLASSES`), `--comments` (positive of `--nocomments`),
  `--xml` (= `--format=xml`), `--embed` (= `--whatsout=fragment`), `--nopost`,
  `--nosplit`, the math-rep negations `--nopmml`/`--nocmml`/`--nomathtex`/
  `--noxmath`, the `--navtoc` alias. `--debug` already existed.
- *Batch 2:* `--timestamp` (`=0` omits; XSLT `TIMESTAMP` footer param),
  `--icon` (XSLT `ICON` param + favicon copy), `--nographicimages`/
  `--graphicimages` (gate the Graphics post-phase), and the positive-complement
  parity flags `--numbersections`, `--mathparse`, `--invisibletimes`,
  `--defaultresources`.

- **Postponed to next release:** `--validate`/`--novalidate` — real RelaxNG
  validation is gated on the rust-libxml fork providing a safe RelaxNG interface
  (published as `libxml 0.3.16`); `Post::Document::validate()` is a stub today.
  Plan in [`SYNC_STATUS.md`](../SYNC_STATUS.md).
- **Remaining cheap gaps (feature exists / near):** `--profile`
  (biggest — `fragment`/`math`/`article`/…; planned as **TOML** profiles
  deserialized into the clap option struct, not Perl `.opt` — design in
  [`OXIDIZED_DESIGN_FUTURE_WORK.md`](../parity/OXIDIZED_DESIGN_FUTURE_WORK.md)
  "TOML profiles") + its `--mode` alias.
- **Feature gaps (flag absent because the feature is) — kept as hard parse
  errors, NOT stubbed:** `--mathimages` / `--mathsvg` / `--pictureimages`
  (need the unwired LaTeXImages latex+dvipng pipeline), `--svg` (**deferred**:
  the HTML5 XSLT already renders `<ltx:picture>` as inline `<svg>` by default,
  so the standalone `svg.rs` post-processor is redundant + produces divergent,
  unverified output — verified 2026-07-09), `--jats` / `--html4` / `--tex` /
  `--box` output, `--crossref` / `--bibliography` / `--index` /
  `--permutedindex` / `--splitbibliography`, `--openmath` / `--unicodemath` /
  `--plane1` / `--hackplane1` / `--parallelmath` / `--linelength` /
  `--mathimagemagnification`, `--scan`/`--noscan` (Scan IS wired as post Phase 2
  but the off-switch is parked with the crossref cluster) / `--prescan` /
  `--dbfile` / `--urlstyle` / `--base` / `--omitdoctype` (no DTD), daemon mode
  (`--port`, `--expire`, `--cache_key`, `--address`, `--autoflush`).
- **Intentional non-goals:** `--output` (we keep `--destination` / `--dest`).

**Design decision (2026-07-09, option C).** Deferred-feature flags are
*deliberately left as clap "unexpected argument" errors* rather than
accept-and-warn stubs — a strict parser never silently accepts a flag whose
feature is absent (no misleading success). Revisit per-flag as each feature
lands. The parser-library question the issue raised (clap vs alternatives) is
settled: clap 4 derive, adopted.

## Not tracked by an issue

Some release gates have no issue number — the license, safety and portability
items in [`RELEASE_CRITERIA.md`](RELEASE_CRITERIA.md). Everything else is a row
above.
