--------------------------------------------------------------------------------
5. RUST DEPENDENCY LICENSES
--------------------------------------------------------------------------------

The Rust crates statically linked into the binary, grouped by license.
Generated by `cargo about` from the release lockfile.

Overview:
{{#each overview}}  {{{name}}} ({{count}})
{{/each}}

{{#each licenses}}
================================================================================
{{{name}}} ({{id}})
Applies to: {{#each used_by}}{{{crate.name}}} {{crate.version}}{{#unless @last}}, {{/unless}}{{/each}}
--------------------------------------------------------------------------------
{{{text}}}

{{/each}}
